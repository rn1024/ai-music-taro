'use strict';

var react = require('react');

let globalZIndex = 500;
function useZIndex(baseZ = 500) {
  const [zIndex, setZIndex] = react.useState(baseZ);
  const acquire = react.useCallback(() => {
    globalZIndex += 2;
    setZIndex(globalZIndex);
    return globalZIndex;
  }, []);
  const release = react.useCallback(() => {
    // optional: reduce globalZIndex when releasing
  }, []);
  return {
    zIndex,
    acquire,
    release
  };
}

exports.useZIndex = useZIndex;
//# sourceMappingURL=index.cjs.map
