'use strict';

var require$$0 = require('react');
var classNames = require('classnames');
var components = require('@tarojs/components');
var Taro = require('@tarojs/taro');
var client = require('react-dom/client');

var jsxRuntime = {exports: {}};

var reactJsxRuntime_production_min = {};

/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var hasRequiredReactJsxRuntime_production_min;

function requireReactJsxRuntime_production_min () {
	if (hasRequiredReactJsxRuntime_production_min) return reactJsxRuntime_production_min;
	hasRequiredReactJsxRuntime_production_min = 1;
var f=require$$0,k=Symbol.for("react.element"),l=Symbol.for("react.fragment"),m=Object.prototype.hasOwnProperty,n=f.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,p={key:true,ref:true,__self:true,__source:true};
	function q(c,a,g){var b,d={},e=null,h=null;void 0!==g&&(e=""+g);void 0!==a.key&&(e=""+a.key);void 0!==a.ref&&(h=a.ref);for(b in a)m.call(a,b)&&!p.hasOwnProperty(b)&&(d[b]=a[b]);if(c&&c.defaultProps)for(b in a=c.defaultProps,a) void 0===d[b]&&(d[b]=a[b]);return {$$typeof:k,type:c,key:e,ref:h,props:d,_owner:n.current}}reactJsxRuntime_production_min.Fragment=l;reactJsxRuntime_production_min.jsx=q;reactJsxRuntime_production_min.jsxs=q;
	return reactJsxRuntime_production_min;
}

var reactJsxRuntime_development = {};

/**
 * @license React
 * react-jsx-runtime.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var hasRequiredReactJsxRuntime_development;

function requireReactJsxRuntime_development () {
	if (hasRequiredReactJsxRuntime_development) return reactJsxRuntime_development;
	hasRequiredReactJsxRuntime_development = 1;

	if (process.env.NODE_ENV !== "production") {
	  (function() {

	var React = require$$0;

	// ATTENTION
	// When adding new symbols to this file,
	// Please consider also adding to 'react-devtools-shared/src/backend/ReactSymbols'
	// The Symbol used to tag the ReactElement-like types.
	var REACT_ELEMENT_TYPE = Symbol.for('react.element');
	var REACT_PORTAL_TYPE = Symbol.for('react.portal');
	var REACT_FRAGMENT_TYPE = Symbol.for('react.fragment');
	var REACT_STRICT_MODE_TYPE = Symbol.for('react.strict_mode');
	var REACT_PROFILER_TYPE = Symbol.for('react.profiler');
	var REACT_PROVIDER_TYPE = Symbol.for('react.provider');
	var REACT_CONTEXT_TYPE = Symbol.for('react.context');
	var REACT_FORWARD_REF_TYPE = Symbol.for('react.forward_ref');
	var REACT_SUSPENSE_TYPE = Symbol.for('react.suspense');
	var REACT_SUSPENSE_LIST_TYPE = Symbol.for('react.suspense_list');
	var REACT_MEMO_TYPE = Symbol.for('react.memo');
	var REACT_LAZY_TYPE = Symbol.for('react.lazy');
	var REACT_OFFSCREEN_TYPE = Symbol.for('react.offscreen');
	var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
	var FAUX_ITERATOR_SYMBOL = '@@iterator';
	function getIteratorFn(maybeIterable) {
	  if (maybeIterable === null || typeof maybeIterable !== 'object') {
	    return null;
	  }

	  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];

	  if (typeof maybeIterator === 'function') {
	    return maybeIterator;
	  }

	  return null;
	}

	var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;

	function error(format) {
	  {
	    {
	      for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
	        args[_key2 - 1] = arguments[_key2];
	      }

	      printWarning('error', format, args);
	    }
	  }
	}

	function printWarning(level, format, args) {
	  // When changing this logic, you might want to also
	  // update consoleWithStackDev.www.js as well.
	  {
	    var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
	    var stack = ReactDebugCurrentFrame.getStackAddendum();

	    if (stack !== '') {
	      format += '%s';
	      args = args.concat([stack]);
	    } // eslint-disable-next-line react-internal/safe-string-coercion


	    var argsWithFormat = args.map(function (item) {
	      return String(item);
	    }); // Careful: RN currently depends on this prefix

	    argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
	    // breaks IE9: https://github.com/facebook/react/issues/13610
	    // eslint-disable-next-line react-internal/no-production-logging

	    Function.prototype.apply.call(console[level], console, argsWithFormat);
	  }
	}

	// -----------------------------------------------------------------------------

	var enableScopeAPI = false; // Experimental Create Event Handle API.
	var enableCacheElement = false;
	var enableTransitionTracing = false; // No known bugs, but needs performance testing

	var enableLegacyHidden = false; // Enables unstable_avoidThisFallback feature in Fiber
	// stuff. Intended to enable React core members to more easily debug scheduling
	// issues in DEV builds.

	var enableDebugTracing = false; // Track which Fiber(s) schedule render work.

	var REACT_MODULE_REFERENCE;

	{
	  REACT_MODULE_REFERENCE = Symbol.for('react.module.reference');
	}

	function isValidElementType(type) {
	  if (typeof type === 'string' || typeof type === 'function') {
	    return true;
	  } // Note: typeof might be other than 'symbol' or 'number' (e.g. if it's a polyfill).


	  if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing  || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden  || type === REACT_OFFSCREEN_TYPE || enableScopeAPI  || enableCacheElement  || enableTransitionTracing ) {
	    return true;
	  }

	  if (typeof type === 'object' && type !== null) {
	    if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || // This needs to include all possible module reference object
	    // types supported by any Flight configuration anywhere since
	    // we don't know which Flight build this will end up being used
	    // with.
	    type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== undefined) {
	      return true;
	    }
	  }

	  return false;
	}

	function getWrappedName(outerType, innerType, wrapperName) {
	  var displayName = outerType.displayName;

	  if (displayName) {
	    return displayName;
	  }

	  var functionName = innerType.displayName || innerType.name || '';
	  return functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName;
	} // Keep in sync with react-reconciler/getComponentNameFromFiber


	function getContextName(type) {
	  return type.displayName || 'Context';
	} // Note that the reconciler package should generally prefer to use getComponentNameFromFiber() instead.


	function getComponentNameFromType(type) {
	  if (type == null) {
	    // Host root, text node or just invalid type.
	    return null;
	  }

	  {
	    if (typeof type.tag === 'number') {
	      error('Received an unexpected object in getComponentNameFromType(). ' + 'This is likely a bug in React. Please file an issue.');
	    }
	  }

	  if (typeof type === 'function') {
	    return type.displayName || type.name || null;
	  }

	  if (typeof type === 'string') {
	    return type;
	  }

	  switch (type) {
	    case REACT_FRAGMENT_TYPE:
	      return 'Fragment';

	    case REACT_PORTAL_TYPE:
	      return 'Portal';

	    case REACT_PROFILER_TYPE:
	      return 'Profiler';

	    case REACT_STRICT_MODE_TYPE:
	      return 'StrictMode';

	    case REACT_SUSPENSE_TYPE:
	      return 'Suspense';

	    case REACT_SUSPENSE_LIST_TYPE:
	      return 'SuspenseList';

	  }

	  if (typeof type === 'object') {
	    switch (type.$$typeof) {
	      case REACT_CONTEXT_TYPE:
	        var context = type;
	        return getContextName(context) + '.Consumer';

	      case REACT_PROVIDER_TYPE:
	        var provider = type;
	        return getContextName(provider._context) + '.Provider';

	      case REACT_FORWARD_REF_TYPE:
	        return getWrappedName(type, type.render, 'ForwardRef');

	      case REACT_MEMO_TYPE:
	        var outerName = type.displayName || null;

	        if (outerName !== null) {
	          return outerName;
	        }

	        return getComponentNameFromType(type.type) || 'Memo';

	      case REACT_LAZY_TYPE:
	        {
	          var lazyComponent = type;
	          var payload = lazyComponent._payload;
	          var init = lazyComponent._init;

	          try {
	            return getComponentNameFromType(init(payload));
	          } catch (x) {
	            return null;
	          }
	        }

	      // eslint-disable-next-line no-fallthrough
	    }
	  }

	  return null;
	}

	var assign = Object.assign;

	// Helpers to patch console.logs to avoid logging during side-effect free
	// replaying on render function. This currently only patches the object
	// lazily which won't cover if the log function was extracted eagerly.
	// We could also eagerly patch the method.
	var disabledDepth = 0;
	var prevLog;
	var prevInfo;
	var prevWarn;
	var prevError;
	var prevGroup;
	var prevGroupCollapsed;
	var prevGroupEnd;

	function disabledLog() {}

	disabledLog.__reactDisabledLog = true;
	function disableLogs() {
	  {
	    if (disabledDepth === 0) {
	      /* eslint-disable react-internal/no-production-logging */
	      prevLog = console.log;
	      prevInfo = console.info;
	      prevWarn = console.warn;
	      prevError = console.error;
	      prevGroup = console.group;
	      prevGroupCollapsed = console.groupCollapsed;
	      prevGroupEnd = console.groupEnd; // https://github.com/facebook/react/issues/19099

	      var props = {
	        configurable: true,
	        enumerable: true,
	        value: disabledLog,
	        writable: true
	      }; // $FlowFixMe Flow thinks console is immutable.

	      Object.defineProperties(console, {
	        info: props,
	        log: props,
	        warn: props,
	        error: props,
	        group: props,
	        groupCollapsed: props,
	        groupEnd: props
	      });
	      /* eslint-enable react-internal/no-production-logging */
	    }

	    disabledDepth++;
	  }
	}
	function reenableLogs() {
	  {
	    disabledDepth--;

	    if (disabledDepth === 0) {
	      /* eslint-disable react-internal/no-production-logging */
	      var props = {
	        configurable: true,
	        enumerable: true,
	        writable: true
	      }; // $FlowFixMe Flow thinks console is immutable.

	      Object.defineProperties(console, {
	        log: assign({}, props, {
	          value: prevLog
	        }),
	        info: assign({}, props, {
	          value: prevInfo
	        }),
	        warn: assign({}, props, {
	          value: prevWarn
	        }),
	        error: assign({}, props, {
	          value: prevError
	        }),
	        group: assign({}, props, {
	          value: prevGroup
	        }),
	        groupCollapsed: assign({}, props, {
	          value: prevGroupCollapsed
	        }),
	        groupEnd: assign({}, props, {
	          value: prevGroupEnd
	        })
	      });
	      /* eslint-enable react-internal/no-production-logging */
	    }

	    if (disabledDepth < 0) {
	      error('disabledDepth fell below zero. ' + 'This is a bug in React. Please file an issue.');
	    }
	  }
	}

	var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
	var prefix;
	function describeBuiltInComponentFrame(name, source, ownerFn) {
	  {
	    if (prefix === undefined) {
	      // Extract the VM specific prefix used by each line.
	      try {
	        throw Error();
	      } catch (x) {
	        var match = x.stack.trim().match(/\n( *(at )?)/);
	        prefix = match && match[1] || '';
	      }
	    } // We use the prefix to ensure our stacks line up with native stack frames.


	    return '\n' + prefix + name;
	  }
	}
	var reentry = false;
	var componentFrameCache;

	{
	  var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map;
	  componentFrameCache = new PossiblyWeakMap();
	}

	function describeNativeComponentFrame(fn, construct) {
	  // If something asked for a stack inside a fake render, it should get ignored.
	  if ( !fn || reentry) {
	    return '';
	  }

	  {
	    var frame = componentFrameCache.get(fn);

	    if (frame !== undefined) {
	      return frame;
	    }
	  }

	  var control;
	  reentry = true;
	  var previousPrepareStackTrace = Error.prepareStackTrace; // $FlowFixMe It does accept undefined.

	  Error.prepareStackTrace = undefined;
	  var previousDispatcher;

	  {
	    previousDispatcher = ReactCurrentDispatcher.current; // Set the dispatcher in DEV because this might be call in the render function
	    // for warnings.

	    ReactCurrentDispatcher.current = null;
	    disableLogs();
	  }

	  try {
	    // This should throw.
	    if (construct) {
	      // Something should be setting the props in the constructor.
	      var Fake = function () {
	        throw Error();
	      }; // $FlowFixMe


	      Object.defineProperty(Fake.prototype, 'props', {
	        set: function () {
	          // We use a throwing setter instead of frozen or non-writable props
	          // because that won't throw in a non-strict mode function.
	          throw Error();
	        }
	      });

	      if (typeof Reflect === 'object' && Reflect.construct) {
	        // We construct a different control for this case to include any extra
	        // frames added by the construct call.
	        try {
	          Reflect.construct(Fake, []);
	        } catch (x) {
	          control = x;
	        }

	        Reflect.construct(fn, [], Fake);
	      } else {
	        try {
	          Fake.call();
	        } catch (x) {
	          control = x;
	        }

	        fn.call(Fake.prototype);
	      }
	    } else {
	      try {
	        throw Error();
	      } catch (x) {
	        control = x;
	      }

	      fn();
	    }
	  } catch (sample) {
	    // This is inlined manually because closure doesn't do it for us.
	    if (sample && control && typeof sample.stack === 'string') {
	      // This extracts the first frame from the sample that isn't also in the control.
	      // Skipping one frame that we assume is the frame that calls the two.
	      var sampleLines = sample.stack.split('\n');
	      var controlLines = control.stack.split('\n');
	      var s = sampleLines.length - 1;
	      var c = controlLines.length - 1;

	      while (s >= 1 && c >= 0 && sampleLines[s] !== controlLines[c]) {
	        // We expect at least one stack frame to be shared.
	        // Typically this will be the root most one. However, stack frames may be
	        // cut off due to maximum stack limits. In this case, one maybe cut off
	        // earlier than the other. We assume that the sample is longer or the same
	        // and there for cut off earlier. So we should find the root most frame in
	        // the sample somewhere in the control.
	        c--;
	      }

	      for (; s >= 1 && c >= 0; s--, c--) {
	        // Next we find the first one that isn't the same which should be the
	        // frame that called our sample function and the control.
	        if (sampleLines[s] !== controlLines[c]) {
	          // In V8, the first line is describing the message but other VMs don't.
	          // If we're about to return the first line, and the control is also on the same
	          // line, that's a pretty good indicator that our sample threw at same line as
	          // the control. I.e. before we entered the sample frame. So we ignore this result.
	          // This can happen if you passed a class to function component, or non-function.
	          if (s !== 1 || c !== 1) {
	            do {
	              s--;
	              c--; // We may still have similar intermediate frames from the construct call.
	              // The next one that isn't the same should be our match though.

	              if (c < 0 || sampleLines[s] !== controlLines[c]) {
	                // V8 adds a "new" prefix for native classes. Let's remove it to make it prettier.
	                var _frame = '\n' + sampleLines[s].replace(' at new ', ' at '); // If our component frame is labeled "<anonymous>"
	                // but we have a user-provided "displayName"
	                // splice it in to make the stack more readable.


	                if (fn.displayName && _frame.includes('<anonymous>')) {
	                  _frame = _frame.replace('<anonymous>', fn.displayName);
	                }

	                {
	                  if (typeof fn === 'function') {
	                    componentFrameCache.set(fn, _frame);
	                  }
	                } // Return the line we found.


	                return _frame;
	              }
	            } while (s >= 1 && c >= 0);
	          }

	          break;
	        }
	      }
	    }
	  } finally {
	    reentry = false;

	    {
	      ReactCurrentDispatcher.current = previousDispatcher;
	      reenableLogs();
	    }

	    Error.prepareStackTrace = previousPrepareStackTrace;
	  } // Fallback to just using the name if we couldn't make it throw.


	  var name = fn ? fn.displayName || fn.name : '';
	  var syntheticFrame = name ? describeBuiltInComponentFrame(name) : '';

	  {
	    if (typeof fn === 'function') {
	      componentFrameCache.set(fn, syntheticFrame);
	    }
	  }

	  return syntheticFrame;
	}
	function describeFunctionComponentFrame(fn, source, ownerFn) {
	  {
	    return describeNativeComponentFrame(fn, false);
	  }
	}

	function shouldConstruct(Component) {
	  var prototype = Component.prototype;
	  return !!(prototype && prototype.isReactComponent);
	}

	function describeUnknownElementTypeFrameInDEV(type, source, ownerFn) {

	  if (type == null) {
	    return '';
	  }

	  if (typeof type === 'function') {
	    {
	      return describeNativeComponentFrame(type, shouldConstruct(type));
	    }
	  }

	  if (typeof type === 'string') {
	    return describeBuiltInComponentFrame(type);
	  }

	  switch (type) {
	    case REACT_SUSPENSE_TYPE:
	      return describeBuiltInComponentFrame('Suspense');

	    case REACT_SUSPENSE_LIST_TYPE:
	      return describeBuiltInComponentFrame('SuspenseList');
	  }

	  if (typeof type === 'object') {
	    switch (type.$$typeof) {
	      case REACT_FORWARD_REF_TYPE:
	        return describeFunctionComponentFrame(type.render);

	      case REACT_MEMO_TYPE:
	        // Memo may contain any component type so we recursively resolve it.
	        return describeUnknownElementTypeFrameInDEV(type.type, source, ownerFn);

	      case REACT_LAZY_TYPE:
	        {
	          var lazyComponent = type;
	          var payload = lazyComponent._payload;
	          var init = lazyComponent._init;

	          try {
	            // Lazy may contain any component type so we recursively resolve it.
	            return describeUnknownElementTypeFrameInDEV(init(payload), source, ownerFn);
	          } catch (x) {}
	        }
	    }
	  }

	  return '';
	}

	var hasOwnProperty = Object.prototype.hasOwnProperty;

	var loggedTypeFailures = {};
	var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;

	function setCurrentlyValidatingElement(element) {
	  {
	    if (element) {
	      var owner = element._owner;
	      var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
	      ReactDebugCurrentFrame.setExtraStackFrame(stack);
	    } else {
	      ReactDebugCurrentFrame.setExtraStackFrame(null);
	    }
	  }
	}

	function checkPropTypes(typeSpecs, values, location, componentName, element) {
	  {
	    // $FlowFixMe This is okay but Flow doesn't know it.
	    var has = Function.call.bind(hasOwnProperty);

	    for (var typeSpecName in typeSpecs) {
	      if (has(typeSpecs, typeSpecName)) {
	        var error$1 = void 0; // Prop type validation may throw. In case they do, we don't want to
	        // fail the render phase where it didn't fail before. So we log it.
	        // After these have been cleaned up, we'll let them throw.

	        try {
	          // This is intentionally an invariant that gets caught. It's the same
	          // behavior as without this statement except with a better message.
	          if (typeof typeSpecs[typeSpecName] !== 'function') {
	            // eslint-disable-next-line react-internal/prod-error-codes
	            var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' + 'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.');
	            err.name = 'Invariant Violation';
	            throw err;
	          }

	          error$1 = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED');
	        } catch (ex) {
	          error$1 = ex;
	        }

	        if (error$1 && !(error$1 instanceof Error)) {
	          setCurrentlyValidatingElement(element);

	          error('%s: type specification of %s' + ' `%s` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a %s. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).', componentName || 'React class', location, typeSpecName, typeof error$1);

	          setCurrentlyValidatingElement(null);
	        }

	        if (error$1 instanceof Error && !(error$1.message in loggedTypeFailures)) {
	          // Only monitor this failure once because there tends to be a lot of the
	          // same error.
	          loggedTypeFailures[error$1.message] = true;
	          setCurrentlyValidatingElement(element);

	          error('Failed %s type: %s', location, error$1.message);

	          setCurrentlyValidatingElement(null);
	        }
	      }
	    }
	  }
	}

	var isArrayImpl = Array.isArray; // eslint-disable-next-line no-redeclare

	function isArray(a) {
	  return isArrayImpl(a);
	}

	/*
	 * The `'' + value` pattern (used in in perf-sensitive code) throws for Symbol
	 * and Temporal.* types. See https://github.com/facebook/react/pull/22064.
	 *
	 * The functions in this module will throw an easier-to-understand,
	 * easier-to-debug exception with a clear errors message message explaining the
	 * problem. (Instead of a confusing exception thrown inside the implementation
	 * of the `value` object).
	 */
	// $FlowFixMe only called in DEV, so void return is not possible.
	function typeName(value) {
	  {
	    // toStringTag is needed for namespaced types like Temporal.Instant
	    var hasToStringTag = typeof Symbol === 'function' && Symbol.toStringTag;
	    var type = hasToStringTag && value[Symbol.toStringTag] || value.constructor.name || 'Object';
	    return type;
	  }
	} // $FlowFixMe only called in DEV, so void return is not possible.


	function willCoercionThrow(value) {
	  {
	    try {
	      testStringCoercion(value);
	      return false;
	    } catch (e) {
	      return true;
	    }
	  }
	}

	function testStringCoercion(value) {
	  // If you ended up here by following an exception call stack, here's what's
	  // happened: you supplied an object or symbol value to React (as a prop, key,
	  // DOM attribute, CSS property, string ref, etc.) and when React tried to
	  // coerce it to a string using `'' + value`, an exception was thrown.
	  //
	  // The most common types that will cause this exception are `Symbol` instances
	  // and Temporal objects like `Temporal.Instant`. But any object that has a
	  // `valueOf` or `[Symbol.toPrimitive]` method that throws will also cause this
	  // exception. (Library authors do this to prevent users from using built-in
	  // numeric operators like `+` or comparison operators like `>=` because custom
	  // methods are needed to perform accurate arithmetic or comparison.)
	  //
	  // To fix the problem, coerce this object or symbol value to a string before
	  // passing it to React. The most reliable way is usually `String(value)`.
	  //
	  // To find which value is throwing, check the browser or debugger console.
	  // Before this exception was thrown, there should be `console.error` output
	  // that shows the type (Symbol, Temporal.PlainDate, etc.) that caused the
	  // problem and how that type was used: key, atrribute, input value prop, etc.
	  // In most cases, this console output also shows the component and its
	  // ancestor components where the exception happened.
	  //
	  // eslint-disable-next-line react-internal/safe-string-coercion
	  return '' + value;
	}
	function checkKeyStringCoercion(value) {
	  {
	    if (willCoercionThrow(value)) {
	      error('The provided key is an unsupported type %s.' + ' This value must be coerced to a string before before using it here.', typeName(value));

	      return testStringCoercion(value); // throw (to help callers find troubleshooting comments)
	    }
	  }
	}

	var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
	var RESERVED_PROPS = {
	  key: true,
	  ref: true,
	  __self: true,
	  __source: true
	};
	var specialPropKeyWarningShown;
	var specialPropRefWarningShown;

	function hasValidRef(config) {
	  {
	    if (hasOwnProperty.call(config, 'ref')) {
	      var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;

	      if (getter && getter.isReactWarning) {
	        return false;
	      }
	    }
	  }

	  return config.ref !== undefined;
	}

	function hasValidKey(config) {
	  {
	    if (hasOwnProperty.call(config, 'key')) {
	      var getter = Object.getOwnPropertyDescriptor(config, 'key').get;

	      if (getter && getter.isReactWarning) {
	        return false;
	      }
	    }
	  }

	  return config.key !== undefined;
	}

	function warnIfStringRefCannotBeAutoConverted(config, self) {
	  {
	    if (typeof config.ref === 'string' && ReactCurrentOwner.current && self) ;
	  }
	}

	function defineKeyPropWarningGetter(props, displayName) {
	  {
	    var warnAboutAccessingKey = function () {
	      if (!specialPropKeyWarningShown) {
	        specialPropKeyWarningShown = true;

	        error('%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://reactjs.org/link/special-props)', displayName);
	      }
	    };

	    warnAboutAccessingKey.isReactWarning = true;
	    Object.defineProperty(props, 'key', {
	      get: warnAboutAccessingKey,
	      configurable: true
	    });
	  }
	}

	function defineRefPropWarningGetter(props, displayName) {
	  {
	    var warnAboutAccessingRef = function () {
	      if (!specialPropRefWarningShown) {
	        specialPropRefWarningShown = true;

	        error('%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://reactjs.org/link/special-props)', displayName);
	      }
	    };

	    warnAboutAccessingRef.isReactWarning = true;
	    Object.defineProperty(props, 'ref', {
	      get: warnAboutAccessingRef,
	      configurable: true
	    });
	  }
	}
	/**
	 * Factory method to create a new React element. This no longer adheres to
	 * the class pattern, so do not use new to call it. Also, instanceof check
	 * will not work. Instead test $$typeof field against Symbol.for('react.element') to check
	 * if something is a React Element.
	 *
	 * @param {*} type
	 * @param {*} props
	 * @param {*} key
	 * @param {string|object} ref
	 * @param {*} owner
	 * @param {*} self A *temporary* helper to detect places where `this` is
	 * different from the `owner` when React.createElement is called, so that we
	 * can warn. We want to get rid of owner and replace string `ref`s with arrow
	 * functions, and as long as `this` and owner are the same, there will be no
	 * change in behavior.
	 * @param {*} source An annotation object (added by a transpiler or otherwise)
	 * indicating filename, line number, and/or other information.
	 * @internal
	 */


	var ReactElement = function (type, key, ref, self, source, owner, props) {
	  var element = {
	    // This tag allows us to uniquely identify this as a React Element
	    $$typeof: REACT_ELEMENT_TYPE,
	    // Built-in properties that belong on the element
	    type: type,
	    key: key,
	    ref: ref,
	    props: props,
	    // Record the component responsible for creating this element.
	    _owner: owner
	  };

	  {
	    // The validation flag is currently mutative. We put it on
	    // an external backing store so that we can freeze the whole object.
	    // This can be replaced with a WeakMap once they are implemented in
	    // commonly used development environments.
	    element._store = {}; // To make comparing ReactElements easier for testing purposes, we make
	    // the validation flag non-enumerable (where possible, which should
	    // include every environment we run tests in), so the test framework
	    // ignores it.

	    Object.defineProperty(element._store, 'validated', {
	      configurable: false,
	      enumerable: false,
	      writable: true,
	      value: false
	    }); // self and source are DEV only properties.

	    Object.defineProperty(element, '_self', {
	      configurable: false,
	      enumerable: false,
	      writable: false,
	      value: self
	    }); // Two elements created in two different places should be considered
	    // equal for testing purposes and therefore we hide it from enumeration.

	    Object.defineProperty(element, '_source', {
	      configurable: false,
	      enumerable: false,
	      writable: false,
	      value: source
	    });

	    if (Object.freeze) {
	      Object.freeze(element.props);
	      Object.freeze(element);
	    }
	  }

	  return element;
	};
	/**
	 * https://github.com/reactjs/rfcs/pull/107
	 * @param {*} type
	 * @param {object} props
	 * @param {string} key
	 */

	function jsxDEV(type, config, maybeKey, source, self) {
	  {
	    var propName; // Reserved names are extracted

	    var props = {};
	    var key = null;
	    var ref = null; // Currently, key can be spread in as a prop. This causes a potential
	    // issue if key is also explicitly declared (ie. <div {...props} key="Hi" />
	    // or <div key="Hi" {...props} /> ). We want to deprecate key spread,
	    // but as an intermediary step, we will use jsxDEV for everything except
	    // <div {...props} key="Hi" />, because we aren't currently able to tell if
	    // key is explicitly declared to be undefined or not.

	    if (maybeKey !== undefined) {
	      {
	        checkKeyStringCoercion(maybeKey);
	      }

	      key = '' + maybeKey;
	    }

	    if (hasValidKey(config)) {
	      {
	        checkKeyStringCoercion(config.key);
	      }

	      key = '' + config.key;
	    }

	    if (hasValidRef(config)) {
	      ref = config.ref;
	      warnIfStringRefCannotBeAutoConverted(config, self);
	    } // Remaining properties are added to a new props object


	    for (propName in config) {
	      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
	        props[propName] = config[propName];
	      }
	    } // Resolve default props


	    if (type && type.defaultProps) {
	      var defaultProps = type.defaultProps;

	      for (propName in defaultProps) {
	        if (props[propName] === undefined) {
	          props[propName] = defaultProps[propName];
	        }
	      }
	    }

	    if (key || ref) {
	      var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;

	      if (key) {
	        defineKeyPropWarningGetter(props, displayName);
	      }

	      if (ref) {
	        defineRefPropWarningGetter(props, displayName);
	      }
	    }

	    return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
	  }
	}

	var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
	var ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;

	function setCurrentlyValidatingElement$1(element) {
	  {
	    if (element) {
	      var owner = element._owner;
	      var stack = describeUnknownElementTypeFrameInDEV(element.type, element._source, owner ? owner.type : null);
	      ReactDebugCurrentFrame$1.setExtraStackFrame(stack);
	    } else {
	      ReactDebugCurrentFrame$1.setExtraStackFrame(null);
	    }
	  }
	}

	var propTypesMisspellWarningShown;

	{
	  propTypesMisspellWarningShown = false;
	}
	/**
	 * Verifies the object is a ReactElement.
	 * See https://reactjs.org/docs/react-api.html#isvalidelement
	 * @param {?object} object
	 * @return {boolean} True if `object` is a ReactElement.
	 * @final
	 */


	function isValidElement(object) {
	  {
	    return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
	  }
	}

	function getDeclarationErrorAddendum() {
	  {
	    if (ReactCurrentOwner$1.current) {
	      var name = getComponentNameFromType(ReactCurrentOwner$1.current.type);

	      if (name) {
	        return '\n\nCheck the render method of `' + name + '`.';
	      }
	    }

	    return '';
	  }
	}

	function getSourceInfoErrorAddendum(source) {
	  {

	    return '';
	  }
	}
	/**
	 * Warn if there's no key explicitly set on dynamic arrays of children or
	 * object keys are not valid. This allows us to keep track of children between
	 * updates.
	 */


	var ownerHasKeyUseWarning = {};

	function getCurrentComponentErrorInfo(parentType) {
	  {
	    var info = getDeclarationErrorAddendum();

	    if (!info) {
	      var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;

	      if (parentName) {
	        info = "\n\nCheck the top-level render call using <" + parentName + ">.";
	      }
	    }

	    return info;
	  }
	}
	/**
	 * Warn if the element doesn't have an explicit key assigned to it.
	 * This element is in an array. The array could grow and shrink or be
	 * reordered. All children that haven't already been validated are required to
	 * have a "key" property assigned to it. Error statuses are cached so a warning
	 * will only be shown once.
	 *
	 * @internal
	 * @param {ReactElement} element Element that requires a key.
	 * @param {*} parentType element's parent's type.
	 */


	function validateExplicitKey(element, parentType) {
	  {
	    if (!element._store || element._store.validated || element.key != null) {
	      return;
	    }

	    element._store.validated = true;
	    var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);

	    if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
	      return;
	    }

	    ownerHasKeyUseWarning[currentComponentErrorInfo] = true; // Usually the current owner is the offender, but if it accepts children as a
	    // property, it may be the creator of the child that's responsible for
	    // assigning it a key.

	    var childOwner = '';

	    if (element && element._owner && element._owner !== ReactCurrentOwner$1.current) {
	      // Give the component that originally created this child.
	      childOwner = " It was passed a child from " + getComponentNameFromType(element._owner.type) + ".";
	    }

	    setCurrentlyValidatingElement$1(element);

	    error('Each child in a list should have a unique "key" prop.' + '%s%s See https://reactjs.org/link/warning-keys for more information.', currentComponentErrorInfo, childOwner);

	    setCurrentlyValidatingElement$1(null);
	  }
	}
	/**
	 * Ensure that every element either is passed in a static location, in an
	 * array with an explicit keys property defined, or in an object literal
	 * with valid key property.
	 *
	 * @internal
	 * @param {ReactNode} node Statically passed child of any type.
	 * @param {*} parentType node's parent's type.
	 */


	function validateChildKeys(node, parentType) {
	  {
	    if (typeof node !== 'object') {
	      return;
	    }

	    if (isArray(node)) {
	      for (var i = 0; i < node.length; i++) {
	        var child = node[i];

	        if (isValidElement(child)) {
	          validateExplicitKey(child, parentType);
	        }
	      }
	    } else if (isValidElement(node)) {
	      // This element was passed in a valid location.
	      if (node._store) {
	        node._store.validated = true;
	      }
	    } else if (node) {
	      var iteratorFn = getIteratorFn(node);

	      if (typeof iteratorFn === 'function') {
	        // Entry iterators used to provide implicit keys,
	        // but now we print a separate warning for them later.
	        if (iteratorFn !== node.entries) {
	          var iterator = iteratorFn.call(node);
	          var step;

	          while (!(step = iterator.next()).done) {
	            if (isValidElement(step.value)) {
	              validateExplicitKey(step.value, parentType);
	            }
	          }
	        }
	      }
	    }
	  }
	}
	/**
	 * Given an element, validate that its props follow the propTypes definition,
	 * provided by the type.
	 *
	 * @param {ReactElement} element
	 */


	function validatePropTypes(element) {
	  {
	    var type = element.type;

	    if (type === null || type === undefined || typeof type === 'string') {
	      return;
	    }

	    var propTypes;

	    if (typeof type === 'function') {
	      propTypes = type.propTypes;
	    } else if (typeof type === 'object' && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
	    // Inner props are checked in the reconciler.
	    type.$$typeof === REACT_MEMO_TYPE)) {
	      propTypes = type.propTypes;
	    } else {
	      return;
	    }

	    if (propTypes) {
	      // Intentionally inside to avoid triggering lazy initializers:
	      var name = getComponentNameFromType(type);
	      checkPropTypes(propTypes, element.props, 'prop', name, element);
	    } else if (type.PropTypes !== undefined && !propTypesMisspellWarningShown) {
	      propTypesMisspellWarningShown = true; // Intentionally inside to avoid triggering lazy initializers:

	      var _name = getComponentNameFromType(type);

	      error('Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', _name || 'Unknown');
	    }

	    if (typeof type.getDefaultProps === 'function' && !type.getDefaultProps.isReactClassApproved) {
	      error('getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.');
	    }
	  }
	}
	/**
	 * Given a fragment, validate that it can only be provided with fragment props
	 * @param {ReactElement} fragment
	 */


	function validateFragmentProps(fragment) {
	  {
	    var keys = Object.keys(fragment.props);

	    for (var i = 0; i < keys.length; i++) {
	      var key = keys[i];

	      if (key !== 'children' && key !== 'key') {
	        setCurrentlyValidatingElement$1(fragment);

	        error('Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.', key);

	        setCurrentlyValidatingElement$1(null);
	        break;
	      }
	    }

	    if (fragment.ref !== null) {
	      setCurrentlyValidatingElement$1(fragment);

	      error('Invalid attribute `ref` supplied to `React.Fragment`.');

	      setCurrentlyValidatingElement$1(null);
	    }
	  }
	}

	var didWarnAboutKeySpread = {};
	function jsxWithValidation(type, props, key, isStaticChildren, source, self) {
	  {
	    var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to
	    // succeed and there will likely be errors in render.

	    if (!validType) {
	      var info = '';

	      if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
	        info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
	      }

	      var sourceInfo = getSourceInfoErrorAddendum();

	      if (sourceInfo) {
	        info += sourceInfo;
	      } else {
	        info += getDeclarationErrorAddendum();
	      }

	      var typeString;

	      if (type === null) {
	        typeString = 'null';
	      } else if (isArray(type)) {
	        typeString = 'array';
	      } else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {
	        typeString = "<" + (getComponentNameFromType(type.type) || 'Unknown') + " />";
	        info = ' Did you accidentally export a JSX literal instead of a component?';
	      } else {
	        typeString = typeof type;
	      }

	      error('React.jsx: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', typeString, info);
	    }

	    var element = jsxDEV(type, props, key, source, self); // The result can be nullish if a mock or a custom function is used.
	    // TODO: Drop this when these are no longer allowed as the type argument.

	    if (element == null) {
	      return element;
	    } // Skip key warning if the type isn't valid since our key validation logic
	    // doesn't expect a non-string/function type and can throw confusing errors.
	    // We don't want exception behavior to differ between dev and prod.
	    // (Rendering will throw with a helpful message and as soon as the type is
	    // fixed, the key warnings will appear.)


	    if (validType) {
	      var children = props.children;

	      if (children !== undefined) {
	        if (isStaticChildren) {
	          if (isArray(children)) {
	            for (var i = 0; i < children.length; i++) {
	              validateChildKeys(children[i], type);
	            }

	            if (Object.freeze) {
	              Object.freeze(children);
	            }
	          } else {
	            error('React.jsx: Static children should always be an array. ' + 'You are likely explicitly calling React.jsxs or React.jsxDEV. ' + 'Use the Babel transform instead.');
	          }
	        } else {
	          validateChildKeys(children, type);
	        }
	      }
	    }

	    {
	      if (hasOwnProperty.call(props, 'key')) {
	        var componentName = getComponentNameFromType(type);
	        var keys = Object.keys(props).filter(function (k) {
	          return k !== 'key';
	        });
	        var beforeExample = keys.length > 0 ? '{key: someKey, ' + keys.join(': ..., ') + ': ...}' : '{key: someKey}';

	        if (!didWarnAboutKeySpread[componentName + beforeExample]) {
	          var afterExample = keys.length > 0 ? '{' + keys.join(': ..., ') + ': ...}' : '{}';

	          error('A props object containing a "key" prop is being spread into JSX:\n' + '  let props = %s;\n' + '  <%s {...props} />\n' + 'React keys must be passed directly to JSX without using spread:\n' + '  let props = %s;\n' + '  <%s key={someKey} {...props} />', beforeExample, componentName, afterExample, componentName);

	          didWarnAboutKeySpread[componentName + beforeExample] = true;
	        }
	      }
	    }

	    if (type === REACT_FRAGMENT_TYPE) {
	      validateFragmentProps(element);
	    } else {
	      validatePropTypes(element);
	    }

	    return element;
	  }
	} // These two functions exist to still get child warnings in dev
	// even with the prod transform. This means that jsxDEV is purely
	// opt-in behavior for better messages but that we won't stop
	// giving you warnings if you use production apis.

	function jsxWithValidationStatic(type, props, key) {
	  {
	    return jsxWithValidation(type, props, key, true);
	  }
	}
	function jsxWithValidationDynamic(type, props, key) {
	  {
	    return jsxWithValidation(type, props, key, false);
	  }
	}

	var jsx =  jsxWithValidationDynamic ; // we may want to special case jsxs internally to take advantage of static children.
	// for now we can ship identical prod functions

	var jsxs =  jsxWithValidationStatic ;

	reactJsxRuntime_development.Fragment = REACT_FRAGMENT_TYPE;
	reactJsxRuntime_development.jsx = jsx;
	reactJsxRuntime_development.jsxs = jsxs;
	  })();
	}
	return reactJsxRuntime_development;
}

if (process.env.NODE_ENV === 'production') {
  jsxRuntime.exports = requireReactJsxRuntime_production_min();
} else {
  jsxRuntime.exports = requireReactJsxRuntime_development();
}

var jsxRuntimeExports = jsxRuntime.exports;

/**
 * Main Hook
 */
function useControllable({
  value: controlledValue,
  defaultValue,
  onChange
}) {
  /**
   * States
   */
  const [uncontrolledValue, setUncontrolledValue] = require$$0.useState(defaultValue);
  /**
   * Derived Values
   */
  const isControlled = controlledValue !== undefined;
  const value = isControlled ? controlledValue : uncontrolledValue;
  /**
   * Refs
   */
  // 使用 ref 存储最新的值和回调，避免 useCallback 依赖导致的重建
  const stateRef = require$$0.useRef({
    value,
    onChange,
    isControlled
  });
  stateRef.current = {
    value,
    onChange,
    isControlled
  };
  /**
   * Callbacks
   */
  const setValue = require$$0.useCallback(nextValue => {
    const {
      value: currentValue,
      onChange: currentOnChange,
      isControlled: currentIsControlled
    } = stateRef.current;
    const newValue = typeof nextValue === 'function' ? nextValue(currentValue) : nextValue;
    if (!currentIsControlled) {
      setUncontrolledValue(newValue);
    }
    currentOnChange?.(newValue);
  }, []);
  return [value, setValue];
}

const circlePath = 'M512 64C264.6 64 64 264.6 64 512s200.6 448 448 448 448-200.6 448-448S759.4 64 512 64z';
const closePath = 'M256 192L192 256L448 512L192 768L256 832L512 576L768 832L832 768L576 512L832 256L768 192L512 448L256 192Z';
// 实心圆内带镂空X的图标（专为清除按钮设计）
const closeCircleFillPath = 'M512 64C264.6 64 64 264.6 64 512s200.6 448 448 448 448-200.6 448-448S759.4 64 512 64zM672 633.6L633.6 672 512 550.4 390.4 672 352 633.6 473.6 512 352 390.4 390.4 352 512 473.6 633.6 352 672 390.4 550.4 512 672 633.6z';
const checkPath = 'M384 640L192 448L256 384L384 512L768 128L832 192L384 640Z';
const arrowRightPath = 'M384 192L704 512L384 832L320 768L576 512L320 256L384 192Z';
const arrowLeftPath = 'M640 192L320 512L640 832L704 768L448 512L704 256L640 192Z';
const arrowUpPath = 'M192 640L512 320L832 640L768 704L512 448L256 704L192 640Z';
const arrowDownPath = 'M192 384L256 320L512 576L768 320L832 384L512 704L192 384Z';
const plusPath = 'M480 192h64v256h256v64H544v256h-64V512H224v-64h256V192z';
const minusPath = 'M224 480h576v64H224z';
const infoPath = `${circlePath}M480 416h64v320h-64zM512 272a40 40 0 1 0 0 80a40 40 0 1 0 0-80z`;
const warningPath = `${circlePath}M480 320h64v320h-64zM512 704a40 40 0 1 0 0 80a40 40 0 1 0 0-80z`;
const searchPath = 'M448 160a288 288 0 1 0 0 576 288 288 0 0 0 0-576zM448 640a192 192 0 1 1 0-384 192 192 0 0 1 0 384zM704 704l192 192-64 64-192-192z';
const loadingPath = 'M512 128a384 384 0 1 0 384 384h-96a288 288 0 1 1-288-288V128z';
const clockPath = `${circlePath}M480 256h64v320h-64zM512 512l192 96-32 56-224-112z`;
const homePath = 'M512 128L96 480v416h288V640h256v256h288V480L512 128zm256 672H640V576H384v224H256V518l256-204 256 204v282z';
const userPath = 'M512 512a192 192 0 1 0 0-384 192 192 0 0 0 0 384zm0-288a96 96 0 1 1 0 192 96 96 0 0 1 0-192zm256 576c0-141.4-114.6-256-256-256s-256 114.6-256 256v96h512v-96zm-416 0c0-88.4 71.6-160 160-160s160 71.6 160 160H352z';
const settingPath = 'M512 320a192 192 0 1 0 0 384 192 192 0 0 0 0-384zm0 288a96 96 0 1 1 0-192 96 96 0 0 1 0 192zm416-96h-72.6a344.8 344.8 0 0 0-29.8-72l51.4-51.4-90.6-90.6-51.4 51.4a344.8 344.8 0 0 0-72-29.8V248h-128v72.6a344.8 344.8 0 0 0-72 29.8l-51.4-51.4-90.6 90.6 51.4 51.4a344.8 344.8 0 0 0-29.8 72H168v128h72.6a344.8 344.8 0 0 0 29.8 72l-51.4 51.4 90.6 90.6 51.4-51.4a344.8 344.8 0 0 0 72 29.8V776h128v-72.6a344.8 344.8 0 0 0 72-29.8l51.4 51.4 90.6-90.6-51.4-51.4a344.8 344.8 0 0 0 29.8-72H928v-128z';
const messagePath = 'M832 128H192c-70.6 0-128 57.4-128 128v448c0 70.6 57.4 128 128 128h128v128l192-128h320c70.6 0 128-57.4 128-128V256c0-70.6-57.4-128-128-128zm32 576c0 17.6-14.4 32-32 32H489.6L320 864V736H192c-17.6 0-32-14.4-32-32V256c0-17.6 14.4-32 32-32h640c17.6 0 32 14.4 32 32v448z';
const heartPath = 'M512 864c-12.8 0-25.6-4.8-35.2-14.4L168 544c-89.6-89.6-89.6-236.8 0-326.4 89.6-89.6 236.8-89.6 326.4 0l17.6 17.6 17.6-17.6c89.6-89.6 236.8-89.6 326.4 0 89.6 89.6 89.6 236.8 0 326.4L547.2 849.6c-9.6 9.6-22.4 14.4-35.2 14.4z';
const starPath = 'M512 736l-264 160 72-296L96 408l304-24L512 96l112 288 304 24-224 192 72 296z';
const editPath = 'M832 512v320c0 35.2-28.8 64-64 64H192c-35.2 0-64-28.8-64-64V256c0-35.2 28.8-64 64-64h320v96H224v512h512V512h96zm32-384l128 128-384 384H480V512l384-384z';
const deletePath = 'M832 256h-160V192c0-35.2-28.8-64-64-64H416c-35.2 0-64 28.8-64 64v64H192v64h64v512c0 35.2 28.8 64 64 64h384c35.2 0 64-28.8 64-64V320h64v-64zM416 224h192v32H416v-32zm320 608H288V320h448v512z M384 416h64v352h-64zM576 416h64v352h-64z';
const locationPath = 'M512 64C335.3 64 192 207.3 192 384c0 252.2 288 544 304 560a24 24 0 0 0 32 0c16-16 304-307.8 304-560C832 207.3 688.7 64 512 64zm0 456a136 136 0 1 1 0-272 136 136 0 0 1 0 272z';
const bellPath = 'M816 768h-48V544c0-141.4-104.6-258.4-240-279.4V224a48 48 0 1 0-96 0v40.6C296.6 285.6 192 402.6 192 544v224h-48a32 32 0 0 0 0 64h672a32 32 0 0 0 0-64zM288 768V544c0-123.7 100.3-224 224-224s224 100.3 224 224v224H288zm224 128a96 96 0 0 0 96-96H416a96 96 0 0 0 96 96z';
const moonPath = 'M512 128c-35.3 0-69.7 4.6-102.9 13.7 136.4 47.1 234.5 175.7 234.5 326.3s-98.1 279.2-234.5 326.3c33.2 9.1 67.6 13.7 102.9 13.7 212.1 0 384-171.9 384-384S724.1 128 512 128z';
const globePath = 'M512 64C264.6 64 64 264.6 64 512s200.6 448 448 448 448-200.6 448-448S759.4 64 512 64zm0 820c-41.5 0-81.6-6.8-119-19.4 7.9-11.2 15.1-23 21.5-35.3 21.6-41.4 38.7-90.4 49.4-143.3H536v-80h-80.6c-2.5-27.7-3.8-56.8-3.8-87s1.3-59.3 3.8-87H536v-80h-72.1c-10.7-52.9-27.8-101.9-49.4-143.3-6.4-12.3-13.6-24.1-21.5-35.3 37.4-12.6 77.5-19.4 119-19.4 205.6 0 372 166.4 372 372S717.6 884 512 884z';
const refreshPath = 'M512 128V64L384 192l128 128v-80c141.4 0 256 114.6 256 256a254.2 254.2 0 0 1-48.6 150l58.2 58.2A334 334 0 0 0 848 512c0-185.6-150.4-336-336-336zm0 592c-141.4 0-256-114.6-256-256a254.2 254.2 0 0 1 48.6-150l-58.2-58.2A334 334 0 0 0 176 512c0 185.6 150.4 336 336 336v80l128-128-128-128v64z';
const orderPath = 'M864 128H160c-35.3 0-64 28.7-64 64v640c0 35.3 28.7 64 64 64h704c35.3 0 64-28.7 64-64V192c0-35.3-28.7-64-64-64zm-32 672H192V224h640v576zM320 352h384v64H320zm0 160h384v64H320zm0 160h256v64H320z';
const rightPath = arrowRightPath;
const iconPaths = {
  close: closePath,
  'close-circle': closeCircleFillPath,
  'close-circle-fill': closeCircleFillPath,
  check: checkPath,
  'check-circle': `${circlePath}${checkPath}`,
  'check-circle-fill': `${circlePath}${checkPath}`,
  'arrow-left': arrowLeftPath,
  'arrow-right': arrowRightPath,
  'arrow-up': arrowUpPath,
  'arrow-down': arrowDownPath,
  'chevron-up': arrowUpPath,
  'chevron-down': arrowDownPath,
  loading: loadingPath,
  search: searchPath,
  plus: plusPath,
  add: plusPath,
  minus: minusPath,
  'info-circle': infoPath,
  'warning-circle': warningPath,
  'success-circle': `${circlePath}${checkPath}`,
  clock: clockPath,
  home: homePath,
  user: userPath,
  setting: settingPath,
  message: messagePath,
  heart: heartPath,
  star: starPath,
  edit: editPath,
  delete: deletePath,
  trash: deletePath,
  location: locationPath,
  bell: bellPath,
  moon: moonPath,
  globe: globePath,
  refresh: refreshPath,
  order: orderPath,
  right: rightPath
};

function styleInject(css, ref) {
  if ( ref === void 0 ) ref = {};
  var insertAt = ref.insertAt;

  if (!css || typeof document === 'undefined') { return; }

  var head = document.head || document.getElementsByTagName('head')[0];
  var style = document.createElement('style');
  style.type = 'text/css';

  if (insertAt === 'top') {
    if (head.firstChild) {
      head.insertBefore(style, head.firstChild);
    } else {
      head.appendChild(style);
    }
  } else {
    head.appendChild(style);
  }

  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
}

var css_248z$S = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-icon {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  flex-shrink: 0;\n}\n.ct-icon svg {\n  width: 100%;\n  height: 100%;\n}";
styleInject(css_248z$S);

/**
 * Helpers
 */
const toBase64 = str => {
  if (typeof btoa === 'function') {
    return btoa(str);
  }
  // 小程序环境使用 ArrayBuffer 转换
  const encoder = new TextEncoder();
  const bytes = encoder.encode(str);
  const base64Chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
  let result = '';
  const len = bytes.length;
  for (let i = 0; i < len; i += 3) {
    const a = bytes[i];
    const b = bytes[i + 1] ?? 0;
    const c = bytes[i + 2] ?? 0;
    result += base64Chars[a >> 2];
    result += base64Chars[(a & 3) << 4 | b >> 4];
    result += i + 1 < len ? base64Chars[(b & 15) << 2 | c >> 6] : '=';
    result += i + 2 < len ? base64Chars[c & 63] : '=';
  }
  return result;
};
const Icon = ({
  name,
  size = 40,
  color,
  renderMode = 'auto',
  onClick,
  className
}) => {
  /**
   * Derived Values
   */
  const path = iconPaths[name];
  const env = Taro.getEnv?.();
  const useInlineSvg = renderMode === 'svg' || renderMode === 'auto' && env === Taro.ENV_TYPE?.WEB;
  const isCssVar = typeof color === 'string' && color.startsWith('var(');
  const svgFill = useInlineSvg ? color ?? 'currentColor' : isCssVar ? '#1f2937' : color ?? '#1f2937';
  const svgMarkup = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024"><path fill="${svgFill}" fill-rule="evenodd" d="${path}"/></svg>`;
  const svgBase64 = `data:image/svg+xml;base64,${toBase64(svgMarkup)}`;
  /**
   * Early Returns
   */
  if (!path) return null;
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-icon', className),
    style: {
      width: `${size}rpx`,
      height: `${size}rpx`,
      color
    },
    onClick: onClick,
    children: useInlineSvg ? jsxRuntimeExports.jsx("svg", {
      viewBox: "0 0 1024 1024",
      fill: "currentColor",
      children: jsxRuntimeExports.jsx("path", {
        d: path,
        fillRule: "evenodd"
      })
    }) : jsxRuntimeExports.jsx(components.Image, {
      src: svgBase64,
      mode: "aspectFit",
      style: {
        width: '100%',
        height: '100%'
      }
    })
  });
};

var css_248z$R = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-collapse {\n  background: var(--ct-color-bg);\n}\n.ct-collapse--bordered {\n  border-radius: 24rpx;\n  position: relative;\n}\n.ct-collapse--bordered::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  top: 0;\n  left: 0;\n  width: 200%;\n  height: 200%;\n  border: 1px solid var(--ct-color-border);\n  -webkit-transform: scale(0.5);\n          transform: scale(0.5);\n  -webkit-transform-origin: top left;\n          transform-origin: top left;\n}\n\n.ct-collapse-panel:not(:last-child) {\n  position: relative;\n}\n.ct-collapse-panel:not(:last-child)::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-collapse-panel--disabled .ct-collapse-panel__header {\n  opacity: 0.4;\n  pointer-events: none;\n}\n.ct-collapse-panel__header {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 32rpx;\n}\n.ct-collapse-panel__header:active, .ct-collapse-panel__header.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-collapse-panel__title {\n  -webkit-box-flex: 1;\n          flex: 1;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-collapse-panel__extra {\n  margin-right: 16rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-collapse-panel__arrow {\n  -webkit-transition: -webkit-transform 250ms;\n  transition: -webkit-transform 250ms;\n  transition: transform 250ms;\n  transition: transform 250ms, -webkit-transform 250ms;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-collapse-panel__arrow--expanded {\n  -webkit-transform: rotate(180deg);\n          transform: rotate(180deg);\n}\n.ct-collapse-panel__content-wrapper {\n  overflow: hidden;\n  -webkit-transition: height 250ms ease-out;\n  transition: height 250ms ease-out;\n}\n.ct-collapse-panel__content {\n  padding: 0 32rpx 32rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n  line-height: 1.6;\n}";
styleInject(css_248z$R);

/**
 * Context
 */
const CollapseContext = /*#__PURE__*/require$$0.createContext(null);
/**
 * Helpers
 */
const normalizeKeys = keys => {
  if (!keys) return [];
  return Array.isArray(keys) ? keys : [keys];
};
/**
 * Sub Components
 */
const CollapsePanel = ({
  title,
  extra,
  disabled = false,
  children,
  className,
  __key
}) => {
  /**
   * Hooks
   */
  const context = require$$0.useContext(CollapseContext);
  /**
   * States
   */
  const [contentHeight, setContentHeight] = require$$0.useState(0);
  /**
   * Refs
   */
  const contentId = require$$0.useRef(`collapse-content-${Date.now()}`);
  /**
   * Early Returns
   */
  if (!context || !__key) return null;
  /**
   * Derived Values
   */
  const {
    activeKeys,
    onToggle
  } = context;
  const isExpanded = activeKeys.includes(__key);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (!isExpanded) return;
    Taro.nextTick(() => {
      const query = Taro.createSelectorQuery();
      query.select(`#${contentId.current}`).boundingClientRect();
      query.exec(res => {
        if (res[0]) {
          setContentHeight(res[0].height);
        }
      });
    });
  }, [children, isExpanded]);
  /**
   * Callbacks
   */
  const handleClick = () => {
    if (!disabled) {
      onToggle(__key);
    }
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-collapse-panel', {
      'ct-collapse-panel--expanded': isExpanded,
      'ct-collapse-panel--disabled': disabled
    }, className),
    children: [jsxRuntimeExports.jsxs(components.View, {
      className: "ct-collapse-panel__header",
      onClick: handleClick,
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-collapse-panel__title",
        children: typeof title === 'string' ? jsxRuntimeExports.jsx(components.Text, {
          children: title
        }) : title
      }), extra && jsxRuntimeExports.jsx(components.View, {
        className: "ct-collapse-panel__extra",
        children: extra
      }), jsxRuntimeExports.jsx(Icon, {
        name: "chevron-down",
        size: 32,
        className: classNames('ct-collapse-panel__arrow', {
          'ct-collapse-panel__arrow--expanded': isExpanded
        })
      })]
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-collapse-panel__content-wrapper",
      style: {
        height: isExpanded ? `${contentHeight}px` : '0'
      },
      children: jsxRuntimeExports.jsx(components.View, {
        id: contentId.current,
        className: "ct-collapse-panel__content",
        children: children
      })
    })]
  });
};
/**
 * Main Component
 */
const Collapse = ({
  activeKey,
  defaultActiveKey = [],
  accordion = false,
  onChange,
  bordered = true,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const [activeKeys, setActiveKeys] = useControllable({
    value: activeKey !== undefined ? normalizeKeys(activeKey) : undefined,
    defaultValue: normalizeKeys(defaultActiveKey),
    onChange: keys => {
      onChange?.(accordion ? keys[0] || '' : keys);
    }
  });
  /**
   * Callbacks
   */
  const handleToggle = key => {
    let newKeys;
    if (accordion) {
      newKeys = activeKeys.includes(key) ? [] : [key];
    } else {
      newKeys = activeKeys.includes(key) ? activeKeys.filter(current => current !== key) : [...activeKeys, key];
    }
    setActiveKeys(newKeys);
  };
  /**
   * Render Helpers
   */
  const renderChildren = () => require$$0.Children.map(children, (child, index) => {
    if (! /*#__PURE__*/require$$0.isValidElement(child)) return child;
    const resolvedKey = child.key !== null && child.key !== undefined ? String(child.key) : String(index);
    if (child.type === CollapsePanel && (child.key === null || child.key === undefined) && process.env.NODE_ENV !== 'production') {
      console.warn('[Collapse] Each Collapse.Panel should have a stable key.');
    }
    if (child.type !== CollapsePanel) return child;
    return /*#__PURE__*/require$$0.cloneElement(child, {
      __key: resolvedKey,
      key: resolvedKey
    });
  });
  return jsxRuntimeExports.jsx(CollapseContext.Provider, {
    value: {
      activeKeys,
      onToggle: handleToggle
    },
    children: jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-collapse', {
        'ct-collapse--bordered': bordered
      }, className),
      children: renderChildren()
    })
  });
};
Collapse.Panel = CollapsePanel;

var css_248z$Q = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}";
styleInject(css_248z$Q);

/**
 * Main Component
 */
const Accordion = ({
  items,
  activeKey,
  defaultActiveKey,
  onChange,
  bordered = true,
  className
}) => {
  return jsxRuntimeExports.jsx(Collapse, {
    activeKey: activeKey,
    defaultActiveKey: defaultActiveKey,
    accordion: true,
    onChange: key => onChange?.(key),
    bordered: bordered,
    className: classNames('ct-accordion', className),
    children: items.map(item => jsxRuntimeExports.jsx(Collapse.Panel, {
      title: item.title,
      disabled: item.disabled,
      children: item.content
    }, item.key))
  });
};

/**
 * Constants
 */
const OVERLAY_ANIMATION_DURATION = 300;

var css_248z$P = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-action-sheet {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 500;\n}\n.ct-action-sheet__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg-mask);\n  opacity: 0;\n  -webkit-transition: opacity 250ms ease-out;\n  transition: opacity 250ms ease-out;\n}\n.ct-action-sheet__mask--visible {\n  opacity: 1;\n}\n.ct-action-sheet__panel {\n  position: absolute;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg-page);\n  border-radius: 32rpx 32rpx 0 0;\n  -webkit-transform: translateY(100%);\n          transform: translateY(100%);\n  -webkit-transition: -webkit-transform 250ms ease-out;\n  transition: -webkit-transform 250ms ease-out;\n  transition: transform 250ms ease-out;\n  transition: transform 250ms ease-out, -webkit-transform 250ms ease-out;\n  padding-bottom: calc(0 + constant(safe-area-inset-bottom));\n  padding-bottom: calc(0 + env(safe-area-inset-bottom));\n}\n.ct-action-sheet__panel--visible {\n  -webkit-transform: translateY(0);\n          transform: translateY(0);\n}\n.ct-action-sheet__header {\n  padding: 32rpx;\n  text-align: center;\n  position: relative;\n}\n.ct-action-sheet__header::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-action-sheet__title {\n  font-size: 32rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n}\n.ct-action-sheet__description {\n  display: block;\n  margin-top: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-action-sheet__actions {\n  background: var(--ct-color-bg-card);\n}\n.ct-action-sheet__action {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  padding: 32rpx;\n  position: relative;\n}\n.ct-action-sheet__action::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-action-sheet__action--hover {\n  background: var(--ct-color-bg-hover);\n}\n.ct-action-sheet__action--danger {\n  color: var(--ct-color-error);\n}\n.ct-action-sheet__action--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-action-sheet__action-text {\n  font-size: 32rpx;\n  color: inherit;\n}\n.ct-action-sheet__action-desc {\n  margin-top: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-action-sheet__cancel {\n  margin-top: 16rpx;\n  padding: 32rpx;\n  background: var(--ct-color-bg-card);\n  text-align: center;\n  font-size: 32rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-action-sheet__cancel:active, .ct-action-sheet__cancel.ct-clickable--hover {\n  opacity: 0.7;\n}";
styleInject(css_248z$P);

const ActionSheet = ({
  open,
  actions,
  title,
  description,
  cancelText = '取消',
  onAction,
  onCancel,
  onClose,
  closeOnOverlayClick = true
}) => {
  /**
   * States
   */
  const [show, setShow] = require$$0.useState(false);
  const [animating, setAnimating] = require$$0.useState(false);
  /**
   * Callbacks
   */
  const handleOverlayClick = () => {
    if (closeOnOverlayClick) {
      onCancel?.();
      onClose?.();
    }
  };
  const handleActionClick = (action, index) => {
    if (action.disabled) return;
    onAction?.(action, index);
    onClose?.();
  };
  const handleCancel = () => {
    onCancel?.();
    onClose?.();
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (open) {
      setShow(true);
      Taro.nextTick(() => setAnimating(true));
    } else {
      setAnimating(false);
      const timer = setTimeout(() => setShow(false), OVERLAY_ANIMATION_DURATION);
      return () => clearTimeout(timer);
    }
    return undefined;
  }, [open]);
  /**
   * Early Returns
   */
  if (!show) return null;
  return jsxRuntimeExports.jsxs(components.View, {
    className: "ct-action-sheet",
    catchMove: true,
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-action-sheet__mask', {
        'ct-action-sheet__mask--visible': animating
      }),
      onClick: handleOverlayClick
    }), jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-action-sheet__panel', {
        'ct-action-sheet__panel--visible': animating
      }),
      children: [(title || description) && jsxRuntimeExports.jsxs(components.View, {
        className: "ct-action-sheet__header",
        children: [title && jsxRuntimeExports.jsx(components.Text, {
          className: "ct-action-sheet__title",
          children: title
        }), description && jsxRuntimeExports.jsx(components.Text, {
          className: "ct-action-sheet__description",
          children: description
        })]
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-action-sheet__actions",
        children: actions.map((action, index) => jsxRuntimeExports.jsxs(components.View, {
          className: classNames('ct-action-sheet__action', {
            'ct-action-sheet__action--danger': action.danger,
            'ct-action-sheet__action--disabled': action.disabled
          }),
          style: action.color ? {
            color: action.color
          } : undefined,
          onClick: () => handleActionClick(action, index),
          hoverClass: action.disabled ? 'none' : 'ct-action-sheet__action--hover',
          children: [jsxRuntimeExports.jsx(components.Text, {
            className: "ct-action-sheet__action-text",
            children: action.text
          }), action.description && jsxRuntimeExports.jsx(components.Text, {
            className: "ct-action-sheet__action-desc",
            children: action.description
          })]
        }, action.value ?? index))
      }), cancelText && jsxRuntimeExports.jsx(components.View, {
        className: "ct-action-sheet__cancel",
        onClick: handleCancel,
        children: jsxRuntimeExports.jsx(components.Text, {
          children: cancelText
        })
      })]
    })]
  });
};

var css_248z$O = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-avatar {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  background: var(--ct-color-fill-secondary);\n  overflow: hidden;\n  flex-shrink: 0;\n}\n.ct-avatar--circle {\n  border-radius: 50%;\n}\n.ct-avatar--square {\n  border-radius: 16rpx;\n}\n.ct-avatar__image {\n  width: 100%;\n  height: 100%;\n}\n.ct-avatar__content, .ct-avatar__text {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  color: var(--ct-color-text-secondary);\n  font-weight: 500;\n}\n\n.ct-avatar-group {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-avatar-group__item {\n  position: relative;\n}\n.ct-avatar-group__item .ct-avatar {\n  border: 4rpx solid var(--ct-color-bg-card);\n}";
styleInject(css_248z$O);

/**
 * Constants
 */
const SIZE_MAP$1 = {
  xs: 48,
  sm: 64,
  md: 80,
  lg: 96,
  xl: 128
};
const Avatar = ({
  src,
  fallback,
  size = 'md',
  shape = 'circle',
  alt,
  backgroundColor,
  color,
  onClick,
  children,
  className
}) => {
  /**
   * States
   */
  const [error, setError] = require$$0.useState(false);
  const [currentSrc, setCurrentSrc] = require$$0.useState(src || fallback);
  /**
   * Derived Values
   */
  const sizeValue = typeof size === 'number' ? size : SIZE_MAP$1[size];
  const style = {
    width: `${sizeValue}rpx`,
    height: `${sizeValue}rpx`,
    backgroundColor,
    color
  };
  const imageSrc = currentSrc;
  const displayText = alt?.charAt(0)?.toUpperCase();
  /**
   * Callbacks
   */
  const handleError = () => {
    if (currentSrc && currentSrc !== fallback && fallback) {
      setCurrentSrc(fallback);
      setError(false);
      return;
    }
    setError(true);
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    setCurrentSrc(src || fallback);
    setError(false);
  }, [fallback, src]);
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-avatar', `ct-avatar--${shape}`, className),
    style: style,
    onClick: onClick,
    children: imageSrc && !error ? jsxRuntimeExports.jsx(components.Image, {
      src: imageSrc,
      className: "ct-avatar__image",
      mode: "aspectFill",
      onError: handleError
    }) : children ? jsxRuntimeExports.jsx(components.View, {
      className: "ct-avatar__content",
      children: children
    }) : displayText ? jsxRuntimeExports.jsx(components.Text, {
      className: "ct-avatar__text",
      style: {
        fontSize: `${sizeValue * 0.4}rpx`
      },
      children: displayText
    }) : null
  });
};
const AvatarGroup = ({
  max = 5,
  size = 'md',
  gap = -16,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const avatars = require$$0.Children.toArray(children).filter(require$$0.isValidElement);
  const visibleAvatars = max ? avatars.slice(0, max) : avatars;
  const remainingCount = avatars.length - visibleAvatars.length;
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-avatar-group', className),
    children: [visibleAvatars.map((child, index) => jsxRuntimeExports.jsx(components.View, {
      className: "ct-avatar-group__item",
      style: {
        marginLeft: index === 0 ? 0 : `${gap}rpx`
      },
      children: /*#__PURE__*/require$$0.cloneElement(child, {
        size
      })
    }, index)), remainingCount > 0 && jsxRuntimeExports.jsx(components.View, {
      className: "ct-avatar-group__item",
      style: {
        marginLeft: `${gap}rpx`
      },
      children: jsxRuntimeExports.jsx(Avatar, {
        size: size,
        backgroundColor: "var(--ct-color-fill-secondary)",
        children: jsxRuntimeExports.jsxs(components.Text, {
          children: ["+", remainingCount]
        })
      })
    })]
  });
};
Avatar.Group = AvatarGroup;

var css_248z$N = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-badge {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  position: relative;\n}\n.ct-badge__badge {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  min-width: 32rpx;\n  height: 32rpx;\n  padding: 0 8rpx;\n  background: var(--ct-color-error);\n  border-radius: 16rpx;\n  box-sizing: border-box;\n}\n.ct-badge > .ct-badge__badge {\n  position: absolute;\n  top: -8rpx;\n  right: -8rpx;\n  -webkit-transform: translate(50%, -50%);\n          transform: translate(50%, -50%);\n}\n.ct-badge__badge--dot {\n  min-width: 16rpx;\n  width: 16rpx;\n  height: 16rpx;\n  padding: 0;\n  border-radius: 50%;\n}\n.ct-badge__text {\n  font-size: 20rpx;\n  color: #fff;\n  line-height: 1;\n  white-space: nowrap;\n}";
styleInject(css_248z$N);

const Badge = ({
  content,
  max = 99,
  dot = false,
  color,
  wrapper = true,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const badgeStyle = color ? {
    backgroundColor: color
  } : undefined;
  /**
   * Render Helpers
   */
  const renderContent = () => {
    if (dot) return null;
    if (typeof content === 'number' && max && content > max) {
      return `${max}+`;
    }
    return content;
  };
  const badge = (content !== undefined || dot) && jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-badge__badge', {
      'ct-badge__badge--dot': dot
    }, (!wrapper || !children) && className),
    style: badgeStyle,
    children: !dot && jsxRuntimeExports.jsx(components.Text, {
      className: "ct-badge__text",
      children: renderContent()
    })
  });
  /**
   * Early Returns
   */
  if (!wrapper || !children) {
    return badge || null;
  }
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-badge', className),
    children: [children, badge]
  });
};

var css_248z$M = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-bottom-sheet {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 500;\n}\n.ct-bottom-sheet__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg-mask);\n  opacity: 0;\n  -webkit-transition: opacity 250ms ease-out;\n  transition: opacity 250ms ease-out;\n}\n.ct-bottom-sheet__mask--visible {\n  opacity: 1;\n}\n.ct-bottom-sheet__container {\n  position: absolute;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg);\n  border-radius: 32rpx 32rpx 0 0;\n  -webkit-transform: translateY(100%);\n          transform: translateY(100%);\n  -webkit-transition: -webkit-transform 250ms ease-out;\n  transition: -webkit-transform 250ms ease-out;\n  transition: transform 250ms ease-out;\n  transition: transform 250ms ease-out, -webkit-transform 250ms ease-out;\n  max-height: 100vh;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  padding-bottom: calc(0 + constant(safe-area-inset-bottom));\n  padding-bottom: calc(0 + env(safe-area-inset-bottom));\n}\n.ct-bottom-sheet__container--visible {\n  -webkit-transform: translateY(0);\n          transform: translateY(0);\n}\n.ct-bottom-sheet__handle {\n  width: 64rpx;\n  height: 8rpx;\n  margin: 24rpx auto;\n  background: var(--ct-color-border);\n  border-radius: 4rpx;\n}\n.ct-bottom-sheet__header {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: start;\n          align-items: flex-start;\n  padding: 32rpx 40rpx;\n  position: relative;\n}\n.ct-bottom-sheet__header::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-bottom-sheet__header-content {\n  -webkit-box-flex: 1;\n          flex: 1;\n}\n.ct-bottom-sheet__title {\n  font-size: 36rpx;\n  font-weight: 600;\n  color: var(--ct-color-text-primary);\n}\n.ct-bottom-sheet__description {\n  display: block;\n  margin-top: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-bottom-sheet__close {\n  padding: 8rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-bottom-sheet__body {\n  -webkit-box-flex: 1;\n          flex: 1;\n  overflow-y: auto;\n  padding: 32rpx 40rpx;\n}";
styleInject(css_248z$M);

const BottomSheet = ({
  open,
  title,
  description,
  onClose,
  showHandle = true,
  closable = false,
  closeOnOverlayClick = true,
  height = 'auto',
  children,
  className
}) => {
  /**
   * States
   */
  const [visible, setVisible] = require$$0.useState(false);
  const [animating, setAnimating] = require$$0.useState(false);
  /**
   * Callbacks
   */
  const handleOverlayClick = () => {
    if (closeOnOverlayClick) {
      onClose?.();
    }
  };
  /**
   * Render Helpers
   */
  const getHeightStyle = () => {
    if (height === 'auto') return {};
    if (height === 'full') return {
      height: '100vh'
    };
    return {
      height: `${height}rpx`
    };
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (open) {
      setVisible(true);
      Taro.nextTick(() => setAnimating(true));
    } else {
      setAnimating(false);
      const timer = setTimeout(() => setVisible(false), OVERLAY_ANIMATION_DURATION);
      return () => clearTimeout(timer);
    }
    return undefined;
  }, [open]);
  /**
   * Early Returns
   */
  if (!visible) return null;
  return jsxRuntimeExports.jsxs(components.View, {
    className: "ct-bottom-sheet",
    catchMove: true,
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-bottom-sheet__mask', {
        'ct-bottom-sheet__mask--visible': animating
      }),
      onClick: handleOverlayClick
    }), jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-bottom-sheet__container', {
        'ct-bottom-sheet__container--visible': animating
      }, className),
      style: getHeightStyle(),
      children: [showHandle && jsxRuntimeExports.jsx(components.View, {
        className: "ct-bottom-sheet__handle",
        onClick: onClose
      }), (title || closable) && jsxRuntimeExports.jsxs(components.View, {
        className: "ct-bottom-sheet__header",
        children: [jsxRuntimeExports.jsxs(components.View, {
          className: "ct-bottom-sheet__header-content",
          children: [title && jsxRuntimeExports.jsx(components.Text, {
            className: "ct-bottom-sheet__title",
            children: title
          }), description && jsxRuntimeExports.jsx(components.Text, {
            className: "ct-bottom-sheet__description",
            children: description
          })]
        }), closable && jsxRuntimeExports.jsx(components.View, {
          className: "ct-bottom-sheet__close",
          onClick: onClose,
          children: jsxRuntimeExports.jsx(Icon, {
            name: "close",
            size: 40
          })
        })]
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-bottom-sheet__body",
        children: children
      })]
    })]
  });
};

var css_248z$L = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-breadcrumb {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  flex-wrap: nowrap;\n  overflow-x: auto;\n  -webkit-overflow-scrolling: touch;\n}\n.ct-breadcrumb::-webkit-scrollbar {\n  display: none;\n}\n.ct-breadcrumb__item {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  flex-shrink: 0;\n  color: var(--ct-color-text-secondary);\n  font-size: 26rpx;\n  line-height: 1.5;\n  min-height: 44rpx;\n  padding: 8rpx 0;\n}\n.ct-breadcrumb__item--clickable {\n  color: var(--ct-color-text-primary);\n}\n.ct-breadcrumb__item--hover {\n  color: var(--ct-color-primary);\n}\n.ct-breadcrumb__item--current {\n  color: var(--ct-color-text-primary);\n  font-weight: 500;\n  pointer-events: none;\n}\n.ct-breadcrumb__item--disabled {\n  color: var(--ct-color-text-disabled);\n  pointer-events: none;\n}\n.ct-breadcrumb__icon {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  margin-right: 8rpx;\n}\n.ct-breadcrumb__label {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  max-width: 200rpx;\n}\n.ct-breadcrumb__separator {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  flex-shrink: 0;\n  margin: 0 16rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-breadcrumb__ellipsis-wrapper {\n  position: relative;\n  flex-shrink: 0;\n}\n.ct-breadcrumb__ellipsis {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  min-width: 48rpx;\n  min-height: 48rpx;\n  padding: 8rpx;\n  color: var(--ct-color-text-secondary);\n  border-radius: 8rpx;\n  -webkit-transition: background-color 150ms ease;\n  transition: background-color 150ms ease;\n}\n.ct-breadcrumb__ellipsis--hover, .ct-breadcrumb__ellipsis--active {\n  background: var(--ct-color-bg-hover);\n  color: var(--ct-color-primary);\n}\n.ct-breadcrumb__mask {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 99;\n}\n.ct-breadcrumb__dropdown {\n  position: absolute;\n  top: calc(100% + 16rpx);\n  left: 50%;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n  min-width: 200rpx;\n  max-width: 400rpx;\n  background: var(--ct-color-bg-container);\n  border-radius: 16rpx;\n  box-shadow: 0 16rpx 48rpx rgba(0, 0, 0, 0.12);\n  z-index: 100;\n  overflow: hidden;\n}\n.ct-breadcrumb__dropdown-item {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  -webkit-transition: background-color 150ms ease;\n  transition: background-color 150ms ease;\n}\n.ct-breadcrumb__dropdown-item--hover {\n  background: var(--ct-color-bg-hover);\n}\n.ct-breadcrumb__dropdown-item--disabled {\n  color: var(--ct-color-text-disabled);\n  pointer-events: none;\n}\n.ct-breadcrumb__dropdown-item + .ct-breadcrumb__dropdown-item {\n  border-top: 2rpx solid var(--ct-color-border);\n}\n.ct-breadcrumb__dropdown-icon {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  margin-right: 16rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-breadcrumb__dropdown-label {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  -webkit-box-flex: 1;\n          flex: 1;\n}";
styleInject(css_248z$L);

const Breadcrumb = ({
  items,
  separator,
  maxItems = 0,
  itemsBeforeCollapse = 1,
  itemsAfterCollapse = 1,
  onItemClick,
  className,
  style
}) => {
  /**
   * States
   */
  const [showCollapsed, setShowCollapsed] = require$$0.useState(false);
  /**
   * Derived Values
   */
  const {
    visibleItems,
    collapsedItems,
    shouldCollapse,
    collapseIndex
  } = require$$0.useMemo(() => {
    if (maxItems <= 0 || items.length <= maxItems) {
      return {
        visibleItems: items,
        collapsedItems: [],
        shouldCollapse: false,
        collapseIndex: -1
      };
    }
    const before = items.slice(0, itemsBeforeCollapse);
    const after = items.slice(-itemsAfterCollapse);
    const collapsed = items.slice(itemsBeforeCollapse, items.length - itemsAfterCollapse);
    return {
      visibleItems: [...before, ...after],
      collapsedItems: collapsed,
      shouldCollapse: true,
      collapseIndex: itemsBeforeCollapse
    };
  }, [items, maxItems, itemsBeforeCollapse, itemsAfterCollapse]);
  /**
   * Callbacks
   */
  const handleItemClick = (item, index, isLast) => {
    if (item.disabled || isLast) return;
    if (onItemClick) {
      onItemClick(item, index);
    } else if (item.path) {
      Taro.navigateTo({
        url: item.path
      }).catch(() => {
        Taro.redirectTo({
          url: item.path
        });
      });
    }
  };
  const handleCollapsedItemClick = (item, originalIndex) => {
    setShowCollapsed(false);
    if (item.disabled) return;
    if (onItemClick) {
      onItemClick(item, originalIndex);
    } else if (item.path) {
      Taro.navigateTo({
        url: item.path
      }).catch(() => {
        Taro.redirectTo({
          url: item.path
        });
      });
    }
  };
  /**
   * Render Helpers
   */
  const renderSeparator = () => jsxRuntimeExports.jsx(components.View, {
    className: "ct-breadcrumb__separator",
    children: separator || jsxRuntimeExports.jsx(Icon, {
      name: "chevron-right",
      size: 24
    })
  });
  const renderItem = (item, index, isLast) => {
    const isClickable = !isLast && !item.disabled && (item.path || onItemClick);
    return jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-breadcrumb__item', {
        'ct-breadcrumb__item--current': isLast,
        'ct-breadcrumb__item--disabled': item.disabled,
        'ct-breadcrumb__item--clickable': isClickable
      }),
      onClick: () => handleItemClick(item, index, isLast),
      hoverClass: isClickable ? 'ct-breadcrumb__item--hover' : 'none',
      children: [item.icon && jsxRuntimeExports.jsx(components.View, {
        className: "ct-breadcrumb__icon",
        children: item.icon
      }), jsxRuntimeExports.jsx(components.Text, {
        className: "ct-breadcrumb__label",
        children: item.label
      })]
    }, index);
  };
  const renderEllipsis = () => jsxRuntimeExports.jsxs(components.View, {
    className: "ct-breadcrumb__ellipsis-wrapper",
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-breadcrumb__ellipsis', {
        'ct-breadcrumb__ellipsis--active': showCollapsed
      }),
      onClick: () => setShowCollapsed(!showCollapsed),
      hoverClass: "ct-breadcrumb__ellipsis--hover",
      children: jsxRuntimeExports.jsx(Icon, {
        name: "more-horizontal",
        size: 32
      })
    }), showCollapsed && jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-breadcrumb__mask",
        onClick: () => setShowCollapsed(false)
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-breadcrumb__dropdown",
        children: collapsedItems.map((item, idx) => {
          const originalIndex = itemsBeforeCollapse + idx;
          return jsxRuntimeExports.jsxs(components.View, {
            className: classNames('ct-breadcrumb__dropdown-item', {
              'ct-breadcrumb__dropdown-item--disabled': item.disabled
            }),
            onClick: () => handleCollapsedItemClick(item, originalIndex),
            hoverClass: item.disabled ? 'none' : 'ct-breadcrumb__dropdown-item--hover',
            children: [item.icon && jsxRuntimeExports.jsx(components.View, {
              className: "ct-breadcrumb__dropdown-icon",
              children: item.icon
            }), jsxRuntimeExports.jsx(components.Text, {
              className: "ct-breadcrumb__dropdown-label",
              children: item.label
            })]
          }, originalIndex);
        })
      })]
    })]
  });
  const renderItems = () => {
    const result = [];
    visibleItems.forEach((item, idx) => {
      const isLastVisible = idx === visibleItems.length - 1;
      const originalIndex = idx < itemsBeforeCollapse ? idx : items.length - itemsAfterCollapse + (idx - itemsBeforeCollapse);
      result.push(jsxRuntimeExports.jsx(require$$0.Fragment, {
        children: renderItem(item, originalIndex, isLastVisible)
      }, `item-${originalIndex}`));
      if (shouldCollapse && idx === itemsBeforeCollapse - 1) {
        result.push(jsxRuntimeExports.jsx(require$$0.Fragment, {
          children: renderSeparator()
        }, "sep-before-ellipsis"));
        result.push(jsxRuntimeExports.jsx(require$$0.Fragment, {
          children: renderEllipsis()
        }, "ellipsis"));
      }
      if (!isLastVisible) {
        result.push(jsxRuntimeExports.jsx(require$$0.Fragment, {
          children: renderSeparator()
        }, `sep-${originalIndex}`));
      }
    });
    return result;
  };
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-breadcrumb', className),
    style: style,
    children: renderItems()
  });
};

var css_248z$K = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n@-webkit-keyframes ct-fade-in {\n  from {\n    opacity: 0;\n  }\n  to {\n    opacity: 1;\n  }\n}\n\n@keyframes ct-fade-in {\n  from {\n    opacity: 0;\n  }\n  to {\n    opacity: 1;\n  }\n}\n@-webkit-keyframes ct-fade-out {\n  from {\n    opacity: 1;\n  }\n  to {\n    opacity: 0;\n  }\n}\n@keyframes ct-fade-out {\n  from {\n    opacity: 1;\n  }\n  to {\n    opacity: 0;\n  }\n}\n@-webkit-keyframes ct-scale-in {\n  from {\n    opacity: 0;\n    -webkit-transform: scale(0.95);\n            transform: scale(0.95);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n}\n@keyframes ct-scale-in {\n  from {\n    opacity: 0;\n    -webkit-transform: scale(0.95);\n            transform: scale(0.95);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n}\n@-webkit-keyframes ct-scale-out {\n  from {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n  to {\n    opacity: 0;\n    -webkit-transform: scale(0.95);\n            transform: scale(0.95);\n  }\n}\n@keyframes ct-scale-out {\n  from {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n  to {\n    opacity: 0;\n    -webkit-transform: scale(0.95);\n            transform: scale(0.95);\n  }\n}\n@-webkit-keyframes ct-zoom-in {\n  from {\n    opacity: 0;\n    -webkit-transform: scale(0.5);\n            transform: scale(0.5);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n}\n@keyframes ct-zoom-in {\n  from {\n    opacity: 0;\n    -webkit-transform: scale(0.5);\n            transform: scale(0.5);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n}\n@-webkit-keyframes ct-slide-up-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateY(100%);\n            transform: translateY(100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n}\n@keyframes ct-slide-up-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateY(100%);\n            transform: translateY(100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n}\n@-webkit-keyframes ct-slide-up-out {\n  from {\n    opacity: 1;\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n  to {\n    opacity: 0;\n    -webkit-transform: translateY(100%);\n            transform: translateY(100%);\n  }\n}\n@keyframes ct-slide-up-out {\n  from {\n    opacity: 1;\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n  to {\n    opacity: 0;\n    -webkit-transform: translateY(100%);\n            transform: translateY(100%);\n  }\n}\n@-webkit-keyframes ct-slide-down-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateY(-100%);\n            transform: translateY(-100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n}\n@keyframes ct-slide-down-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateY(-100%);\n            transform: translateY(-100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n}\n@-webkit-keyframes ct-slide-right-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateX(100%);\n            transform: translateX(100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateX(0);\n            transform: translateX(0);\n  }\n}\n@keyframes ct-slide-right-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateX(100%);\n            transform: translateX(100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateX(0);\n            transform: translateX(0);\n  }\n}\n@-webkit-keyframes ct-slide-left-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateX(-100%);\n            transform: translateX(-100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateX(0);\n            transform: translateX(0);\n  }\n}\n@keyframes ct-slide-left-in {\n  from {\n    opacity: 0;\n    -webkit-transform: translateX(-100%);\n            transform: translateX(-100%);\n  }\n  to {\n    opacity: 1;\n    -webkit-transform: translateX(0);\n            transform: translateX(0);\n  }\n}\n@-webkit-keyframes ct-pulse {\n  0%, 100% {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n  50% {\n    opacity: 0.85;\n    -webkit-transform: scale(1.02);\n            transform: scale(1.02);\n  }\n}\n@keyframes ct-pulse {\n  0%, 100% {\n    opacity: 1;\n    -webkit-transform: scale(1);\n            transform: scale(1);\n  }\n  50% {\n    opacity: 0.85;\n    -webkit-transform: scale(1.02);\n            transform: scale(1.02);\n  }\n}\n@-webkit-keyframes ct-spin {\n  from {\n    -webkit-transform: rotate(0deg);\n            transform: rotate(0deg);\n  }\n  to {\n    -webkit-transform: rotate(360deg);\n            transform: rotate(360deg);\n  }\n}\n@keyframes ct-spin {\n  from {\n    -webkit-transform: rotate(0deg);\n            transform: rotate(0deg);\n  }\n  to {\n    -webkit-transform: rotate(360deg);\n            transform: rotate(360deg);\n  }\n}\n@-webkit-keyframes ct-bounce {\n  0%, 100% {\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n  50% {\n    -webkit-transform: translateY(-10rpx);\n            transform: translateY(-10rpx);\n  }\n}\n@keyframes ct-bounce {\n  0%, 100% {\n    -webkit-transform: translateY(0);\n            transform: translateY(0);\n  }\n  50% {\n    -webkit-transform: translateY(-10rpx);\n            transform: translateY(-10rpx);\n  }\n}\n@-webkit-keyframes ct-shake {\n  0%, 100% {\n    -webkit-transform: translateX(0);\n            transform: translateX(0);\n  }\n  10%, 30%, 50%, 70%, 90% {\n    -webkit-transform: translateX(-4rpx);\n            transform: translateX(-4rpx);\n  }\n  20%, 40%, 60%, 80% {\n    -webkit-transform: translateX(4rpx);\n            transform: translateX(4rpx);\n  }\n}\n@keyframes ct-shake {\n  0%, 100% {\n    -webkit-transform: translateX(0);\n            transform: translateX(0);\n  }\n  10%, 30%, 50%, 70%, 90% {\n    -webkit-transform: translateX(-4rpx);\n            transform: translateX(-4rpx);\n  }\n  20%, 40%, 60%, 80% {\n    -webkit-transform: translateX(4rpx);\n            transform: translateX(4rpx);\n  }\n}\n@-webkit-keyframes ct-shimmer {\n  0% {\n    background-position: -200% 0;\n  }\n  100% {\n    background-position: 200% 0;\n  }\n}\n@keyframes ct-shimmer {\n  0% {\n    background-position: -200% 0;\n  }\n  100% {\n    background-position: 200% 0;\n  }\n}\n.ct-animate-fade-in {\n  -webkit-animation: ct-fade-in 250ms ease-out forwards;\n          animation: ct-fade-in 250ms ease-out forwards;\n}\n\n.ct-animate-fade-out {\n  -webkit-animation: ct-fade-out 250ms ease-in forwards;\n          animation: ct-fade-out 250ms ease-in forwards;\n}\n\n.ct-animate-scale-in {\n  -webkit-animation: ct-scale-in 250ms cubic-bezier(0.34, 1.56, 0.64, 1) forwards;\n          animation: ct-scale-in 250ms cubic-bezier(0.34, 1.56, 0.64, 1) forwards;\n}\n\n.ct-animate-slide-up {\n  -webkit-animation: ct-slide-up-in 250ms ease-out forwards;\n          animation: ct-slide-up-in 250ms ease-out forwards;\n}\n\n.ct-animate-slide-down {\n  -webkit-animation: ct-slide-down-in 250ms ease-out forwards;\n          animation: ct-slide-down-in 250ms ease-out forwards;\n}\n\n.ct-animate-pulse {\n  -webkit-animation: ct-pulse 2s ease-in-out infinite;\n          animation: ct-pulse 2s ease-in-out infinite;\n}\n\n.ct-animate-spin {\n  -webkit-animation: ct-spin 1s linear infinite;\n          animation: ct-spin 1s linear infinite;\n}\n\n.ct-animate-bounce {\n  -webkit-animation: ct-bounce 1s ease-in-out infinite;\n          animation: ct-bounce 1s ease-in-out infinite;\n}\n\n.ct-animate-shimmer {\n  background: -webkit-gradient(linear, left top, right top, color-stop(25%, var(--ct-color-bg-hover)), color-stop(50%, var(--ct-color-bg-card)), color-stop(75%, var(--ct-color-bg-hover)));\n  background: -webkit-linear-gradient(left, var(--ct-color-bg-hover) 25%, var(--ct-color-bg-card) 50%, var(--ct-color-bg-hover) 75%);\n  background: linear-gradient(90deg, var(--ct-color-bg-hover) 25%, var(--ct-color-bg-card) 50%, var(--ct-color-bg-hover) 75%);\n  background-size: 200% 100%;\n  -webkit-animation: ct-shimmer 1.5s infinite;\n          animation: ct-shimmer 1.5s infinite;\n}\n\n.ct-button {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  border: none;\n  -webkit-transition: all 150ms ease;\n  transition: all 150ms ease;\n  position: relative;\n  box-sizing: border-box;\n  margin-bottom: 16rpx;\n  --ct-button-height-xs: 56rpx;\n  --ct-button-height-sm: 64rpx;\n  --ct-button-height-md: 80rpx;\n  --ct-button-height-lg: 96rpx;\n  --ct-button-height-xl: 112rpx;\n  --ct-button-padding-xs: 0 24rpx;\n  --ct-button-padding-sm: 0 32rpx;\n  --ct-button-padding-md: 0 40rpx;\n  --ct-button-padding-lg: 0 48rpx;\n  --ct-button-padding-xl: 0 64rpx;\n  --ct-button-radius-xs: 8rpx;\n  --ct-button-radius-sm: 16rpx;\n  --ct-button-radius-md: 16rpx;\n  --ct-button-radius-lg: 24rpx;\n  --ct-button-radius-xl: 9999rpx;\n  --ct-button-font-size-xs: 22rpx;\n  --ct-button-font-size-sm: 26rpx;\n  --ct-button-font-size-md: 28rpx;\n  --ct-button-font-size-lg: 32rpx;\n  --ct-button-font-size-xl: 36rpx;\n}\n.ct-button--primary {\n  background: var(--ct-color-primary);\n  color: #fff;\n}\n.ct-button--primary .ct-button__text {\n  color: #fff;\n}\n.ct-button--primary .ct-button__spinner {\n  border-color: rgba(255, 255, 255, 0.3);\n  border-top-color: #fff;\n}\n.ct-button--secondary {\n  background: var(--ct-color-primary-light);\n}\n.ct-button--secondary .ct-button__text {\n  color: var(--ct-color-primary);\n}\n.ct-button--outline {\n  background: transparent;\n  border: 2rpx solid var(--ct-color-border);\n}\n.ct-button--outline .ct-button__text {\n  color: var(--ct-color-text-primary);\n}\n.ct-button--ghost {\n  background: transparent;\n}\n.ct-button--ghost .ct-button__text {\n  color: var(--ct-color-text-secondary);\n}\n.ct-button--link {\n  background: transparent;\n  padding: 0 !important;\n  height: auto !important;\n}\n.ct-button--link .ct-button__text {\n  color: var(--ct-color-primary);\n  text-decoration: underline;\n}\n.ct-button--danger {\n  background: var(--ct-color-error);\n  color: #fff;\n}\n.ct-button--danger .ct-button__text {\n  color: #fff;\n}\n.ct-button--danger .ct-button__spinner {\n  border-color: rgba(255, 255, 255, 0.3);\n  border-top-color: #fff;\n}\n.ct-button--hover {\n  opacity: 0.85;\n  -webkit-transform: scale(0.98);\n          transform: scale(0.98);\n}\n.ct-button--xs {\n  height: var(--ct-button-height-xs);\n  padding: var(--ct-button-padding-xs);\n  border-radius: var(--ct-button-radius-xs);\n}\n.ct-button--xs .ct-button__text {\n  font-size: var(--ct-button-font-size-xs);\n}\n.ct-button--sm {\n  height: var(--ct-button-height-sm);\n  padding: var(--ct-button-padding-sm);\n  border-radius: var(--ct-button-radius-sm);\n}\n.ct-button--sm .ct-button__text {\n  font-size: var(--ct-button-font-size-sm);\n}\n.ct-button--md {\n  height: var(--ct-button-height-md);\n  padding: var(--ct-button-padding-md);\n  border-radius: var(--ct-button-radius-md);\n}\n.ct-button--md .ct-button__text {\n  font-size: var(--ct-button-font-size-md);\n}\n.ct-button--lg {\n  height: var(--ct-button-height-lg);\n  padding: var(--ct-button-padding-lg);\n  border-radius: var(--ct-button-radius-lg);\n}\n.ct-button--lg .ct-button__text {\n  font-size: var(--ct-button-font-size-lg);\n}\n.ct-button--xl {\n  height: var(--ct-button-height-xl);\n  padding: var(--ct-button-padding-xl);\n  border-radius: var(--ct-button-radius-xl);\n}\n.ct-button--xl .ct-button__text {\n  font-size: var(--ct-button-font-size-xl);\n}\n.ct-button--block {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  width: 100%;\n}\n.ct-button--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-button--loading {\n  pointer-events: none;\n}\n.ct-button__text {\n  font-weight: 500;\n}\n.ct-button__icon {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  color: inherit;\n}\n.ct-button__icon--left {\n  margin-right: 4rpx;\n}\n.ct-button__icon--right {\n  margin-left: 4rpx;\n}\n.ct-button__loading {\n  margin-right: 16rpx;\n}\n.ct-button__spinner {\n  width: 32rpx;\n  height: 32rpx;\n  border: 4rpx solid currentColor;\n  border-top-color: transparent;\n  border-radius: 50%;\n  -webkit-animation: ct-spin 0.8s linear infinite;\n          animation: ct-spin 0.8s linear infinite;\n}";
styleInject(css_248z$K);

/**
 * Helpers
 */
const getIconColor = variant => {
  switch (variant) {
    case 'primary':
    case 'danger':
      return '#ffffff';
    case 'secondary':
    case 'link':
      return 'var(--ct-color-primary)';
    case 'outline':
    case 'ghost':
      return 'var(--ct-color-text-primary)';
    default:
      return undefined;
  }
};
const Button = ({
  variant = 'primary',
  size = 'md',
  disabled = false,
  loading = false,
  block = false,
  icon,
  iconRight,
  onClick,
  className,
  style,
  children
}) => {
  /**
   * Callbacks
   */
  const handleClick = () => {
    if (disabled || loading) return;
    onClick?.();
  };
  /**
   * Render Helpers
   */
  const renderIcon = iconElement => {
    if (/*#__PURE__*/require$$0.isValidElement(iconElement)) {
      const iconColor = getIconColor(variant);
      return /*#__PURE__*/require$$0.cloneElement(iconElement, {
        color: iconColor
      });
    }
    return iconElement;
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-button', `ct-button--${variant}`, `ct-button--${size}`, {
      'ct-button--block': block,
      'ct-button--disabled': disabled,
      'ct-button--loading': loading
    }, className),
    style: style,
    onClick: handleClick,
    hoverClass: disabled || loading ? 'none' : 'ct-button--hover',
    hoverStayTime: 100,
    children: [loading && jsxRuntimeExports.jsx(components.View, {
      className: "ct-button__loading",
      children: jsxRuntimeExports.jsx(components.View, {
        className: "ct-button__spinner"
      })
    }), !loading && icon && jsxRuntimeExports.jsx(components.View, {
      className: "ct-button__icon ct-button__icon--left",
      children: renderIcon(icon)
    }), jsxRuntimeExports.jsx(components.Text, {
      className: "ct-button__text",
      children: children
    }), !loading && iconRight && jsxRuntimeExports.jsx(components.View, {
      className: "ct-button__icon ct-button__icon--right",
      children: renderIcon(iconRight)
    })]
  });
};

var css_248z$J = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-calendar__wrapper {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 500;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  padding: 40rpx;\n}\n.ct-calendar__mask {\n  position: absolute;\n  inset: 0;\n  background: var(--ct-color-bg-mask);\n  z-index: 400;\n}\n.ct-calendar {\n  background: var(--ct-color-bg);\n  border-radius: 24rpx;\n  padding: 32rpx;\n  position: relative;\n  z-index: 500;\n}\n.ct-calendar__header {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: justify;\n          justify-content: space-between;\n}\n.ct-calendar__title {\n  font-size: 32rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-calendar__close:active, .ct-calendar__close.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-calendar__month {\n  margin-top: 24rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: justify;\n          justify-content: space-between;\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-calendar__nav:active, .ct-calendar__nav.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-calendar__nav {\n  color: var(--ct-color-text-tertiary);\n}\n.ct-calendar__nav--disabled {\n  color: var(--ct-color-text-quaternary);\n  pointer-events: none;\n}\n.ct-calendar__week {\n  margin-top: 24rpx;\n  display: grid;\n  grid-template-columns: repeat(7, 1fr);\n}\n.ct-calendar__week-item {\n  text-align: center;\n  padding: 16rpx 0;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-calendar__grid {\n  display: grid;\n  grid-template-columns: repeat(7, 1fr);\n  gap: 16rpx;\n  margin-top: 16rpx;\n}\n.ct-calendar__cell {\n  height: 80rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  border-radius: 16rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-calendar__cell:active, .ct-calendar__cell.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-calendar__cell--disabled {\n  color: var(--ct-color-text-quaternary);\n  pointer-events: none;\n}\n.ct-calendar__cell--in-range {\n  background: var(--ct-color-primary-light);\n}\n.ct-calendar__cell--selected, .ct-calendar__cell--start, .ct-calendar__cell--end {\n  background: var(--ct-color-primary);\n  color: #fff;\n}\n.ct-calendar__footer {\n  margin-top: 32rpx;\n}";
styleInject(css_248z$J);

/**
 * Constants
 */
const dayLabels = ['日', '一', '二', '三', '四', '五', '六'];
/**
 * Helpers
 */
const toDayNumber = date => date.getFullYear() * 10000 + (date.getMonth() + 1) * 100 + date.getDate();
const isSameDay = (a, b) => toDayNumber(a) === toDayNumber(b);
const getBaseDate = (value, type) => {
  if (value instanceof Date) return value;
  if (Array.isArray(value) && value.length > 0) return value[0];
  if (type === 'single') return new Date();
  return new Date();
};
const normalizeRange = value => {
  if (Array.isArray(value)) {
    return {
      start: value[0],
      end: value[1]
    };
  }
  if (value instanceof Date) {
    return {
      start: value,
      end: undefined
    };
  }
  return {
    start: undefined,
    end: undefined
  };
};
/**
 * Main Component
 */
const Calendar = ({
  type = 'single',
  value,
  defaultValue,
  minDate,
  maxDate,
  open,
  visible,
  showMask = true,
  closeOnOverlayClick = true,
  title = '选择日期',
  confirmText = '确定',
  showConfirm = true,
  firstDayOfWeek = 0,
  onChange,
  onConfirm,
  onClose,
  className
}) => {
  /**
   * Derived Values
   */
  const resolvedOpen = open ?? visible ?? false;
  /**
   * Hooks
   */
  const [selectedValue, setSelectedValue] = useControllable({
    value,
    defaultValue: defaultValue ?? (type === 'single' ? undefined : []),
    onChange
  });
  /**
   * States
   */
  const [selectingRangeEnd, setSelectingRangeEnd] = require$$0.useState(false);
  const [viewDate, setViewDate] = require$$0.useState(() => getBaseDate(selectedValue, type));
  /**
   * Derived Values
   */
  const baseDate = require$$0.useMemo(() => getBaseDate(selectedValue, type), [selectedValue, type]);
  const year = viewDate.getFullYear();
  const month = viewDate.getMonth();
  const weekLabels = require$$0.useMemo(() => {
    const start = Math.max(0, Math.min(6, firstDayOfWeek));
    return [...dayLabels.slice(start), ...dayLabels.slice(0, start)];
  }, [firstDayOfWeek]);
  const monthCells = require$$0.useMemo(() => {
    const firstDay = new Date(year, month, 1).getDay();
    const daysInMonth = new Date(year, month + 1, 0).getDate();
    const offset = (firstDay - firstDayOfWeek + 7) % 7;
    const total = Math.ceil((offset + daysInMonth) / 7) * 7;
    return Array.from({
      length: total
    }).map((_, index) => {
      const day = index - offset + 1;
      if (day <= 0 || day > daysInMonth) return null;
      return new Date(year, month, day);
    });
  }, [firstDayOfWeek, month, year]);
  const toMonthNumber = date => date.getFullYear() * 12 + date.getMonth();
  const minMonth = minDate ? toMonthNumber(minDate) : undefined;
  const maxMonth = maxDate ? toMonthNumber(maxDate) : undefined;
  const canGoPrev = minMonth === undefined || toMonthNumber(viewDate) > minMonth;
  const canGoNext = maxMonth === undefined || toMonthNumber(viewDate) < maxMonth;
  const selectedSingle = selectedValue instanceof Date ? selectedValue : undefined;
  const selectedMultiple = Array.isArray(selectedValue) ? selectedValue : [];
  const {
    start: rangeStart,
    end: rangeEnd
  } = normalizeRange(selectedValue);
  /**
   * Callbacks
   */
  const handlePrevMonth = () => {
    if (!canGoPrev) return;
    setViewDate(new Date(year, month - 1, 1));
  };
  const handleNextMonth = () => {
    if (!canGoNext) return;
    setViewDate(new Date(year, month + 1, 1));
  };
  const isDisabled = date => {
    const dayValue = toDayNumber(date);
    if (minDate && dayValue < toDayNumber(minDate)) return true;
    if (maxDate && dayValue > toDayNumber(maxDate)) return true;
    return false;
  };
  const handleSelect = date => {
    if (isDisabled(date)) return;
    if (type === 'single') {
      setSelectedValue(date);
      return;
    }
    if (type === 'multiple') {
      const current = Array.isArray(selectedValue) ? selectedValue : [];
      const exists = current.some(item => isSameDay(item, date));
      const next = exists ? current.filter(item => !isSameDay(item, date)) : [...current, date];
      setSelectedValue(next);
      return;
    }
    const {
      start,
      end
    } = normalizeRange(selectedValue);
    if (!start || end && !selectingRangeEnd) {
      setSelectedValue([date, date]);
      setSelectingRangeEnd(true);
      return;
    }
    const [rangeStartNew, rangeEndNew] = toDayNumber(date) < toDayNumber(start) ? [date, start] : [start, date];
    setSelectedValue([rangeStartNew, rangeEndNew]);
    setSelectingRangeEnd(false);
  };
  const handleConfirm = () => {
    if (!onConfirm) return;
    if (type === 'single') {
      onConfirm(selectedValue);
      return;
    }
    if (type === 'multiple') {
      onConfirm(selectedValue || []);
      return;
    }
    const {
      start,
      end
    } = normalizeRange(selectedValue);
    if (start && end) {
      onConfirm([start, end]);
    } else if (start) {
      onConfirm([start, start]);
    }
  };
  const handleOverlayClick = () => {
    if (!closeOnOverlayClick) return;
    onClose?.();
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (type !== 'range') return;
    const {
      start,
      end
    } = normalizeRange(selectedValue);
    if (!start) {
      setSelectingRangeEnd(false);
      return;
    }
    if (end && !isSameDay(start, end)) {
      setSelectingRangeEnd(false);
    }
  }, [selectedValue, type]);
  require$$0.useEffect(() => {
    if (resolvedOpen) {
      setViewDate(baseDate);
    }
  }, [baseDate, resolvedOpen]);
  /**
   * Early Returns
   */
  if (!resolvedOpen) return null;
  return jsxRuntimeExports.jsxs(components.View, {
    className: "ct-calendar__wrapper",
    children: [showMask && jsxRuntimeExports.jsx(components.View, {
      className: "ct-calendar__mask",
      onClick: handleOverlayClick
    }), jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-calendar', className),
      children: [jsxRuntimeExports.jsxs(components.View, {
        className: "ct-calendar__header",
        children: [jsxRuntimeExports.jsx(components.Text, {
          className: "ct-calendar__title",
          children: title
        }), onClose && jsxRuntimeExports.jsx(components.View, {
          className: "ct-calendar__close",
          hoverClass: "ct-clickable--hover",
          onClick: onClose,
          children: jsxRuntimeExports.jsx(Icon, {
            name: "close",
            size: 28
          })
        })]
      }), jsxRuntimeExports.jsxs(components.View, {
        className: "ct-calendar__month",
        children: [jsxRuntimeExports.jsx(components.View, {
          className: classNames('ct-calendar__nav', {
            'ct-calendar__nav--disabled': !canGoPrev
          }),
          hoverClass: canGoPrev ? 'ct-clickable--hover' : 'none',
          onClick: handlePrevMonth,
          children: jsxRuntimeExports.jsx(Icon, {
            name: "arrow-left",
            size: 28
          })
        }), jsxRuntimeExports.jsx(components.Text, {
          children: `${year}年${month + 1}月`
        }), jsxRuntimeExports.jsx(components.View, {
          className: classNames('ct-calendar__nav', {
            'ct-calendar__nav--disabled': !canGoNext
          }),
          hoverClass: canGoNext ? 'ct-clickable--hover' : 'none',
          onClick: handleNextMonth,
          children: jsxRuntimeExports.jsx(Icon, {
            name: "arrow-right",
            size: 28
          })
        })]
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-calendar__week",
        children: weekLabels.map(label => jsxRuntimeExports.jsx(components.View, {
          className: "ct-calendar__week-item",
          children: jsxRuntimeExports.jsx(components.Text, {
            children: label
          })
        }, label))
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-calendar__grid",
        children: monthCells.map((date, index) => {
          if (!date) {
            return jsxRuntimeExports.jsx(components.View, {
              className: "ct-calendar__cell"
            }, `empty-${index}`);
          }
          const disabled = isDisabled(date);
          const selected = type === 'single' ? selectedSingle && isSameDay(selectedSingle, date) : type === 'multiple' ? selectedMultiple.some(item => isSameDay(item, date)) : rangeStart && isSameDay(rangeStart, date) || rangeEnd && isSameDay(rangeEnd, date);
          const inRange = type === 'range' && rangeStart && rangeEnd ? toDayNumber(date) >= toDayNumber(rangeStart) && toDayNumber(date) <= toDayNumber(rangeEnd) : false;
          const isStart = type === 'range' && rangeStart && isSameDay(rangeStart, date);
          const isEnd = type === 'range' && rangeEnd && isSameDay(rangeEnd, date);
          return jsxRuntimeExports.jsx(components.View, {
            className: classNames('ct-calendar__cell', {
              'ct-calendar__cell--disabled': disabled,
              'ct-calendar__cell--selected': selected,
              'ct-calendar__cell--in-range': inRange,
              'ct-calendar__cell--start': isStart,
              'ct-calendar__cell--end': isEnd
            }),
            hoverClass: disabled ? 'none' : 'ct-clickable--hover',
            onClick: () => handleSelect(date),
            children: jsxRuntimeExports.jsx(components.Text, {
              children: date.getDate()
            })
          }, date.toISOString());
        })
      }), showConfirm && jsxRuntimeExports.jsx(components.View, {
        className: "ct-calendar__footer",
        children: jsxRuntimeExports.jsx(Button, {
          variant: "primary",
          onClick: handleConfirm,
          children: confirmText
        })
      })]
    })]
  });
};

var css_248z$I = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-card {\n  background: var(--ct-color-bg-card);\n  border-radius: 24rpx;\n  overflow: hidden;\n}\n.ct-card--bordered {\n  border: 2rpx solid var(--ct-color-border);\n}\n.ct-card--shadow {\n  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);\n}\n.ct-card--clickable:active, .ct-card--clickable.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-card--hover {\n  opacity: 0.9;\n}\n.ct-card__cover {\n  width: 100%;\n  aspect-ratio: 16/9;\n}\n.ct-card__cover-image {\n  width: 100%;\n  height: 100%;\n}\n.ct-card__header {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: start;\n          align-items: flex-start;\n  -webkit-box-pack: justify;\n          justify-content: space-between;\n  padding: 32rpx;\n  position: relative;\n}\n.ct-card__header::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-card__header--no-divider::after {\n  display: none;\n}\n.ct-card__header-content {\n  -webkit-box-flex: 1;\n          flex: 1;\n}\n.ct-card__title {\n  font-size: 32rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n}\n.ct-card__subtitle {\n  display: block;\n  margin-top: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-card__extra {\n  flex-shrink: 0;\n  margin-left: 24rpx;\n}\n.ct-card__body {\n  padding: 32rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n  line-height: 1.6;\n}\n.ct-card__header + .ct-card__body {\n  padding-top: 0;\n}\n.ct-card__footer {\n  padding: 24rpx 32rpx;\n  position: relative;\n}\n.ct-card__footer::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  top: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: top;\n          transform-origin: top;\n}";
styleInject(css_248z$I);

const Card = ({
  title,
  subtitle,
  extra,
  cover,
  footer,
  bordered = true,
  shadow = false,
  headerDivider = true,
  clickable = false,
  onClick,
  className,
  children
}) => {
  /**
   * Derived Values
   */
  const isClickable = clickable || !!onClick;
  /**
   * Callbacks
   */
  const handleClick = () => {
    if (isClickable) {
      onClick?.();
    }
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-card', {
      'ct-card--bordered': bordered,
      'ct-card--shadow': shadow,
      'ct-card--clickable': isClickable
    }, className),
    onClick: handleClick,
    hoverClass: isClickable ? 'ct-card--hover' : 'none',
    children: [cover && jsxRuntimeExports.jsx(components.View, {
      className: "ct-card__cover",
      children: jsxRuntimeExports.jsx(components.Image, {
        className: "ct-card__cover-image",
        src: cover,
        mode: "aspectFill"
      })
    }), (title || extra) && jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-card__header', {
        'ct-card__header--no-divider': !headerDivider
      }),
      children: [jsxRuntimeExports.jsxs(components.View, {
        className: "ct-card__header-content",
        children: [title && jsxRuntimeExports.jsx(components.Text, {
          className: "ct-card__title",
          children: title
        }), subtitle && jsxRuntimeExports.jsx(components.Text, {
          className: "ct-card__subtitle",
          children: subtitle
        })]
      }), extra && jsxRuntimeExports.jsx(components.View, {
        className: "ct-card__extra",
        children: extra
      })]
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-card__body",
      children: children
    }), footer && jsxRuntimeExports.jsx(components.View, {
      className: "ct-card__footer",
      children: footer
    })]
  });
};

var css_248z$H = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-cascader__header {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: justify;\n          justify-content: space-between;\n  height: 96rpx;\n  padding: 0 32rpx;\n  border-bottom: 2rpx solid var(--ct-color-border);\n}\n.ct-cascader__cancel:active, .ct-cascader__cancel.ct-clickable--hover, .ct-cascader__confirm:active, .ct-cascader__confirm.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-cascader__cancel, .ct-cascader__confirm {\n  padding: 16rpx 8rpx;\n}\n.ct-cascader__cancel-text {\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-cascader__confirm-text {\n  font-size: 28rpx;\n  color: var(--ct-color-primary);\n}\n.ct-cascader__title {\n  font-size: 36rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n}\n.ct-cascader__tabs {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  padding: 0 32rpx;\n  border-bottom: 2rpx solid var(--ct-color-border);\n  white-space: nowrap;\n}\n.ct-cascader__tab {\n  position: relative;\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n}\n.ct-cascader__tab:active, .ct-cascader__tab.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-cascader__tab--active .ct-cascader__tab-text {\n  color: var(--ct-color-primary);\n  font-weight: 500;\n}\n.ct-cascader__tab-text {\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  -webkit-transition: color 150ms ease;\n  transition: color 150ms ease;\n}\n.ct-cascader__tab-text--placeholder {\n  color: var(--ct-color-text-tertiary);\n}\n.ct-cascader__tab-line {\n  position: absolute;\n  bottom: 0;\n  left: 32rpx;\n  right: 32rpx;\n  height: 4rpx;\n  background: var(--ct-color-primary);\n  border-radius: 2rpx;\n}\n.ct-cascader__content {\n  position: relative;\n  height: 600rpx;\n}\n.ct-cascader__panel {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  padding: 16rpx 0;\n  opacity: 0;\n  visibility: hidden;\n  -webkit-transition: opacity 150ms ease, visibility 150ms ease;\n  transition: opacity 150ms ease, visibility 150ms ease;\n}\n.ct-cascader__panel--active {\n  opacity: 1;\n  visibility: visible;\n}\n.ct-cascader__option {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: justify;\n          justify-content: space-between;\n  padding: 24rpx 32rpx;\n}\n.ct-cascader__option:active, .ct-cascader__option.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-cascader__option--selected .ct-cascader__option-text {\n  color: var(--ct-color-primary);\n  font-weight: 500;\n}\n.ct-cascader__option--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n  pointer-events: none;\n}\n.ct-cascader__option-text {\n  -webkit-box-flex: 1;\n          flex: 1;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-cascader__option-icon {\n  flex-shrink: 0;\n  color: var(--ct-color-primary);\n}";
styleInject(css_248z$H);

const Cascader = ({
  open,
  options,
  value,
  defaultValue,
  title,
  placeholder = '请选择',
  confirmText = '确定',
  cancelText = '取消',
  onChange,
  onConfirm,
  onCancel,
  onClose,
  resetOnClose = true,
  className,
  renderOption
}) => {
  /**
   * Hooks
   */
  const [internalValue, setInternalValue] = useControllable({
    value,
    defaultValue: defaultValue || []
  });
  /**
   * States
   */
  const [tempValue, setTempValue] = require$$0.useState([]);
  const [activeLevel, setActiveLevel] = require$$0.useState(0);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (open) {
      setTempValue(internalValue || []);
      const level = internalValue?.length || 0;
      setActiveLevel(Math.max(0, level > 0 ? level - 1 : 0));
    }
  }, [open, internalValue]);
  /**
   * Derived Values
   */
  const levelOptions = require$$0.useMemo(() => {
    const levels = [options];
    let currentOptions = options;
    for (const val of tempValue) {
      const selected = currentOptions.find(opt => opt.value === val);
      if (selected?.children && selected.children.length > 0) {
        levels.push(selected.children);
        currentOptions = selected.children;
      } else {
        break;
      }
    }
    return levels;
  }, [options, tempValue]);
  const selectedOptions = require$$0.useMemo(() => {
    const result = [];
    let currentOptions = options;
    for (const val of tempValue) {
      const selected = currentOptions.find(opt => opt.value === val);
      if (selected) {
        result.push(selected);
        if (selected.children) {
          currentOptions = selected.children;
        } else {
          break;
        }
      } else {
        break;
      }
    }
    return result;
  }, [options, tempValue]);
  /**
   * Callbacks
   */
  const handleOptionClick = require$$0.useCallback((option, level) => {
    if (option.disabled) return;
    const newValue = [...tempValue.slice(0, level), option.value];
    setTempValue(newValue);
    if (option.children && option.children.length > 0) {
      setActiveLevel(level + 1);
    } else {
      setActiveLevel(level);
    }
  }, [tempValue]);
  const handleTabClick = require$$0.useCallback(level => {
    setActiveLevel(level);
  }, []);
  const handleConfirm = require$$0.useCallback(() => {
    setInternalValue(tempValue);
    onChange?.(tempValue, selectedOptions);
    onConfirm?.(tempValue, selectedOptions);
    onClose?.();
  }, [tempValue, selectedOptions, setInternalValue, onChange, onConfirm, onClose]);
  const handleCancel = require$$0.useCallback(() => {
    if (resetOnClose) {
      setTempValue(internalValue || []);
    }
    onCancel?.();
    onClose?.();
  }, [resetOnClose, internalValue, onCancel, onClose]);
  const handleClose = require$$0.useCallback(() => {
    if (resetOnClose) {
      setTempValue(internalValue || []);
    }
    onClose?.();
  }, [resetOnClose, internalValue, onClose]);
  /**
   * Render Helpers
   */
  const renderTabs = () => {
    const tabs = [];
    for (let i = 0; i <= Math.min(activeLevel, levelOptions.length - 1); i++) {
      const selected = selectedOptions[i];
      const isActive = i === activeLevel;
      const isPlaceholder = !selected;
      tabs.push(jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-cascader__tab', {
          'ct-cascader__tab--active': isActive
        }),
        onClick: () => handleTabClick(i),
        children: [jsxRuntimeExports.jsx(components.Text, {
          className: classNames('ct-cascader__tab-text', {
            'ct-cascader__tab-text--placeholder': isPlaceholder
          }),
          children: selected?.label || placeholder
        }), isActive && jsxRuntimeExports.jsx(components.View, {
          className: "ct-cascader__tab-line"
        })]
      }, i));
    }
    return tabs;
  };
  const renderOptions = (opts, level) => {
    const currentValue = tempValue[level];
    return opts.map(option => {
      const isSelected = option.value === currentValue;
      if (renderOption) {
        return jsxRuntimeExports.jsx(components.View, {
          className: classNames('ct-cascader__option', {
            'ct-cascader__option--selected': isSelected,
            'ct-cascader__option--disabled': option.disabled
          }),
          onClick: () => handleOptionClick(option, level),
          children: renderOption(option, isSelected)
        }, option.value);
      }
      return jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-cascader__option', {
          'ct-cascader__option--selected': isSelected,
          'ct-cascader__option--disabled': option.disabled
        }),
        onClick: () => handleOptionClick(option, level),
        children: [jsxRuntimeExports.jsx(components.Text, {
          className: "ct-cascader__option-text",
          children: option.label
        }), isSelected && jsxRuntimeExports.jsx(Icon, {
          name: "check",
          size: 36,
          className: "ct-cascader__option-icon"
        })]
      }, option.value);
    });
  };
  return jsxRuntimeExports.jsxs(BottomSheet, {
    open: open,
    onClose: handleClose,
    className: classNames('ct-cascader', className),
    children: [jsxRuntimeExports.jsxs(components.View, {
      className: "ct-cascader__header",
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-cascader__cancel",
        onClick: handleCancel,
        children: jsxRuntimeExports.jsx(components.Text, {
          className: "ct-cascader__cancel-text",
          children: cancelText
        })
      }), title && jsxRuntimeExports.jsx(components.Text, {
        className: "ct-cascader__title",
        children: title
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-cascader__confirm",
        onClick: handleConfirm,
        children: jsxRuntimeExports.jsx(components.Text, {
          className: "ct-cascader__confirm-text",
          children: confirmText
        })
      })]
    }), jsxRuntimeExports.jsx(components.ScrollView, {
      scrollX: true,
      className: "ct-cascader__tabs",
      children: renderTabs()
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-cascader__content",
      children: levelOptions.map((opts, level) => jsxRuntimeExports.jsx(components.ScrollView, {
        scrollY: true,
        className: classNames('ct-cascader__panel', {
          'ct-cascader__panel--active': level === activeLevel
        }),
        children: renderOptions(opts, level)
      }, level))
    })]
  });
};

var css_248z$G = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-checkbox {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-checkbox:active, .ct-checkbox.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-checkbox--right {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: reverse;\n          flex-direction: row-reverse;\n}\n.ct-checkbox--right .ct-checkbox__label {\n  margin-right: 16rpx;\n  margin-left: 0;\n}\n.ct-checkbox__icon {\n  width: 40rpx;\n  height: 40rpx;\n  border: 2rpx solid var(--ct-color-border);\n  background: var(--ct-color-bg-card);\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  -webkit-transition: all 150ms ease;\n  transition: all 150ms ease;\n  flex-shrink: 0;\n}\n.ct-checkbox--square .ct-checkbox__icon {\n  border-radius: 8rpx;\n}\n.ct-checkbox--round .ct-checkbox__icon {\n  border-radius: 50%;\n}\n.ct-checkbox--checked .ct-checkbox__icon {\n  background: var(--ct-color-primary);\n  border-color: var(--ct-color-primary);\n}\n.ct-checkbox--indeterminate .ct-checkbox__icon {\n  background: var(--ct-color-primary);\n  border-color: var(--ct-color-primary);\n}\n.ct-checkbox__indeterminate {\n  width: 20rpx;\n  height: 4rpx;\n  background: #fff;\n  border-radius: 2rpx;\n}\n.ct-checkbox__label {\n  margin-left: 16rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  line-height: 1.5;\n}\n.ct-checkbox--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-checkbox--disabled .ct-checkbox__icon {\n  background: var(--ct-color-bg-disabled);\n  border-color: var(--ct-color-border);\n}\n.ct-checkbox--disabled.ct-checkbox--checked .ct-checkbox__icon {\n  background: var(--ct-color-primary-light);\n  border-color: var(--ct-color-primary-light);\n}\n\n.ct-checkbox-group {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  flex-wrap: wrap;\n}\n.ct-checkbox-group--vertical {\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n}\n.ct-checkbox-group--vertical .ct-checkbox {\n  margin-bottom: 24rpx;\n}\n.ct-checkbox-group--vertical .ct-checkbox:last-child {\n  margin-bottom: 0;\n}\n.ct-checkbox-group--horizontal {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: normal;\n          flex-direction: row;\n}\n.ct-checkbox-group--horizontal .ct-checkbox {\n  margin-right: 32rpx;\n}\n.ct-checkbox-group--horizontal .ct-checkbox:last-child {\n  margin-right: 0;\n}";
styleInject(css_248z$G);

/**
 * Context
 */
const CheckboxContext = /*#__PURE__*/require$$0.createContext({});
const Checkbox = ({
  checked: propChecked,
  defaultChecked = false,
  disabled = false,
  indeterminate = false,
  value,
  onChange,
  shape = 'square',
  iconPosition = 'left',
  checkedColor,
  className,
  children
}) => {
  /**
   * Hooks
   */
  const ctx = require$$0.useContext(CheckboxContext);
  /**
   * Derived Values
   */
  const isGroupMode = ctx.value !== undefined;
  const isChecked = isGroupMode ? value !== undefined && ctx.value?.includes(value) : propChecked;
  const [checked, setChecked] = useControllable({
    value: isGroupMode ? isChecked : propChecked,
    defaultValue: defaultChecked,
    onChange
  });
  const isDisabled = disabled || ctx.disabled;
  const iconStyle = checkedColor && checked ? {
    backgroundColor: checkedColor,
    borderColor: checkedColor
  } : undefined;
  /**
   * Callbacks
   */
  const handleClick = () => {
    if (isDisabled) return;
    if (isGroupMode) {
      if (value === undefined) {
        if (process.env.NODE_ENV !== 'production') {
          console.warn('[Checkbox] value is required when used inside Checkbox.Group.');
        }
        return;
      }
      ctx.onChange?.(value, !checked);
    } else {
      setChecked(!checked);
    }
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-checkbox', `ct-checkbox--${shape}`, {
      'ct-checkbox--checked': checked,
      'ct-checkbox--disabled': isDisabled,
      'ct-checkbox--indeterminate': indeterminate,
      'ct-checkbox--right': iconPosition === 'right'
    }, className),
    hoverClass: isDisabled ? 'none' : 'ct-clickable--hover',
    onClick: handleClick,
    children: [jsxRuntimeExports.jsxs(components.View, {
      className: "ct-checkbox__icon",
      style: iconStyle,
      children: [checked && !indeterminate && jsxRuntimeExports.jsx(Icon, {
        name: "check",
        size: 28,
        color: "#fff"
      }), indeterminate && jsxRuntimeExports.jsx(components.View, {
        className: "ct-checkbox__indeterminate"
      })]
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-checkbox__label",
      children: typeof children === 'string' ? jsxRuntimeExports.jsx(components.Text, {
        children: children
      }) : children
    })]
  });
};
const CheckboxGroup = ({
  value: propValue,
  defaultValue = [],
  disabled = false,
  max,
  direction = 'vertical',
  onChange,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange
  });
  /**
   * Callbacks
   */
  const handleChange = (val, checked) => {
    let newValue;
    if (checked) {
      if (max && value.length >= max) return;
      newValue = [...value, val];
    } else {
      newValue = value.filter(v => v !== val);
    }
    setValue(newValue);
  };
  return jsxRuntimeExports.jsx(CheckboxContext.Provider, {
    value: {
      value,
      disabled,
      onChange: handleChange
    },
    children: jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-checkbox-group', `ct-checkbox-group--${direction}`, className),
      children: children
    })
  });
};
Checkbox.Group = CheckboxGroup;

const zhCN = {
  locale: 'zh-CN',
  common: {
    confirm: '确定',
    cancel: '取消',
    loading: '加载中...',
    empty: '暂无数据'
  },
  calendar: {
    weekdays: ['日', '一', '二', '三', '四', '五', '六'],
    months: ['一月', '二月', '三月', '四月', '五月', '六月', '七月', '八月', '九月', '十月', '十一月', '十二月'],
    today: '今天',
    startDate: '开始日期',
    endDate: '结束日期'
  },
  picker: {
    confirm: '确定',
    cancel: '取消'
  },
  pullRefresh: {
    pulling: '下拉刷新',
    canRelease: '释放刷新',
    refreshing: '刷新中...',
    complete: '刷新完成'
  },
  infiniteScroll: {
    loadingText: '加载中...',
    finishedText: '没有更多了',
    errorText: '加载失败，点击重试'
  }
};

/**
 * Constants
 */
const defaultTheme = {
  mode: 'light'
};
const defaultLocale = zhCN;
/**
 * Context
 */
const ConfigContext = /*#__PURE__*/require$$0.createContext({
  theme: defaultTheme,
  locale: defaultLocale
});
/**
 * Hooks
 */
const useConfig = () => require$$0.useContext(ConfigContext);
/**
 * Main Component
 */
const ConfigProvider = ({
  theme,
  locale,
  children
}) => {
  /**
   * Derived Values
   */
  const mergedTheme = require$$0.useMemo(() => ({
    ...defaultTheme,
    ...theme
  }), [theme]);
  const mergedLocale = require$$0.useMemo(() => ({
    ...defaultLocale,
    ...locale,
    common: {
      ...defaultLocale.common,
      ...locale?.common
    },
    calendar: {
      ...defaultLocale.calendar,
      ...locale?.calendar
    },
    picker: {
      ...defaultLocale.picker,
      ...locale?.picker
    },
    pullRefresh: {
      ...defaultLocale.pullRefresh,
      ...locale?.pullRefresh
    },
    infiniteScroll: {
      ...defaultLocale.infiniteScroll,
      ...locale?.infiniteScroll
    }
  }), [locale]);
  const contextValue = require$$0.useMemo(() => ({
    theme: mergedTheme,
    locale: mergedLocale
  }), [mergedTheme, mergedLocale]);
  /**
   * Effects
   */
  // 应用主题 CSS 变量
  require$$0.useEffect(() => {
    const cssVars = {};
    if (mergedTheme.primaryColor) {
      cssVars['--ct-color-primary'] = mergedTheme.primaryColor;
    }
    if (mergedTheme.successColor) {
      cssVars['--ct-color-success'] = mergedTheme.successColor;
    }
    if (mergedTheme.warningColor) {
      cssVars['--ct-color-warning'] = mergedTheme.warningColor;
    }
    if (mergedTheme.errorColor) {
      cssVars['--ct-color-error'] = mergedTheme.errorColor;
    }
    // 在 Web 环境下设置 CSS 变量
    if (Taro.getEnv?.() === Taro.ENV_TYPE.WEB && typeof document !== 'undefined') {
      Object.entries(cssVars).forEach(([key, value]) => {
        document.documentElement.style.setProperty(key, value);
      });
    }
  }, [mergedTheme]);
  // 处理深色模式
  require$$0.useEffect(() => {
    if (Taro.getEnv?.() === Taro.ENV_TYPE.WEB && typeof document !== 'undefined') {
      const html = document.documentElement;
      if (mergedTheme.mode === 'dark') {
        html.setAttribute('data-theme', 'dark');
      } else if (mergedTheme.mode === 'light') {
        html.removeAttribute('data-theme');
      } else if (mergedTheme.mode === 'system') {
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (prefersDark) {
          html.setAttribute('data-theme', 'dark');
        } else {
          html.removeAttribute('data-theme');
        }
      }
    }
  }, [mergedTheme.mode]);
  return jsxRuntimeExports.jsx(ConfigContext.Provider, {
    value: contextValue,
    children: jsxRuntimeExports.jsx(components.View, {
      className: "ct-config-provider",
      children: children
    })
  });
};

/**
 * Constants
 */
const DEFAULT_ITEM_HEIGHT = 88;
const PickerView = ({
  options,
  columns,
  value,
  defaultValue,
  onChange,
  visibleCount = 5,
  itemHeight = DEFAULT_ITEM_HEIGHT,
  className
}) => {
  /**
   * Derived Values
   */
  const normalizedColumns = require$$0.useMemo(() => {
    if (columns) return columns;
    if (options) return [{
      options
    }];
    return [];
  }, [columns, options]);
  /**
   * Render Helpers
   */
  const valueToIndexes = val => {
    if (!normalizedColumns.length) return [];
    if (!val) return normalizedColumns.map(() => 0);
    return normalizedColumns.map((col, i) => {
      const idx = col.options.findIndex(opt => opt.value === val[i]);
      return idx >= 0 ? idx : 0;
    });
  };
  const indexesToValue = indexes => {
    return indexes.map((idx, colIdx) => normalizedColumns[colIdx]?.options[idx]?.value);
  };
  const indexesToOptions = indexes => {
    return indexes.map((idx, colIdx) => normalizedColumns[colIdx]?.options[idx]);
  };
  /**
   * States
   */
  const [selectedIndexes, setSelectedIndexes] = require$$0.useState(() => valueToIndexes(value ?? defaultValue));
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (value !== undefined) {
      setSelectedIndexes(valueToIndexes(value));
    }
  }, [value]);
  require$$0.useEffect(() => {
    if (!normalizedColumns.length) {
      setSelectedIndexes([]);
      return;
    }
    setSelectedIndexes(prev => prev.map((idx, colIdx) => {
      const count = normalizedColumns[colIdx]?.options.length ?? 0;
      if (count <= 0) return 0;
      return Math.min(idx, count - 1);
    }));
  }, [normalizedColumns]);
  /**
   * Early Returns
   */
  if (!normalizedColumns.length) {
    return jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-picker-view', 'ct-picker-view--empty', className),
      children: jsxRuntimeExports.jsx(components.Text, {
        className: "ct-picker-view__empty",
        children: "\u6682\u65E0\u6570\u636E"
      })
    });
  }
  /**
   * Callbacks
   */
  const handleChange = e => {
    const newIndexes = e.detail.value;
    setSelectedIndexes(newIndexes);
    onChange?.(indexesToValue(newIndexes), indexesToOptions(newIndexes));
  };
  const pickerHeight = itemHeight * visibleCount;
  const maskHeight = itemHeight * Math.floor((visibleCount - 1) / 2);
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-picker-view', className),
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-picker-view__mask ct-picker-view__mask--top",
      style: {
        height: `${maskHeight}rpx`
      }
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-picker-view__indicator",
      style: {
        height: `${itemHeight}rpx`,
        top: `${maskHeight}rpx`
      }
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-picker-view__mask ct-picker-view__mask--bottom",
      style: {
        height: `${maskHeight}rpx`
      }
    }), jsxRuntimeExports.jsx(components.PickerView, {
      className: "ct-picker-view__wheel",
      style: {
        height: `${pickerHeight}rpx`
      },
      indicatorStyle: `height: ${itemHeight}rpx`,
      value: selectedIndexes,
      onChange: handleChange,
      children: normalizedColumns.map((column, colIdx) => jsxRuntimeExports.jsx(components.PickerViewColumn, {
        children: column.options.map((option, optIdx) => jsxRuntimeExports.jsx(components.View, {
          className: classNames('ct-picker-view__item', {
            'ct-picker-view__item--selected': optIdx === selectedIndexes[colIdx],
            'ct-picker-view__item--disabled': option.disabled
          }),
          style: {
            height: `${itemHeight}rpx`,
            lineHeight: `${itemHeight}rpx`
          },
          children: jsxRuntimeExports.jsx(components.Text, {
            children: option.label
          })
        }, option.value))
      }, colIdx))
    })]
  });
};

const Picker = ({
  open,
  title,
  confirmText = '确定',
  cancelText = '取消',
  onConfirm,
  onCancel,
  onClose,
  onChange,
  value,
  defaultValue,
  ...pickerViewProps
}) => {
  /**
   * States
   */
  const [show, setShow] = require$$0.useState(false);
  const [animating, setAnimating] = require$$0.useState(false);
  const [tempValue, setTempValue] = require$$0.useState([]);
  const [tempOptions, setTempOptions] = require$$0.useState([]);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (open) {
      setShow(true);
      setTempValue(value ?? defaultValue ?? []);
      Taro.nextTick(() => setAnimating(true));
    } else {
      setAnimating(false);
      const timer = setTimeout(() => setShow(false), OVERLAY_ANIMATION_DURATION);
      return () => clearTimeout(timer);
    }
    return undefined;
  }, [open]);
  /**
   * Callbacks
   */
  const handlePickerChange = (val, opts) => {
    setTempValue(val);
    setTempOptions(opts);
    onChange?.(val, opts);
  };
  const handleConfirm = () => {
    onConfirm?.(tempValue, tempOptions);
    onClose?.();
  };
  const handleCancel = () => {
    onCancel?.();
    onClose?.();
  };
  /**
   * Early Returns
   */
  if (!show) return null;
  return jsxRuntimeExports.jsxs(components.View, {
    className: "ct-picker",
    catchMove: true,
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-picker__mask', {
        'ct-picker__mask--visible': animating
      }),
      onClick: handleCancel
    }), jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-picker__panel', {
        'ct-picker__panel--visible': animating
      }),
      children: [jsxRuntimeExports.jsxs(components.View, {
        className: "ct-picker__toolbar",
        children: [jsxRuntimeExports.jsx(components.View, {
          className: "ct-picker__cancel",
          onClick: handleCancel,
          children: jsxRuntimeExports.jsx(components.Text, {
            children: cancelText
          })
        }), title && jsxRuntimeExports.jsx(components.View, {
          className: "ct-picker__title",
          children: jsxRuntimeExports.jsx(components.Text, {
            children: title
          })
        }), jsxRuntimeExports.jsx(components.View, {
          className: "ct-picker__confirm",
          onClick: handleConfirm,
          children: jsxRuntimeExports.jsx(components.Text, {
            children: confirmText
          })
        })]
      }), jsxRuntimeExports.jsx(PickerView, {
        ...pickerViewProps,
        value: tempValue,
        onChange: handlePickerChange
      })]
    })]
  });
};

var css_248z$F = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-picker {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 500;\n}\n.ct-picker__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg-mask);\n  opacity: 0;\n  -webkit-transition: opacity 250ms ease-out;\n  transition: opacity 250ms ease-out;\n}\n.ct-picker__mask--visible {\n  opacity: 1;\n}\n.ct-picker__panel {\n  position: absolute;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg);\n  border-radius: 32rpx 32rpx 0 0;\n  -webkit-transform: translateY(100%);\n          transform: translateY(100%);\n  -webkit-transition: -webkit-transform 250ms ease-out;\n  transition: -webkit-transform 250ms ease-out;\n  transition: transform 250ms ease-out;\n  transition: transform 250ms ease-out, -webkit-transform 250ms ease-out;\n  padding-bottom: calc(0 + constant(safe-area-inset-bottom));\n  padding-bottom: calc(0 + env(safe-area-inset-bottom));\n}\n.ct-picker__panel--visible {\n  -webkit-transform: translateY(0);\n          transform: translateY(0);\n}\n.ct-picker__toolbar {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: justify;\n          justify-content: space-between;\n  padding: 24rpx 32rpx;\n  position: relative;\n}\n.ct-picker__toolbar::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-picker__cancel, .ct-picker__confirm {\n  padding: 16rpx 24rpx;\n  font-size: 28rpx;\n}\n.ct-picker__cancel {\n  color: var(--ct-color-text-secondary);\n}\n.ct-picker__confirm {\n  color: var(--ct-color-primary);\n  font-weight: 500;\n}\n.ct-picker__title {\n  -webkit-box-flex: 1;\n          flex: 1;\n  text-align: center;\n  font-size: 32rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n}\n\n.ct-picker-view {\n  position: relative;\n}\n.ct-picker-view--empty {\n  min-height: 200rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-picker-view__empty {\n  font-size: 28rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-picker-view__wheel {\n  position: relative;\n}\n.ct-picker-view__mask {\n  position: absolute;\n  left: 0;\n  right: 0;\n  background: -webkit-gradient(linear, left top, left bottom, from(var(--ct-color-bg)), to(rgba(255, 255, 255, 0)));\n  background: -webkit-linear-gradient(top, var(--ct-color-bg) 0%, rgba(255, 255, 255, 0) 100%);\n  background: linear-gradient(to bottom, var(--ct-color-bg) 0%, rgba(255, 255, 255, 0) 100%);\n  z-index: 1;\n}\n.ct-picker-view__mask--top {\n  top: 0;\n}\n.ct-picker-view__mask--bottom {\n  bottom: 0;\n  -webkit-transform: rotate(180deg);\n          transform: rotate(180deg);\n}\n.ct-picker-view__indicator {\n  position: absolute;\n  left: 0;\n  right: 0;\n  border-top: 2rpx solid var(--ct-color-border);\n  border-bottom: 2rpx solid var(--ct-color-border);\n  z-index: 1;\n}\n.ct-picker-view__item {\n  text-align: center;\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-picker-view__item--selected {\n  color: var(--ct-color-text-primary);\n  font-weight: 500;\n}\n.ct-picker-view__item--disabled {\n  color: var(--ct-color-text-quaternary);\n}";
styleInject(css_248z$F);

/**
 * Helpers
 */
const getDaysInMonth = (year, month) => new Date(year, month, 0).getDate();
const generateDateColumns = (min, max, precision, currentValue) => {
  const columns = [];
  const years = [];
  for (let y = min.getFullYear(); y <= max.getFullYear(); y += 1) {
    years.push({
      label: `${y}年`,
      value: y
    });
  }
  columns.push({
    options: years
  });
  if (precision === 'year') return columns;
  const months = [];
  for (let m = 1; m <= 12; m += 1) {
    months.push({
      label: `${m}月`,
      value: m
    });
  }
  columns.push({
    options: months
  });
  if (precision === 'month') return columns;
  const selectedYear = typeof currentValue?.[0] === 'number' ? Number(currentValue[0]) : min.getFullYear();
  const selectedMonth = typeof currentValue?.[1] === 'number' ? Number(currentValue[1]) : min.getMonth() + 1;
  const daysInMonth = getDaysInMonth(selectedYear, selectedMonth);
  const days = [];
  for (let d = 1; d <= daysInMonth; d += 1) {
    days.push({
      label: `${d}日`,
      value: d
    });
  }
  columns.push({
    options: days
  });
  return columns;
};
const DatePicker = ({
  open,
  value,
  defaultValue,
  min = new Date(1900, 0, 1),
  max = new Date(2100, 11, 31),
  precision = 'day',
  title = '选择日期',
  onConfirm,
  onCancel,
  onClose
}) => {
  /**
   * Render Helpers
   */
  const dateToValue = date => {
    if (!date) return undefined;
    const result = [date.getFullYear(), date.getMonth() + 1];
    if (precision === 'day') result.push(date.getDate());
    return result;
  };
  /**
   * States
   */
  const [currentValue, setCurrentValue] = require$$0.useState(dateToValue(value ?? defaultValue ?? new Date()));
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    setCurrentValue(dateToValue(value ?? defaultValue ?? new Date()));
  }, [defaultValue, precision, value]);
  /**
   * Derived Values
   */
  const columns = require$$0.useMemo(() => generateDateColumns(min, max, precision, currentValue), [currentValue, min, max, precision]);
  /**
   * Callbacks
   */
  const handleConfirm = val => {
    const [year, month = 1, day = 1] = val;
    const safeDay = precision === 'day' ? Math.min(day, getDaysInMonth(year, month)) : 1;
    onConfirm?.(new Date(year, month - 1, safeDay));
  };
  return jsxRuntimeExports.jsx(Picker, {
    open: open,
    title: title,
    columns: columns,
    value: dateToValue(value),
    defaultValue: dateToValue(defaultValue ?? new Date()),
    onChange: val => setCurrentValue(val),
    onConfirm: handleConfirm,
    onCancel: onCancel,
    onClose: onClose
  });
};

var css_248z$E = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-divider--horizontal {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  margin: 32rpx 0;\n  color: var(--ct-color-text-tertiary);\n  font-size: 26rpx;\n}\n.ct-divider--horizontal:not(.ct-divider--with-content) {\n  border-top: 2rpx solid var(--ct-color-border);\n}\n.ct-divider--vertical {\n  display: inline-block;\n  width: 2rpx;\n  height: 1em;\n  margin: 0 16rpx;\n  vertical-align: middle;\n  border-left: 2rpx solid var(--ct-color-border);\n}\n.ct-divider--dashed {\n  border-style: dashed;\n}\n.ct-divider--dashed .ct-divider__line {\n  border-style: dashed;\n}\n.ct-divider__line {\n  -webkit-box-flex: 1;\n          flex: 1;\n  border-top: 2rpx solid var(--ct-color-border);\n}\n.ct-divider__line--left {\n  -webkit-box-flex: 0;\n          flex: 0 0 10%;\n}\n.ct-divider__line--right {\n  -webkit-box-flex: 0;\n          flex: 0 0 10%;\n}\n.ct-divider__content {\n  padding: 0 24rpx;\n  white-space: nowrap;\n}";
styleInject(css_248z$E);

const Divider = ({
  direction = 'horizontal',
  contentPosition = 'center',
  dashed = false,
  color,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const lineStyle = color ? {
    borderColor: color
  } : undefined;
  if (direction === 'vertical' && children !== undefined && children !== null && process.env.NODE_ENV !== 'production') {
    // Divider vertical mode doesn't render children by design.
    console.warn('[Divider] children only apply to horizontal direction.');
  }
  /**
   * Early Returns
   */
  if (direction === 'vertical') {
    return jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-divider', 'ct-divider--vertical', {
        'ct-divider--dashed': dashed
      }, className),
      style: lineStyle
    });
  }
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-divider', 'ct-divider--horizontal', {
      'ct-divider--dashed': dashed,
      'ct-divider--with-content': children
    }, className),
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-divider__line', `ct-divider__line--${contentPosition}`),
      style: lineStyle
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-divider__content",
      children: typeof children === 'string' ? jsxRuntimeExports.jsx(components.Text, {
        children: children
      }) : children
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-divider__line",
      style: lineStyle
    })]
  });
};

var css_248z$D = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-dropdown-menu {\n  position: relative;\n}\n.ct-dropdown-menu__bar {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  height: 96rpx;\n  background: var(--ct-color-bg);\n  position: relative;\n}\n.ct-dropdown-menu__bar::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-dropdown-menu__item {\n  -webkit-box-flex: 1;\n          flex: 1;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-dropdown-menu__item:active, .ct-dropdown-menu__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-dropdown-menu__item--disabled {\n  opacity: 0.4;\n  pointer-events: none;\n}\n.ct-dropdown-menu__title {\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  margin-right: 8rpx;\n}\n.ct-dropdown-menu__icon {\n  -webkit-transition: -webkit-transform 150ms;\n  transition: -webkit-transform 150ms;\n  transition: transform 150ms;\n  transition: transform 150ms, -webkit-transform 150ms;\n}\n.ct-dropdown-menu__icon--rotate {\n  -webkit-transform: rotate(180deg);\n          transform: rotate(180deg);\n}\n.ct-dropdown-menu__overlay {\n  position: fixed;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg-mask);\n  z-index: 100;\n}\n.ct-dropdown-menu__content {\n  position: fixed;\n  left: 0;\n  right: 0;\n  z-index: 101;\n  background: var(--ct-color-bg);\n  max-height: 60vh;\n  overflow: hidden;\n}\n.ct-dropdown-menu__content--up {\n  bottom: 100%;\n}\n\n.ct-dropdown-item {\n  max-height: 60vh;\n}\n.ct-dropdown-item__option {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n  font-size: 28rpx;\n}\n.ct-dropdown-item__option:active, .ct-dropdown-item__option.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-dropdown-item__option--disabled {\n  opacity: 0.4;\n  pointer-events: none;\n}\n.ct-dropdown-item__option:not(:last-child) {\n  position: relative;\n}\n.ct-dropdown-item__option:not(:last-child)::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-dropdown-item__icon {\n  margin-right: 24rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-dropdown-item__text {\n  -webkit-box-flex: 1;\n          flex: 1;\n  color: var(--ct-color-text-primary);\n}\n.ct-dropdown-item__check {\n  margin-left: 16rpx;\n}";
styleInject(css_248z$D);

/**
 * Context
 */
const DropdownContext = /*#__PURE__*/require$$0.createContext(null);
/**
 * Sub Components
 */
const DropdownMenu = ({
  activeKey,
  defaultActiveKey = null,
  onActiveKeyChange,
  activeColor = 'var(--ct-color-primary)',
  direction = 'down',
  overlay = true,
  closeOnOverlayClick = true,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const [currentKey, setCurrentKey] = useControllable({
    value: activeKey,
    defaultValue: defaultActiveKey,
    onChange: onActiveKeyChange
  });
  /**
   * States
   */
  const [menuTop, setMenuTop] = require$$0.useState(0);
  const [itemValues, setItemValues] = require$$0.useState({});
  /**
   * Callbacks
   */
  const handleToggle = index => {
    setCurrentKey(currentKey === index ? null : index);
  };
  const handleClose = () => {
    setCurrentKey(null);
  };
  const handleItemValueChange = (index, value) => {
    setItemValues(prev => ({
      ...prev,
      [index]: value
    }));
  };
  const handleOverlayClick = () => {
    if (closeOnOverlayClick) {
      handleClose();
    }
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    const query = Taro.createSelectorQuery();
    query.select('.ct-dropdown-menu__bar').boundingClientRect();
    query.exec(res => {
      if (res[0]) {
        setMenuTop(res[0].bottom);
      }
    });
  }, []);
  /**
   * Render Helpers
   */
  const renderChildren = () => require$$0.Children.map(children, (child, index) => {
    if (/*#__PURE__*/require$$0.isValidElement(child)) {
      return /*#__PURE__*/require$$0.cloneElement(child, {
        __index: index
      });
    }
    return child;
  });
  return jsxRuntimeExports.jsx(DropdownContext.Provider, {
    value: {
      activeKey: currentKey,
      direction,
      activeColor,
      onToggle: handleToggle,
      onClose: handleClose,
      onItemValueChange: handleItemValueChange,
      itemValues
    },
    children: jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-dropdown-menu', className),
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-dropdown-menu__bar",
        children: require$$0.Children.map(children, (child, index) => {
          if (! /*#__PURE__*/require$$0.isValidElement(child)) return null;
          const {
            title,
            options,
            value,
            defaultValue,
            disabled
          } = child.props;
          const isActive = currentKey === index;
          const resolvedValue = value !== undefined ? value : itemValues[index] ?? defaultValue;
          const selectedOption = options.find(option => option.value === resolvedValue);
          return jsxRuntimeExports.jsxs(components.View, {
            className: classNames('ct-dropdown-menu__item', {
              'ct-dropdown-menu__item--active': isActive,
              'ct-dropdown-menu__item--disabled': disabled
            }),
            onClick: () => !disabled && handleToggle(index),
            children: [jsxRuntimeExports.jsx(components.Text, {
              className: "ct-dropdown-menu__title",
              style: isActive ? {
                color: activeColor
              } : undefined,
              children: title || selectedOption?.text || '请选择'
            }), jsxRuntimeExports.jsx(Icon, {
              name: direction === 'down' ? 'chevron-down' : 'chevron-up',
              size: 24,
              className: classNames('ct-dropdown-menu__icon', {
                'ct-dropdown-menu__icon--rotate': isActive
              })
            })]
          }, index);
        })
      }), currentKey !== null && overlay && jsxRuntimeExports.jsx(components.View, {
        className: "ct-dropdown-menu__overlay",
        style: {
          top: `${menuTop}px`
        },
        onClick: handleOverlayClick
      }), jsxRuntimeExports.jsx(components.View, {
        className: classNames('ct-dropdown-menu__content', {
          'ct-dropdown-menu__content--up': direction === 'up'
        }),
        style: direction === 'down' ? {
          top: `${menuTop}px`
        } : undefined,
        children: renderChildren()
      })]
    })
  });
};
const DropdownItem = ({
  value,
  defaultValue,
  options,
  onChange,
  className,
  __index
}) => {
  /**
   * Hooks
   */
  const context = require$$0.useContext(DropdownContext);
  const [currentValue, setCurrentValue] = useControllable({
    value,
    defaultValue: defaultValue ?? options[0]?.value,
    onChange
  });
  /**
   * Early Returns
   */
  if (!context) return null;
  /**
   * Derived Values
   */
  const {
    activeKey,
    activeColor,
    itemValues,
    onClose,
    onItemValueChange
  } = context;
  const isOpen = activeKey === __index;
  /**
   * Callbacks
   */
  const handleSelect = option => {
    if (option.disabled) return;
    setCurrentValue(option.value);
    if (__index !== undefined) {
      onItemValueChange(__index, option.value);
    }
    onClose();
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (__index === undefined || value !== undefined) return;
    if (currentValue !== undefined && itemValues[__index] !== currentValue) {
      onItemValueChange(__index, currentValue);
    }
  }, [__index, currentValue, itemValues, onItemValueChange, value]);
  if (!isOpen) return null;
  return jsxRuntimeExports.jsx(components.ScrollView, {
    className: classNames('ct-dropdown-item', className),
    scrollY: true,
    children: options.map(option => {
      const isSelected = option.value === currentValue;
      return jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-dropdown-item__option', {
          'ct-dropdown-item__option--selected': isSelected,
          'ct-dropdown-item__option--disabled': option.disabled
        }),
        onClick: () => handleSelect(option),
        children: [option.icon && jsxRuntimeExports.jsx(components.View, {
          className: "ct-dropdown-item__icon",
          children: option.icon
        }), jsxRuntimeExports.jsx(components.Text, {
          className: "ct-dropdown-item__text",
          style: isSelected ? {
            color: activeColor
          } : undefined,
          children: option.text
        }), isSelected && jsxRuntimeExports.jsx(Icon, {
          name: "check",
          size: 32,
          color: activeColor,
          className: "ct-dropdown-item__check"
        })]
      }, option.value);
    })
  });
};
const Dropdown = {
  Menu: DropdownMenu,
  Item: DropdownItem
};

var css_248z$C = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-empty {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  padding: 64rpx;\n}\n.ct-empty__image {\n  margin-bottom: 32rpx;\n}\n.ct-empty__placeholder {\n  margin-bottom: 32rpx;\n  border-radius: 24rpx;\n  background: var(--ct-color-fill-tertiary);\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-empty__placeholder-text {\n  font-size: 26rpx;\n  color: var(--ct-color-text-quaternary);\n}\n.ct-empty__description {\n  font-size: 28rpx;\n  color: var(--ct-color-text-tertiary);\n  text-align: center;\n}\n.ct-empty__actions {\n  margin-top: 40rpx;\n}";
styleInject(css_248z$C);

/**
 * Constants
 */
const PLACEHOLDER_TEXT = {
  default: '暂无',
  error: '出错',
  network: '网络',
  search: '搜索',
  cart: '购物车',
  message: '消息',
  favorite: '收藏',
  order: '订单',
  comment: '评论'
};
const Empty = ({
  image,
  imageType = 'default',
  imageSize = 256,
  description = '暂无数据',
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const placeholderText = PLACEHOLDER_TEXT[imageType];
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-empty', className),
    children: [image ? jsxRuntimeExports.jsx(components.Image, {
      className: "ct-empty__image",
      src: image,
      mode: "aspectFit",
      style: {
        width: `${imageSize}rpx`,
        height: `${imageSize}rpx`
      }
    }) : jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-empty__placeholder', `ct-empty__placeholder--${imageType}`),
      style: {
        width: `${imageSize}rpx`,
        height: `${imageSize}rpx`
      },
      children: jsxRuntimeExports.jsx(components.Text, {
        className: "ct-empty__placeholder-text",
        children: placeholderText
      })
    }), description && jsxRuntimeExports.jsx(components.View, {
      className: "ct-empty__description",
      children: typeof description === 'string' ? jsxRuntimeExports.jsx(components.Text, {
        children: description
      }) : description
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-empty__actions",
      children: children
    })]
  });
};

var css_248z$B = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-form--horizontal .ct-form-item {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: start;\n          align-items: flex-start;\n}\n.ct-form--vertical .ct-form-item__label {\n  margin-bottom: 16rpx;\n}\n\n.ct-form-item {\n  margin-bottom: 32rpx;\n}\n.ct-form-item__label {\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-form-item__required {\n  color: var(--ct-color-error);\n  margin-right: 8rpx;\n}\n.ct-form-item__content {\n  -webkit-box-flex: 1;\n          flex: 1;\n}\n.ct-form-item__error {\n  margin-top: 16rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-error);\n}";
styleInject(css_248z$B);

/**
 * Context
 */
const FormContext = /*#__PURE__*/require$$0.createContext(null);
const Form = ({
  values,
  layout = 'horizontal',
  labelWidth,
  initialValues,
  onValuesChange,
  onSubmit,
  onFinishFailed,
  children,
  className
}) => {
  /**
   * States
   */
  const [innerValues, setInnerValues] = require$$0.useState(initialValues ?? {});
  const [errors, setErrors] = require$$0.useState({});
  const rulesRef = require$$0.useRef({});
  /**
   * Derived Values
   */
  const isControlled = values !== undefined;
  const currentValues = require$$0.useMemo(() => isControlled ? values ?? {} : innerValues, [innerValues, isControlled, values]);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (!isControlled && initialValues) {
      setInnerValues(initialValues);
    }
  }, [initialValues, isControlled]);
  /**
   * Callbacks
   */
  const registerField = (name, rules) => {
    rulesRef.current[name] = rules ?? [];
  };
  const unregisterField = name => {
    delete rulesRef.current[name];
  };
  const setFieldValue = (name, value) => {
    const nextValues = {
      ...currentValues,
      [name]: value
    };
    if (!isControlled) {
      setInnerValues(nextValues);
    }
    setErrors(prev => ({
      ...prev,
      [name]: undefined
    }));
    onValuesChange?.({
      [name]: value
    }, nextValues);
  };
  const validateField = async (name, value, rules) => {
    for (const rule of rules) {
      if (rule.required) {
        const empty = value === undefined || value === null || value === '' || Array.isArray(value) && value.length === 0;
        if (empty) {
          return rule.message ?? `${name}不能为空`;
        }
      }
      if (rule.pattern && value !== undefined && value !== null && value !== '') {
        if (!rule.pattern.test(String(value))) {
          return rule.message ?? `${name}格式不正确`;
        }
      }
      if (rule.min !== undefined) {
        const length = typeof value === 'number' ? value : String(value ?? '').length;
        if (length < rule.min) {
          return rule.message ?? `${name}不能少于${rule.min}`;
        }
      }
      if (rule.max !== undefined) {
        const length = typeof value === 'number' ? value : String(value ?? '').length;
        if (length > rule.max) {
          return rule.message ?? `${name}不能超过${rule.max}`;
        }
      }
      if (rule.validator) {
        const result = await rule.validator(value);
        if (!result) {
          return rule.message ?? `${name}校验失败`;
        }
      }
    }
    return undefined;
  };
  const validateAll = async () => {
    const entries = Object.entries(rulesRef.current);
    const errorList = [];
    for (const [name, rules] of entries) {
      const message = await validateField(name, currentValues[name], rules);
      if (message) {
        errorList.push({
          name,
          message
        });
      }
    }
    if (errorList.length) {
      const nextErrors = errorList.reduce((acc, item) => {
        acc[item.name] = item.message;
        return acc;
      }, {});
      setErrors(nextErrors);
      onFinishFailed?.(errorList);
      return false;
    }
    setErrors({});
    return true;
  };
  const handleSubmit = async () => {
    const valid = await validateAll();
    if (valid) {
      onSubmit?.(currentValues);
    }
  };
  return jsxRuntimeExports.jsx(FormContext.Provider, {
    value: {
      layout,
      labelWidth,
      values: currentValues,
      errors,
      setFieldValue,
      registerField,
      unregisterField
    },
    children: jsxRuntimeExports.jsx(components.Form, {
      className: classNames('ct-form', `ct-form--${layout}`, className),
      onSubmit: handleSubmit,
      children: children
    })
  });
};
const FormItem = ({
  label,
  required,
  name,
  rules,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const context = require$$0.useContext(FormContext);
  /**
   * Derived Values
   */
  const mergedRules = require$$0.useMemo(() => {
    const nextRules = rules ? [...rules] : [];
    if (required && !nextRules.some(rule => rule.required)) {
      nextRules.unshift({
        required: true
      });
    }
    return nextRules;
  }, [required, rules]);
  const fieldValue = context?.values?.[name];
  const fieldError = context?.errors?.[name];
  const labelStyle = context?.labelWidth !== undefined ? {
    width: typeof context.labelWidth === 'number' ? `${context.labelWidth}px` : context.labelWidth
  } : undefined;
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (!context) return undefined;
    context.registerField(name, mergedRules);
    return () => context.unregisterField(name);
  }, [context, mergedRules, name]);
  /**
   * Render Helpers
   */
  const renderChildren = () => {
    if (!context || ! /*#__PURE__*/require$$0.isValidElement(children)) return children;
    const valuePropName = children.props.checked !== undefined || children.props.defaultChecked !== undefined || typeof fieldValue === 'boolean' ? 'checked' : 'value';
    const handleChange = (...args) => {
      const nextValue = args[0]?.detail?.value ?? args[0];
      context.setFieldValue(name, nextValue);
      children.props.onChange?.(...args);
    };
    return /*#__PURE__*/require$$0.cloneElement(children, {
      [valuePropName]: fieldValue,
      onChange: handleChange
    });
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-form-item', className),
    children: [label && jsxRuntimeExports.jsxs(components.View, {
      className: "ct-form-item__label",
      style: labelStyle,
      children: [required && jsxRuntimeExports.jsx(components.Text, {
        className: "ct-form-item__required",
        children: "*"
      }), jsxRuntimeExports.jsx(components.Text, {
        children: label
      })]
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-form-item__content",
      children: renderChildren()
    }), fieldError && jsxRuntimeExports.jsx(components.View, {
      className: "ct-form-item__error",
      children: jsxRuntimeExports.jsx(components.Text, {
        children: fieldError
      })
    })]
  });
};
Form.Item = FormItem;

var css_248z$A = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-grid {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  flex-wrap: wrap;\n}\n.ct-grid--bordered {\n  position: relative;\n}\n.ct-grid--bordered::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  top: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: top;\n          transform-origin: top;\n}\n.ct-grid--bordered {\n  position: relative;\n}\n.ct-grid--bordered::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleX(0.5);\n          transform: scaleX(0.5);\n  -webkit-transform-origin: left;\n          transform-origin: left;\n}\n.ct-grid--bordered .ct-grid__item {\n  position: relative;\n}\n.ct-grid--bordered .ct-grid__item::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-grid--bordered .ct-grid__item {\n  position: relative;\n}\n.ct-grid--bordered .ct-grid__item::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  top: 0;\n  bottom: 0;\n  right: 0;\n  width: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleX(0.5);\n          transform: scaleX(0.5);\n  -webkit-transform-origin: right;\n          transform-origin: right;\n}\n.ct-grid__item:active, .ct-grid__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-grid__item--center .ct-grid__item-content {\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-grid__item-content {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  padding: 32rpx;\n}\n.ct-grid--square .ct-grid__item-content {\n  padding-bottom: 100%;\n  position: relative;\n}\n.ct-grid__icon {\n  position: relative;\n  margin-bottom: 16rpx;\n}\n.ct-grid__badge {\n  position: absolute;\n  top: -8rpx;\n  right: -16rpx;\n}\n.ct-grid__text {\n  font-size: 26rpx;\n  color: var(--ct-color-text-primary);\n  text-align: center;\n}";
styleInject(css_248z$A);

/**
 * Context
 */
const GridContext = /*#__PURE__*/require$$0.createContext(null);
/**
 * Sub Components
 */
const GridItem = ({
  icon,
  text,
  badge,
  dot,
  onClick,
  children,
  className,
  iconSize
}) => {
  /**
   * Hooks
   */
  const context = require$$0.useContext(GridContext);
  /**
   * Derived Values
   */
  const columns = context?.columns ?? 1;
  const gutter = context?.gutter ?? 0;
  const center = context?.center ?? true;
  const itemWidth = `${100 / columns}%`;
  const resolvedIconSize = iconSize ?? context?.iconSize ?? 56;
  const isClickable = !!onClick;
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-grid__item', {
      'ct-grid__item--center': center
    }, className),
    style: {
      width: itemWidth,
      padding: gutter ? `${gutter / 2}rpx` : undefined
    },
    hoverClass: isClickable ? 'ct-clickable--hover' : 'none',
    onClick: onClick,
    children: jsxRuntimeExports.jsxs(components.View, {
      className: "ct-grid__item-content",
      children: [children || icon && jsxRuntimeExports.jsxs(components.View, {
        className: "ct-grid__icon",
        style: {
          fontSize: `${resolvedIconSize}rpx`
        },
        children: [icon, (badge !== undefined || dot) && jsxRuntimeExports.jsx(Badge, {
          content: badge,
          dot: dot,
          className: "ct-grid__badge"
        })]
      }), !children && text && jsxRuntimeExports.jsx(components.Text, {
        className: "ct-grid__text",
        children: text
      })]
    })
  });
};
/**
 * Main Component
 */
const Grid = ({
  columns = 4,
  items = [],
  bordered = true,
  square = false,
  iconSize = 56,
  gutter = 0,
  center = true,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const wrapperStyle = gutter ? {
    padding: `${gutter / 2}rpx`
  } : undefined;
  const hasChildren = require$$0.Children.count(children) > 0;
  return jsxRuntimeExports.jsx(GridContext.Provider, {
    value: {
      columns,
      gutter,
      center,
      iconSize
    },
    children: jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-grid', {
        'ct-grid--bordered': bordered,
        'ct-grid--square': square
      }, className),
      style: wrapperStyle,
      children: hasChildren ? children : items.map((item, index) => jsxRuntimeExports.jsx(GridItem, {
        icon: item.icon,
        text: item.text,
        badge: item.badge,
        dot: item.dot,
        onClick: item.onClick
      }, index))
    })
  });
};
Grid.Item = GridItem;

var css_248z$z = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-image-preview {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 500;\n}\n.ct-image-preview__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: #000;\n}\n.ct-image-preview__close {\n  position: absolute;\n  top: 80rpx;\n  right: 32rpx;\n  z-index: 2;\n  padding: 16rpx;\n}\n.ct-image-preview__swiper {\n  width: 100%;\n  height: 100%;\n}\n.ct-image-preview__item {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-image-preview__image {\n  width: 100%;\n  height: 100%;\n}\n.ct-image-preview__indicator {\n  position: absolute;\n  bottom: 80rpx;\n  left: 50%;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n  font-size: 28rpx;\n  color: #fff;\n}";
styleInject(css_248z$z);

/**
 * Main Component
 */
const ImagePreview = ({
  images,
  open,
  current,
  defaultCurrent = 0,
  showIndicator = true,
  closable = true,
  onChange,
  onClose,
  className
}) => {
  /**
   * Hooks
   */
  const [currentIndex, setCurrentIndex] = useControllable({
    value: current,
    defaultValue: defaultCurrent,
    onChange
  });
  /**
   * Early Returns
   */
  if (!open) return null;
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-image-preview', className),
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-image-preview__mask",
      onClick: onClose
    }), closable && jsxRuntimeExports.jsx(components.View, {
      className: "ct-image-preview__close",
      onClick: onClose,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "close",
        size: 48,
        color: "#fff"
      })
    }), jsxRuntimeExports.jsx(components.Swiper, {
      className: "ct-image-preview__swiper",
      current: currentIndex,
      onChange: event => setCurrentIndex(event.detail.current),
      children: images.map((src, index) => jsxRuntimeExports.jsx(components.SwiperItem, {
        className: "ct-image-preview__item",
        children: jsxRuntimeExports.jsx(components.Image, {
          src: src,
          mode: "aspectFit",
          className: "ct-image-preview__image"
        })
      }, index))
    }), showIndicator && jsxRuntimeExports.jsx(components.View, {
      className: "ct-image-preview__indicator",
      children: jsxRuntimeExports.jsxs(components.Text, {
        children: [currentIndex + 1, " / ", images.length]
      })
    })]
  });
};

var css_248z$y = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-index-list {\n  position: relative;\n  height: 100%;\n}\n.ct-index-list__content {\n  height: 100%;\n}\n.ct-index-list__anchor {\n  padding: 16rpx 32rpx;\n  background: var(--ct-color-fill-secondary);\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-index-list__anchor--sticky {\n  position: -webkit-sticky;\n  position: sticky;\n  z-index: 1;\n}\n.ct-index-list__item {\n  padding: 24rpx 32rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  position: relative;\n}\n.ct-index-list__item::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-index-list__item:active, .ct-index-list__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-index-list__sidebar {\n  position: absolute;\n  top: 50%;\n  right: 16rpx;\n  -webkit-transform: translateY(-50%);\n          transform: translateY(-50%);\n}\n.ct-index-list__index {\n  padding: 4rpx 8rpx;\n  font-size: 22rpx;\n  color: var(--ct-color-text-tertiary);\n  text-align: center;\n}\n.ct-index-list__index:active, .ct-index-list__index.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-index-list__index--active {\n  color: var(--ct-color-primary);\n}";
styleInject(css_248z$y);

const IndexList = ({
  data,
  current,
  sticky = true,
  stickyOffset = 0,
  onIndexChange,
  onItemClick,
  className
}) => {
  /**
   * States
   */
  const [activeAnchor, setActiveAnchor] = require$$0.useState(current || data[0]?.anchor);
  const [scrollIntoView, setScrollIntoView] = require$$0.useState();
  /**
   * Callbacks
   */
  const handleAnchorClick = anchor => {
    setActiveAnchor(anchor);
    setScrollIntoView(`ct-index-list-anchor-${anchor}`);
    onIndexChange?.(anchor);
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (current !== undefined) {
      setActiveAnchor(current);
      setScrollIntoView(`ct-index-list-anchor-${current}`);
    }
  }, [current]);
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-index-list', className),
    children: [jsxRuntimeExports.jsx(components.ScrollView, {
      className: "ct-index-list__content",
      scrollY: true,
      scrollIntoView: scrollIntoView,
      children: data.map(group => jsxRuntimeExports.jsxs(components.View, {
        className: "ct-index-list__group",
        id: `ct-index-list-anchor-${group.anchor}`,
        children: [jsxRuntimeExports.jsx(components.View, {
          className: classNames('ct-index-list__anchor', {
            'ct-index-list__anchor--sticky': sticky
          }),
          style: sticky ? {
            top: `${stickyOffset}px`
          } : undefined,
          children: jsxRuntimeExports.jsx(components.Text, {
            children: group.anchor
          })
        }), group.items.map((item, index) => jsxRuntimeExports.jsx(components.View, {
          className: "ct-index-list__item",
          hoverClass: "ct-clickable--hover",
          onClick: () => onItemClick?.(item, group.anchor),
          children: jsxRuntimeExports.jsx(components.Text, {
            children: item.text
          })
        }, index))]
      }, group.anchor))
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-index-list__sidebar",
      children: data.map(group => jsxRuntimeExports.jsx(components.View, {
        className: classNames('ct-index-list__index', {
          'ct-index-list__index--active': activeAnchor === group.anchor
        }),
        hoverClass: "ct-clickable--hover",
        onClick: () => handleAnchorClick(group.anchor),
        children: jsxRuntimeExports.jsx(components.Text, {
          children: group.anchor
        })
      }, group.anchor))
    })]
  });
};

var css_248z$x = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-loading {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-loading--horizontal {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: normal;\n          flex-direction: row;\n}\n.ct-loading--horizontal .ct-loading__text {\n  margin-left: 16rpx;\n}\n.ct-loading--vertical {\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n}\n.ct-loading--vertical .ct-loading__text {\n  margin-top: 16rpx;\n}\n.ct-loading__spinner {\n  border: 4rpx solid var(--ct-color-border);\n  border-top-color: var(--ct-color-primary);\n  border-radius: 50%;\n  -webkit-animation: ct-loading-spin 0.8s linear infinite;\n          animation: ct-loading-spin 0.8s linear infinite;\n}\n.ct-loading__spinner--circular {\n  border-width: 6rpx;\n}\n.ct-loading__dots {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  gap: 8rpx;\n}\n.ct-loading__dot {\n  width: 16rpx;\n  height: 16rpx;\n  background: var(--ct-color-primary);\n  border-radius: 50%;\n  -webkit-animation: ct-loading-bounce 1s ease-in-out infinite;\n          animation: ct-loading-bounce 1s ease-in-out infinite;\n}\n.ct-loading__text {\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n}\n\n@-webkit-keyframes ct-loading-spin {\n  from {\n    -webkit-transform: rotate(0deg);\n            transform: rotate(0deg);\n  }\n  to {\n    -webkit-transform: rotate(360deg);\n            transform: rotate(360deg);\n  }\n}\n\n@keyframes ct-loading-spin {\n  from {\n    -webkit-transform: rotate(0deg);\n            transform: rotate(0deg);\n  }\n  to {\n    -webkit-transform: rotate(360deg);\n            transform: rotate(360deg);\n  }\n}\n@-webkit-keyframes ct-loading-bounce {\n  0%, 80%, 100% {\n    -webkit-transform: scale(0);\n            transform: scale(0);\n    opacity: 0.5;\n  }\n  40% {\n    -webkit-transform: scale(1);\n            transform: scale(1);\n    opacity: 1;\n  }\n}\n@keyframes ct-loading-bounce {\n  0%, 80%, 100% {\n    -webkit-transform: scale(0);\n            transform: scale(0);\n    opacity: 0.5;\n  }\n  40% {\n    -webkit-transform: scale(1);\n            transform: scale(1);\n    opacity: 1;\n  }\n}";
styleInject(css_248z$x);

/**
 * Constants
 */
const SIZE_MAP = {
  sm: 32,
  md: 48,
  lg: 64
};
const Loading = ({
  type = 'spinner',
  size = 'md',
  color,
  text,
  direction = 'horizontal',
  className
}) => {
  /**
   * Derived Values
   */
  const iconSize = SIZE_MAP[size];
  const showSpinner = type === 'spinner' || type === 'circular';
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-loading', `ct-loading--${direction}`, className),
    children: [showSpinner && jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-loading__spinner', {
        'ct-loading__spinner--circular': type === 'circular'
      }),
      style: {
        width: `${iconSize}rpx`,
        height: `${iconSize}rpx`,
        borderTopColor: color
      }
    }), type === 'dots' && jsxRuntimeExports.jsx(components.View, {
      className: "ct-loading__dots",
      children: [0, 1, 2].map(i => jsxRuntimeExports.jsx(components.View, {
        className: "ct-loading__dot",
        style: {
          backgroundColor: color,
          animationDelay: `${i * 0.15}s`
        }
      }, i))
    }), text && jsxRuntimeExports.jsx(components.Text, {
      className: "ct-loading__text",
      children: text
    })]
  });
};

var css_248z$w = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-infinite-scroll {\n  height: 100%;\n}\n.ct-infinite-scroll__footer {\n  padding: 32rpx;\n}\n.ct-infinite-scroll__loading, .ct-infinite-scroll__finished {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-infinite-scroll__text {\n  margin-left: 16rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-infinite-scroll__finished .ct-infinite-scroll__text {\n  margin-left: 0;\n}";
styleInject(css_248z$w);

/**
 * Main Component
 */
const InfiniteScroll = ({
  hasMore,
  loading = false,
  threshold = 100,
  scrollable = true,
  onLoadMore,
  loadingText = '加载中...',
  finishedText = '没有更多了',
  loadingContent,
  finishedContent,
  children,
  className
}) => {
  /**
   * States
   */
  const [isLoading, setIsLoading] = require$$0.useState(false);
  /**
   * Callbacks
   */
  const handleScrollToLower = async () => {
    if (loading || isLoading || !hasMore) return;
    setIsLoading(true);
    try {
      await onLoadMore();
    } finally {
      setIsLoading(false);
    }
  };
  /**
   * Render Helpers
   */
  const content = jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
    children: [children, jsxRuntimeExports.jsxs(components.View, {
      className: "ct-infinite-scroll__footer",
      children: [(loading || isLoading) && hasMore && (loadingContent || jsxRuntimeExports.jsxs(components.View, {
        className: "ct-infinite-scroll__loading",
        children: [jsxRuntimeExports.jsx(Loading, {
          type: "spinner",
          size: "sm"
        }), jsxRuntimeExports.jsx(components.Text, {
          className: "ct-infinite-scroll__text",
          children: loadingText
        })]
      })), !hasMore && (finishedContent || jsxRuntimeExports.jsx(components.View, {
        className: "ct-infinite-scroll__finished",
        children: jsxRuntimeExports.jsx(components.Text, {
          className: "ct-infinite-scroll__text",
          children: finishedText
        })
      }))]
    })]
  });
  /**
   * Early Returns
   */
  if (!scrollable) {
    return jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-infinite-scroll', className),
      children: content
    });
  }
  return jsxRuntimeExports.jsx(components.ScrollView, {
    className: classNames('ct-infinite-scroll', className),
    scrollY: true,
    lowerThreshold: threshold,
    onScrollToLower: handleScrollToLower,
    children: content
  });
};

var css_248z$v = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-input {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 0 32rpx;\n  height: 80rpx;\n  background: var(--ct-color-bg-card);\n  border-radius: 16rpx;\n  border: 2rpx solid var(--ct-color-border);\n  -webkit-transition: border-color 150ms ease;\n  transition: border-color 150ms ease;\n  margin-bottom: 24rpx;\n  box-sizing: border-box;\n}\n.ct-input--focused {\n  border-color: var(--ct-color-primary);\n}\n.ct-input--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n  background: var(--ct-color-bg-disabled);\n}\n.ct-input__content {\n  -webkit-box-flex: 1;\n          flex: 1;\n  min-width: 0;\n  height: 100%;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  overflow: hidden;\n}\n.ct-input__inner {\n  width: 100%;\n  height: 100%;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  background: transparent;\n}\n.ct-input__inner--left {\n  text-align: left;\n}\n.ct-input__inner--center {\n  text-align: center;\n}\n.ct-input__inner--right {\n  text-align: right;\n}\n.ct-input__prefix, .ct-input__suffix {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  flex-shrink: 0;\n  color: var(--ct-color-text-tertiary);\n  font-size: 28rpx;\n}\n.ct-input__prefix {\n  margin-right: 16rpx;\n}\n.ct-input__suffix {\n  margin-left: 16rpx;\n}\n.ct-input__clear {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  flex-shrink: 0;\n  width: 40rpx;\n  height: 40rpx;\n  margin-left: 16rpx;\n  color: var(--ct-color-text-quaternary);\n}\n.ct-input__clear:active, .ct-input__clear.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-input:last-child {\n  margin-bottom: 0;\n}";
styleInject(css_248z$v);

const Input = ({
  type = 'text',
  value: propValue,
  defaultValue = '',
  placeholder,
  disabled = false,
  readOnly = false,
  maxLength,
  clearable = false,
  prefix,
  suffix,
  align = 'left',
  onChange,
  onFocus,
  onBlur,
  onClear,
  className
}) => {
  /**
   * States
   */
  const isControlled = propValue !== undefined;
  const [innerValue, setInnerValue] = require$$0.useState(defaultValue);
  const value = isControlled ? propValue : innerValue;
  const [focused, setFocused] = require$$0.useState(false);
  /**
   * Derived Values
   */
  const showClear = clearable && value && !disabled && !readOnly;
  /**
   * Callbacks
   */
  const handleInput = e => {
    if (disabled || readOnly) return;
    const newValue = e.detail.value;
    if (!isControlled) {
      setInnerValue(newValue);
    }
    onChange?.(newValue);
  };
  const handleFocus = e => {
    setFocused(true);
    onFocus?.(e);
  };
  const handleBlur = e => {
    setFocused(false);
    onBlur?.(e);
  };
  const handleClear = () => {
    if (!isControlled) {
      setInnerValue('');
    }
    onChange?.('');
    onClear?.();
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-input', {
      'ct-input--disabled': disabled,
      'ct-input--focused': focused
    }, className),
    children: [prefix && jsxRuntimeExports.jsx(components.View, {
      className: "ct-input__prefix",
      children: prefix
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-input__content",
      children: jsxRuntimeExports.jsx(components.Input, {
        className: classNames('ct-input__inner', `ct-input__inner--${align}`),
        type: type,
        value: value,
        placeholder: placeholder,
        disabled: disabled,
        maxlength: maxLength,
        onInput: handleInput,
        onFocus: handleFocus,
        onBlur: handleBlur,
        placeholderStyle: "color: #cbd5e1"
      })
    }), showClear && jsxRuntimeExports.jsx(components.View, {
      className: "ct-input__clear",
      onClick: handleClear,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "close-circle",
        size: 32,
        color: "#94a3b8"
      })
    }), suffix && jsxRuntimeExports.jsx(components.View, {
      className: "ct-input__suffix",
      children: suffix
    })]
  });
};

var css_248z$u = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-list {\n  background: var(--ct-color-bg-card);\n}\n.ct-list--bordered .ct-list__body {\n  position: relative;\n}\n.ct-list--bordered .ct-list__body::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  top: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: top;\n          transform-origin: top;\n}\n.ct-list--bordered .ct-list__body {\n  position: relative;\n}\n.ct-list--bordered .ct-list__body::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-list__header, .ct-list__footer {\n  padding: 24rpx 32rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n\n.ct-list-item {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n  background: var(--ct-color-bg-card);\n}\n.ct-list-item:not(:last-child) {\n  position: relative;\n}\n.ct-list-item:not(:last-child)::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-list-item--clickable:active, .ct-list-item--clickable.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-list-item--disabled {\n  opacity: 0.4;\n  pointer-events: none;\n}\n.ct-list-item__prefix {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  margin-right: 24rpx;\n}\n.ct-list-item__content {\n  -webkit-box-flex: 1;\n          flex: 1;\n  min-width: 0;\n}\n.ct-list-item__title {\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n}\n.ct-list-item__description {\n  margin-top: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-list-item__extra {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  margin-left: 24rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-list-item__arrow {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  margin-left: 16rpx;\n}";
styleInject(css_248z$u);

/**
 * Sub Components
 */
const ListItem = ({
  title,
  description,
  prefix,
  extra,
  arrow = false,
  disabled = false,
  clickable = false,
  onClick,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const isClickable = clickable || arrow || !!onClick;
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-list-item', {
      'ct-list-item--disabled': disabled,
      'ct-list-item--clickable': isClickable
    }, className),
    hoverClass: isClickable && !disabled ? 'ct-clickable--hover' : 'none',
    onClick: disabled ? undefined : onClick,
    children: [prefix && jsxRuntimeExports.jsx(components.View, {
      className: "ct-list-item__prefix",
      children: prefix
    }), jsxRuntimeExports.jsxs(components.View, {
      className: "ct-list-item__content",
      children: [title && jsxRuntimeExports.jsx(components.View, {
        className: "ct-list-item__title",
        children: typeof title === 'string' ? jsxRuntimeExports.jsx(components.Text, {
          children: title
        }) : title
      }), description && jsxRuntimeExports.jsx(components.View, {
        className: "ct-list-item__description",
        children: typeof description === 'string' ? jsxRuntimeExports.jsx(components.Text, {
          children: description
        }) : description
      }), children]
    }), extra && jsxRuntimeExports.jsx(components.View, {
      className: "ct-list-item__extra",
      children: extra
    }), arrow && jsxRuntimeExports.jsx(components.View, {
      className: "ct-list-item__arrow",
      children: jsxRuntimeExports.jsx(Icon, {
        name: "arrow-right",
        size: 32,
        color: "var(--ct-color-text-tertiary)"
      })
    })]
  });
};
/**
 * Main Component
 */
const List = ({
  header,
  footer,
  bordered = true,
  children,
  className
}) => {
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-list', {
      'ct-list--bordered': bordered
    }, className),
    children: [header && jsxRuntimeExports.jsx(components.View, {
      className: "ct-list__header",
      children: header
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-list__body",
      children: children
    }), footer && jsxRuntimeExports.jsx(components.View, {
      className: "ct-list__footer",
      children: footer
    })]
  });
};
List.Item = ListItem;

var css_248z$t = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-modal {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 500;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-modal__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: var(--ct-color-bg-mask);\n  opacity: 0;\n  -webkit-transition: opacity 250ms;\n  transition: opacity 250ms;\n}\n.ct-modal__mask--visible {\n  opacity: 1;\n}\n.ct-modal__container {\n  position: relative;\n  width: 560rpx;\n  background: var(--ct-color-bg);\n  border-radius: 32rpx;\n  overflow: hidden;\n  -webkit-transform: scale(0.9);\n          transform: scale(0.9);\n  opacity: 0;\n  -webkit-transition: all 250ms ease-out;\n  transition: all 250ms ease-out;\n}\n.ct-modal__container--visible {\n  -webkit-transform: scale(1);\n          transform: scale(1);\n  opacity: 1;\n}\n.ct-modal__header {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 40rpx 40rpx 24rpx;\n}\n.ct-modal__title {\n  -webkit-box-flex: 1;\n          flex: 1;\n  font-size: 36rpx;\n  font-weight: 600;\n  color: var(--ct-color-text-primary);\n  text-align: center;\n}\n.ct-modal__close {\n  position: absolute;\n  right: 32rpx;\n  top: 32rpx;\n  padding: 8rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-modal__body {\n  padding: 32rpx 40rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n  text-align: center;\n  line-height: 1.6;\n}\n.ct-modal__footer {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  padding: 32rpx 40rpx 40rpx;\n  gap: 24rpx;\n}\n.ct-modal__footer .ct-button {\n  -webkit-box-flex: 1;\n          flex: 1;\n}";
styleInject(css_248z$t);

/**
 * Context
 */
const ModalContext = /*#__PURE__*/require$$0.createContext({});
/**
 * Sub Components
 */
const ModalContent = ({
  children,
  className
}) => jsxRuntimeExports.jsx(components.View, {
  className: classNames('ct-modal__content', className),
  children: children
});
const ModalHeader = ({
  children,
  closable = true,
  onClose
}) => {
  const ctx = require$$0.useContext(ModalContext);
  const handleClose = onClose || ctx.onClose;
  return jsxRuntimeExports.jsxs(components.View, {
    className: "ct-modal__header",
    children: [jsxRuntimeExports.jsx(components.Text, {
      className: "ct-modal__title",
      children: children
    }), closable && jsxRuntimeExports.jsx(components.View, {
      className: "ct-modal__close",
      onClick: handleClose,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "close",
        size: 40
      })
    })]
  });
};
const ModalBody = ({
  children,
  className
}) => jsxRuntimeExports.jsx(components.View, {
  className: classNames('ct-modal__body', className),
  children: children
});
const ModalFooter = ({
  children,
  className
}) => jsxRuntimeExports.jsx(components.View, {
  className: classNames('ct-modal__footer', className),
  children: children
});
/**
 * Main Component
 */
const Modal = ({
  open: openProp,
  defaultOpen = false,
  onOpenChange,
  title,
  onClose,
  onConfirm,
  confirmText = '确认',
  cancelText = '取消',
  closable = true,
  closeOnOverlayClick = true,
  showFooter = true,
  footer,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const [isOpen, setIsOpen] = useControllable({
    value: openProp,
    defaultValue: defaultOpen,
    onChange: onOpenChange
  });
  /**
   * States
   */
  const [visible, setVisible] = require$$0.useState(false);
  const [animating, setAnimating] = require$$0.useState(false);
  /**
   * Derived Values
   */
  const childArray = require$$0.Children.toArray(children);
  const hasHeaderSlot = childArray.some(child => /*#__PURE__*/require$$0.isValidElement(child) && child.type === ModalHeader);
  const hasBodySlot = childArray.some(child => /*#__PURE__*/require$$0.isValidElement(child) && child.type === ModalBody);
  const hasFooterSlot = childArray.some(child => /*#__PURE__*/require$$0.isValidElement(child) && child.type === ModalFooter);
  const hasContentSlot = childArray.some(child => /*#__PURE__*/require$$0.isValidElement(child) && child.type === ModalContent);
  const isCompound = hasHeaderSlot || hasBodySlot || hasFooterSlot || hasContentSlot;
  /**
   * Callbacks
   */
  const handleClose = () => {
    setIsOpen(false);
    onClose?.();
  };
  const handleOverlayClick = () => {
    if (closeOnOverlayClick) {
      handleClose();
    }
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (isOpen) {
      setVisible(true);
      Taro.nextTick(() => setAnimating(true));
    } else {
      setAnimating(false);
      const timer = setTimeout(() => setVisible(false), OVERLAY_ANIMATION_DURATION);
      return () => clearTimeout(timer);
    }
    return undefined;
  }, [isOpen]);
  /**
   * Early Returns
   */
  if (!visible) return null;
  return jsxRuntimeExports.jsx(ModalContext.Provider, {
    value: {
      onClose: handleClose
    },
    children: jsxRuntimeExports.jsxs(components.View, {
      className: "ct-modal",
      catchMove: true,
      children: [jsxRuntimeExports.jsx(components.View, {
        className: classNames('ct-modal__mask', {
          'ct-modal__mask--visible': animating
        }),
        onClick: handleOverlayClick
      }), jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-modal__container', {
          'ct-modal__container--visible': animating
        }, className),
        children: [title && !hasHeaderSlot && jsxRuntimeExports.jsxs(components.View, {
          className: "ct-modal__header",
          children: [jsxRuntimeExports.jsx(components.Text, {
            className: "ct-modal__title",
            children: title
          }), closable && jsxRuntimeExports.jsx(components.View, {
            className: "ct-modal__close",
            onClick: handleClose,
            children: jsxRuntimeExports.jsx(Icon, {
              name: "close",
              size: 40
            })
          })]
        }), isCompound ? children : jsxRuntimeExports.jsx(components.View, {
          className: "ct-modal__body",
          children: children
        }), showFooter && !footer && !hasFooterSlot && jsxRuntimeExports.jsxs(components.View, {
          className: "ct-modal__footer",
          children: [jsxRuntimeExports.jsx(Button, {
            variant: "ghost",
            onClick: handleClose,
            children: cancelText
          }), jsxRuntimeExports.jsx(Button, {
            variant: "primary",
            onClick: onConfirm,
            children: confirmText
          })]
        }), footer && !hasFooterSlot ? footer : null]
      })]
    })
  });
};
Modal.Content = ModalContent;
Modal.Header = ModalHeader;
Modal.Body = ModalBody;
Modal.Footer = ModalFooter;

var css_248z$s = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-navbar {\n  background: var(--ct-color-bg-card);\n}\n.ct-navbar--fixed {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  z-index: 300;\n}\n.ct-navbar--bordered {\n  position: relative;\n}\n.ct-navbar--bordered::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-navbar--transparent {\n  background: transparent;\n}\n.ct-navbar__content {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 0 24rpx;\n}\n.ct-navbar__left, .ct-navbar__right {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  min-width: 80rpx;\n}\n.ct-navbar__right {\n  -webkit-box-pack: end;\n          justify-content: flex-end;\n}\n.ct-navbar__back {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  width: 80rpx;\n  height: 80rpx;\n  margin-left: -16rpx;\n}\n.ct-navbar__back:active, .ct-navbar__back.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-navbar__title {\n  -webkit-box-flex: 1;\n          flex: 1;\n  text-align: center;\n  font-size: 32rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}";
styleInject(css_248z$s);

const NavBar = ({
  title,
  left,
  right,
  showBack = true,
  onBack,
  fixed = false,
  bordered = true,
  transparent = false,
  backgroundColor,
  color,
  placeholder = true,
  className
}) => {
  /**
   * States
   */
  const [statusBarHeight, setStatusBarHeight] = require$$0.useState(0);
  const [navBarHeight, setNavBarHeight] = require$$0.useState(44);
  /**
   * Callbacks
   */
  const handleBack = () => {
    if (onBack) {
      onBack();
      return;
    }
    Taro.navigateBack();
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    const systemInfo = Taro.getSystemInfoSync();
    setStatusBarHeight(systemInfo.statusBarHeight || 0);
    if (process.env.TARO_ENV === 'weapp') {
      const menuButton = Taro.getMenuButtonBoundingClientRect();
      const navHeight = (menuButton.top - (systemInfo.statusBarHeight || 0)) * 2 + menuButton.height;
      setNavBarHeight(navHeight);
    }
  }, []);
  /**
   * Derived Values
   */
  const navStyle = {
    paddingTop: `${statusBarHeight}px`
  };
  if (transparent) {
    navStyle.backgroundColor = 'transparent';
  } else if (backgroundColor) {
    navStyle.backgroundColor = backgroundColor;
  }
  if (color) {
    navStyle.color = color;
  }
  const contentStyle = {
    height: `${navBarHeight}px`
  };
  /**
   * Render Helpers
   */
  const renderNavBar = () => jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-navbar', {
      'ct-navbar--fixed': fixed,
      'ct-navbar--bordered': bordered && !transparent,
      'ct-navbar--transparent': transparent
    }, className),
    style: navStyle,
    children: jsxRuntimeExports.jsxs(components.View, {
      className: "ct-navbar__content",
      style: contentStyle,
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-navbar__left",
        children: left || showBack && jsxRuntimeExports.jsx(components.View, {
          className: "ct-navbar__back",
          onClick: handleBack,
          children: jsxRuntimeExports.jsx(Icon, {
            name: "arrow-left",
            size: 44,
            color: color || 'var(--ct-color-text-primary)'
          })
        })
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-navbar__title",
        children: typeof title === 'string' ? jsxRuntimeExports.jsx(components.Text, {
          children: title
        }) : title
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-navbar__right",
        children: right
      })]
    })
  });
  if (fixed && placeholder) {
    return jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
      children: [renderNavBar(), jsxRuntimeExports.jsx(components.View, {
        style: {
          height: `${statusBarHeight + navBarHeight}px`
        }
      })]
    });
  }
  return renderNavBar();
};

var css_248z$r = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-notice-bar {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 16rpx 32rpx;\n  background: var(--ct-color-warning-light);\n  color: var(--ct-color-warning);\n  font-size: 26rpx;\n}\n.ct-notice-bar__icon {\n  margin-right: 16rpx;\n}\n.ct-notice-bar__content {\n  -webkit-box-flex: 1;\n          flex: 1;\n  overflow: hidden;\n}\n.ct-notice-bar__text {\n  white-space: nowrap;\n}\n.ct-notice-bar__text--scrollable {\n  display: inline-block;\n}\n.ct-notice-bar__close, .ct-notice-bar__action {\n  margin-left: 16rpx;\n}\n.ct-notice-bar__close:active, .ct-notice-bar__close.ct-clickable--hover, .ct-notice-bar__action:active, .ct-notice-bar__action.ct-clickable--hover {\n  opacity: 0.7;\n}";
styleInject(css_248z$r);

/**
 * Main Component
 */
const NoticeBar = ({
  content,
  mode,
  icon,
  scrollable = false,
  speed = 60,
  delay = 1000,
  backgroundColor,
  color,
  open: propOpen,
  defaultOpen = true,
  visible: propVisible,
  onClose,
  onClick,
  className
}) => {
  /**
   * Derived Values
   */
  const mergedOpen = propOpen ?? propVisible;
  /**
   * States
   */
  const [innerOpen, setInnerOpen] = require$$0.useState(mergedOpen ?? defaultOpen);
  const [offset, setOffset] = require$$0.useState(0);
  const [contentWidth, setContentWidth] = require$$0.useState(0);
  const [wrapperWidth, setWrapperWidth] = require$$0.useState(0);
  /**
   * Refs
   */
  const wrapperId = require$$0.useRef(`notice-bar-wrapper-${Date.now()}`);
  const textId = require$$0.useRef(`notice-bar-text-${Date.now()}`);
  const timerRef = require$$0.useRef(null);
  const delayRef = require$$0.useRef(null);
  /**
   * Derived Values
   */
  const isControlled = mergedOpen !== undefined;
  const open = isControlled ? mergedOpen : innerOpen;
  /**
   * Callbacks
   */
  const handleClose = () => {
    if (!isControlled) {
      setInnerOpen(false);
    }
    onClose?.();
  };
  const clearTimers = () => {
    if (timerRef.current) {
      clearInterval(timerRef.current);
      timerRef.current = null;
    }
    if (delayRef.current) {
      clearTimeout(delayRef.current);
      delayRef.current = null;
    }
  };
  const measureWidths = () => {
    if (!scrollable || !open) return;
    Taro.nextTick(() => {
      const query = Taro.createSelectorQuery();
      query.select(`#${wrapperId.current}`).boundingClientRect();
      query.select(`#${textId.current}`).boundingClientRect();
      query.exec(res => {
        const wrapperRect = res[0];
        const textRect = res[1];
        if (!wrapperRect || !textRect) return;
        setWrapperWidth(wrapperRect.width);
        setContentWidth(textRect.width);
      });
    });
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    measureWidths();
  }, [content, icon, scrollable, open]);
  require$$0.useEffect(() => {
    if (!scrollable || !open) return undefined;
    const handleResize = () => measureWidths();
    if (Taro.onWindowResize) {
      Taro.onWindowResize(handleResize);
      return () => {
        Taro.offWindowResize?.(handleResize);
      };
    }
    if (typeof window !== 'undefined') {
      window.addEventListener('resize', handleResize);
      return () => window.removeEventListener('resize', handleResize);
    }
    return undefined;
  }, [scrollable, open]);
  require$$0.useEffect(() => {
    if (!scrollable || !open) {
      clearTimers();
      setOffset(0);
      return;
    }
    if (contentWidth <= wrapperWidth) {
      clearTimers();
      setOffset(0);
      return;
    }
    clearTimers();
    setOffset(wrapperWidth);
    delayRef.current = setTimeout(() => {
      const frameMs = 16;
      const step = speed * frameMs / 1000;
      timerRef.current = setInterval(() => {
        setOffset(prev => {
          const next = prev - step;
          if (next <= -contentWidth) {
            return wrapperWidth;
          }
          return next;
        });
      }, frameMs);
    }, delay);
    return clearTimers;
  }, [contentWidth, delay, scrollable, speed, open, wrapperWidth]);
  /**
   * Early Returns
   */
  if (!open) return null;
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-notice-bar', className),
    style: {
      backgroundColor,
      color
    },
    onClick: mode !== 'closeable' ? onClick : undefined,
    children: [icon && jsxRuntimeExports.jsx(components.View, {
      className: "ct-notice-bar__icon",
      children: icon
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-notice-bar__content",
      id: wrapperId.current,
      children: jsxRuntimeExports.jsx(components.View, {
        id: textId.current,
        className: classNames('ct-notice-bar__text', {
          'ct-notice-bar__text--scrollable': scrollable
        }),
        style: scrollable ? {
          transform: `translateX(${offset}px)`
        } : undefined,
        children: jsxRuntimeExports.jsx(components.Text, {
          children: content
        })
      })
    }), mode === 'closeable' && jsxRuntimeExports.jsx(components.View, {
      className: "ct-notice-bar__close",
      hoverClass: "ct-clickable--hover",
      onClick: handleClose,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "close",
        size: 32
      })
    }), mode === 'link' && jsxRuntimeExports.jsx(components.View, {
      className: "ct-notice-bar__action",
      hoverClass: "ct-clickable--hover",
      onClick: onClick,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "arrow-right",
        size: 32
      })
    })]
  });
};

var css_248z$q = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-pagination {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  flex-wrap: wrap;\n  gap: 24rpx;\n}\n.ct-pagination__main {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  gap: 16rpx;\n}\n.ct-pagination__btn {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  width: 80rpx;\n  height: 80rpx;\n  border-radius: 16rpx;\n  color: var(--ct-color-text-primary);\n  background: var(--ct-color-bg-container);\n  border: 2rpx solid var(--ct-color-border);\n  -webkit-transition: all 150ms ease;\n  transition: all 150ms ease;\n}\n.ct-pagination__btn--hover {\n  color: var(--ct-color-primary);\n  border-color: var(--ct-color-primary);\n}\n.ct-pagination__btn--disabled {\n  color: var(--ct-color-text-disabled);\n  background: var(--ct-color-bg-disabled);\n  border-color: var(--ct-color-border);\n  pointer-events: none;\n}\n.ct-pagination__btn--mini {\n  width: 56rpx;\n  height: 56rpx;\n}\n.ct-pagination__info {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 0 24rpx;\n  min-width: 100rpx;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-pagination__current {\n  font-size: 32rpx;\n  font-weight: 500;\n  color: var(--ct-color-primary);\n}\n.ct-pagination__divider {\n  margin: 0 8rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-pagination__pages {\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-pagination__text-mini {\n  padding: 0 16rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-pagination__pages-wrapper {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  gap: 8rpx;\n}\n.ct-pagination__page {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  min-width: 64rpx;\n  height: 64rpx;\n  padding: 0 16rpx;\n  border-radius: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-primary);\n  background: var(--ct-color-bg-container);\n  -webkit-transition: all 150ms ease;\n  transition: all 150ms ease;\n}\n.ct-pagination__page--hover {\n  color: var(--ct-color-primary);\n  background: var(--ct-color-bg-hover);\n}\n.ct-pagination__page--active {\n  color: #fff;\n  background: var(--ct-color-primary);\n  font-weight: 500;\n}\n.ct-pagination__page--disabled {\n  color: var(--ct-color-text-disabled);\n  pointer-events: none;\n}\n.ct-pagination__ellipsis {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  width: 48rpx;\n  height: 48rpx;\n  color: var(--ct-color-text-tertiary);\n  -webkit-transition: color 150ms ease;\n  transition: color 150ms ease;\n}\n.ct-pagination__ellipsis--hover {\n  color: var(--ct-color-primary);\n}\n.ct-pagination__total {\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-pagination__jumper {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  gap: 8rpx;\n}\n.ct-pagination__jumper-text {\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-pagination__jumper-input {\n  width: 80rpx;\n  height: 56rpx;\n  padding: 0 16rpx;\n  border: 2rpx solid var(--ct-color-border);\n  border-radius: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-primary);\n  text-align: center;\n  background: var(--ct-color-bg-container);\n}\n.ct-pagination__jumper-input:focus {\n  border-color: var(--ct-color-primary);\n}\n.ct-pagination--simple .ct-pagination__btn {\n  width: 80rpx;\n  height: 80rpx;\n}\n.ct-pagination--mini {\n  gap: 8rpx;\n}\n.ct-pagination--mini .ct-pagination__main {\n  gap: 8rpx;\n}\n.ct-pagination--default .ct-pagination__main {\n  gap: 16rpx;\n}\n.ct-pagination--disabled {\n  opacity: 0.6;\n  pointer-events: none;\n}";
styleInject(css_248z$q);

const Pagination = ({
  current: propCurrent,
  defaultCurrent = 1,
  total,
  pageSize = 10,
  mode = 'simple',
  showTotal = false,
  showQuickJumper = false,
  disabled = false,
  onChange,
  prevText,
  nextText,
  className,
  style
}) => {
  /**
   * Derived Values
   */
  const isControlled = propCurrent !== undefined;
  /**
   * States
   */
  const [innerCurrent, setInnerCurrent] = require$$0.useState(defaultCurrent);
  const [jumpValue, setJumpValue] = require$$0.useState('');
  /**
   * Derived Values
   */
  const current = isControlled ? propCurrent : innerCurrent;
  const totalPages = require$$0.useMemo(() => {
    return Math.max(1, Math.ceil(total / pageSize));
  }, [total, pageSize]);
  const range = require$$0.useMemo(() => {
    const start = (current - 1) * pageSize + 1;
    const end = Math.min(current * pageSize, total);
    return [start, end];
  }, [current, pageSize, total]);
  const pageButtons = require$$0.useMemo(() => {
    if (mode !== 'default') return [];
    const buttons = [];
    const maxButtons = 5;
    if (totalPages <= maxButtons + 2) {
      for (let i = 1; i <= totalPages; i++) {
        buttons.push(i);
      }
    } else {
      buttons.push(1);
      if (current > 3) {
        buttons.push('prev');
      }
      let start = Math.max(2, current - 1);
      let end = Math.min(totalPages - 1, current + 1);
      if (current <= 3) {
        end = Math.min(totalPages - 1, maxButtons - 1);
      }
      if (current >= totalPages - 2) {
        start = Math.max(2, totalPages - maxButtons + 2);
      }
      for (let i = start; i <= end; i++) {
        buttons.push(i);
      }
      if (current < totalPages - 2) {
        buttons.push('next');
      }
      buttons.push(totalPages);
    }
    return buttons;
  }, [current, totalPages, mode]);
  /**
   * Callbacks
   */
  const handleChange = require$$0.useCallback(page => {
    const validPage = Math.max(1, Math.min(page, totalPages));
    if (validPage === current || disabled) return;
    if (!isControlled) {
      setInnerCurrent(validPage);
    }
    onChange?.(validPage, pageSize);
  }, [current, totalPages, isControlled, pageSize, onChange, disabled]);
  const handlePrev = () => {
    if (current > 1) {
      handleChange(current - 1);
    }
  };
  const handleNext = () => {
    if (current < totalPages) {
      handleChange(current + 1);
    }
  };
  const handleJump = () => {
    const page = parseInt(jumpValue, 10);
    if (!isNaN(page)) {
      handleChange(page);
    }
    setJumpValue('');
  };
  /**
   * Render Helpers
   */
  const renderTotal = () => {
    if (!showTotal) return null;
    const content = typeof showTotal === 'function' ? showTotal(total, range) : `共 ${total} 条`;
    return jsxRuntimeExports.jsx(components.View, {
      className: "ct-pagination__total",
      children: jsxRuntimeExports.jsx(components.Text, {
        children: content
      })
    });
  };
  const renderSimple = () => jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-pagination__btn', {
        'ct-pagination__btn--disabled': current <= 1 || disabled
      }),
      onClick: handlePrev,
      hoverClass: current <= 1 || disabled ? 'none' : 'ct-pagination__btn--hover',
      children: prevText || jsxRuntimeExports.jsx(Icon, {
        name: "chevron-left",
        size: 40
      })
    }), jsxRuntimeExports.jsxs(components.View, {
      className: "ct-pagination__info",
      children: [jsxRuntimeExports.jsx(components.Text, {
        className: "ct-pagination__current",
        children: current
      }), jsxRuntimeExports.jsx(components.Text, {
        className: "ct-pagination__divider",
        children: "/"
      }), jsxRuntimeExports.jsx(components.Text, {
        className: "ct-pagination__pages",
        children: totalPages
      })]
    }), jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-pagination__btn', {
        'ct-pagination__btn--disabled': current >= totalPages || disabled
      }),
      onClick: handleNext,
      hoverClass: current >= totalPages || disabled ? 'none' : 'ct-pagination__btn--hover',
      children: nextText || jsxRuntimeExports.jsx(Icon, {
        name: "chevron-right",
        size: 40
      })
    })]
  });
  const renderMini = () => jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-pagination__btn ct-pagination__btn--mini', {
        'ct-pagination__btn--disabled': current <= 1 || disabled
      }),
      onClick: handlePrev,
      hoverClass: current <= 1 || disabled ? 'none' : 'ct-pagination__btn--hover',
      children: jsxRuntimeExports.jsx(Icon, {
        name: "chevron-left",
        size: 32
      })
    }), jsxRuntimeExports.jsxs(components.Text, {
      className: "ct-pagination__text-mini",
      children: [current, "/", totalPages]
    }), jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-pagination__btn ct-pagination__btn--mini', {
        'ct-pagination__btn--disabled': current >= totalPages || disabled
      }),
      onClick: handleNext,
      hoverClass: current >= totalPages || disabled ? 'none' : 'ct-pagination__btn--hover',
      children: jsxRuntimeExports.jsx(Icon, {
        name: "chevron-right",
        size: 32
      })
    })]
  });
  const renderDefault = () => jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-pagination__btn', {
        'ct-pagination__btn--disabled': current <= 1 || disabled
      }),
      onClick: handlePrev,
      hoverClass: current <= 1 || disabled ? 'none' : 'ct-pagination__btn--hover',
      children: prevText || jsxRuntimeExports.jsx(Icon, {
        name: "chevron-left",
        size: 40
      })
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-pagination__pages-wrapper",
      children: pageButtons.map((item, index) => {
        if (item === 'prev') {
          return jsxRuntimeExports.jsx(components.View, {
            className: "ct-pagination__ellipsis",
            onClick: () => handleChange(Math.max(1, current - 5)),
            hoverClass: "ct-pagination__ellipsis--hover",
            children: jsxRuntimeExports.jsx(Icon, {
              name: "more-horizontal",
              size: 32
            })
          }, "prev-ellipsis");
        }
        if (item === 'next') {
          return jsxRuntimeExports.jsx(components.View, {
            className: "ct-pagination__ellipsis",
            onClick: () => handleChange(Math.min(totalPages, current + 5)),
            hoverClass: "ct-pagination__ellipsis--hover",
            children: jsxRuntimeExports.jsx(Icon, {
              name: "more-horizontal",
              size: 32
            })
          }, "next-ellipsis");
        }
        return jsxRuntimeExports.jsx(components.View, {
          className: classNames('ct-pagination__page', {
            'ct-pagination__page--active': item === current,
            'ct-pagination__page--disabled': disabled
          }),
          onClick: () => handleChange(item),
          hoverClass: disabled ? 'none' : 'ct-pagination__page--hover',
          children: jsxRuntimeExports.jsx(components.Text, {
            children: item
          })
        }, item);
      })
    }), jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-pagination__btn', {
        'ct-pagination__btn--disabled': current >= totalPages || disabled
      }),
      onClick: handleNext,
      hoverClass: current >= totalPages || disabled ? 'none' : 'ct-pagination__btn--hover',
      children: nextText || jsxRuntimeExports.jsx(Icon, {
        name: "chevron-right",
        size: 40
      })
    })]
  });
  const renderQuickJumper = () => {
    if (!showQuickJumper || mode === 'mini') return null;
    return jsxRuntimeExports.jsxs(components.View, {
      className: "ct-pagination__jumper",
      children: [jsxRuntimeExports.jsx(components.Text, {
        className: "ct-pagination__jumper-text",
        children: "\u8DF3\u81F3"
      }), jsxRuntimeExports.jsx(components.Input, {
        className: "ct-pagination__jumper-input",
        type: "number",
        value: jumpValue,
        disabled: disabled,
        onInput: e => setJumpValue(e.detail.value),
        onConfirm: handleJump,
        onBlur: handleJump
      }), jsxRuntimeExports.jsx(components.Text, {
        className: "ct-pagination__jumper-text",
        children: "\u9875"
      })]
    });
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-pagination', `ct-pagination--${mode}`, {
      'ct-pagination--disabled': disabled
    }, className),
    style: style,
    children: [renderTotal(), jsxRuntimeExports.jsxs(components.View, {
      className: "ct-pagination__main",
      children: [mode === 'simple' && renderSimple(), mode === 'mini' && renderMini(), mode === 'default' && renderDefault()]
    }), renderQuickJumper()]
  });
};

var css_248z$p = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-popover {\n  position: relative;\n  display: inline-block;\n}\n.ct-popover__trigger {\n  display: inline-block;\n}\n.ct-popover__overlay {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 599;\n}\n.ct-popover__content {\n  position: fixed;\n  z-index: 600;\n  border-radius: 24rpx;\n  overflow: hidden;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.12);\n}\n.ct-popover__content--light {\n  background: var(--ct-color-bg);\n}\n.ct-popover__content--light .ct-popover__action {\n  color: var(--ct-color-text-primary);\n}\n.ct-popover__content--dark {\n  background: var(--ct-color-text-primary);\n}\n.ct-popover__content--dark .ct-popover__action {\n  color: #fff;\n}\n.ct-popover__content--top, .ct-popover__content--top-start, .ct-popover__content--top-end {\n  -webkit-transform: translateX(-50%) translateY(-100%);\n          transform: translateX(-50%) translateY(-100%);\n}\n.ct-popover__content--top .ct-popover__arrow, .ct-popover__content--top-start .ct-popover__arrow, .ct-popover__content--top-end .ct-popover__arrow {\n  bottom: -12rpx;\n  border-top-color: inherit;\n}\n.ct-popover__content--bottom .ct-popover__arrow, .ct-popover__content--bottom-start .ct-popover__arrow, .ct-popover__content--bottom-end .ct-popover__arrow {\n  top: -12rpx;\n  border-bottom-color: inherit;\n}\n.ct-popover__content--top-start, .ct-popover__content--bottom-start {\n  -webkit-transform: translateY(0);\n          transform: translateY(0);\n}\n.ct-popover__content--top-end, .ct-popover__content--bottom-end {\n  -webkit-transform: translateX(-100%);\n          transform: translateX(-100%);\n}\n.ct-popover__content--left, .ct-popover__content--left-start, .ct-popover__content--left-end {\n  -webkit-transform: translateX(-100%) translateY(-50%);\n          transform: translateX(-100%) translateY(-50%);\n}\n.ct-popover__content--left .ct-popover__arrow, .ct-popover__content--left-start .ct-popover__arrow, .ct-popover__content--left-end .ct-popover__arrow {\n  right: -12rpx;\n  border-left-color: inherit;\n}\n.ct-popover__content--right, .ct-popover__content--right-start, .ct-popover__content--right-end {\n  -webkit-transform: translateY(-50%);\n          transform: translateY(-50%);\n}\n.ct-popover__content--right .ct-popover__arrow, .ct-popover__content--right-start .ct-popover__arrow, .ct-popover__content--right-end .ct-popover__arrow {\n  left: -12rpx;\n  border-right-color: inherit;\n}\n.ct-popover__arrow {\n  position: absolute;\n  width: 0;\n  height: 0;\n  border: 12rpx solid transparent;\n  left: 50%;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n}\n.ct-popover__body {\n  padding: 24rpx;\n}\n.ct-popover__actions {\n  min-width: 240rpx;\n}\n.ct-popover__action {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n  font-size: 28rpx;\n}\n.ct-popover__action:active, .ct-popover__action.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-popover__action--disabled {\n  opacity: 0.4;\n  pointer-events: none;\n}\n.ct-popover__action:not(:last-child) {\n  position: relative;\n}\n.ct-popover__action:not(:last-child)::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: bottom;\n          transform-origin: bottom;\n}\n.ct-popover__action-icon {\n  margin-right: 16rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-popover__action-text {\n  -webkit-box-flex: 1;\n          flex: 1;\n}";
styleInject(css_248z$p);

const Popover = ({
  open,
  defaultOpen = false,
  onOpenChange,
  actions = [],
  onSelect,
  placement = 'bottom',
  trigger = 'click',
  children,
  content,
  overlay = false,
  closeOnOverlayClick = true,
  closeOnSelect = true,
  theme = 'light',
  offset = [0, 8],
  className
}) => {
  /**
   * Hooks
   */
  const [isOpen, setIsOpen] = useControllable({
    value: open,
    defaultValue: defaultOpen,
    onChange: onOpenChange
  });
  /**
   * Refs
   */
  const triggerRef = require$$0.useRef(`popover-trigger-${Date.now()}`);
  /**
   * States
   */
  const [position, setPosition] = require$$0.useState({
    top: 0,
    left: 0
  });
  /**
   * Callbacks
   */
  const calculatePosition = () => {
    const query = Taro.createSelectorQuery();
    query.select(`#${triggerRef.current}`).boundingClientRect();
    query.selectViewport().scrollOffset();
    query.exec(res => {
      if (!res[0]) return;
      const rect = res[0];
      const scrollTop = res[1]?.scrollTop || 0;
      const [offsetX, offsetY] = offset;
      let top = 0;
      let left = 0;
      if (placement.startsWith('top')) {
        top = rect.top + scrollTop - offsetY;
      } else if (placement.startsWith('bottom')) {
        top = rect.bottom + scrollTop + offsetY;
      } else if (placement.startsWith('left')) {
        left = rect.left - offsetX;
        top = rect.top + scrollTop + rect.height / 2;
      } else if (placement.startsWith('right')) {
        left = rect.right + offsetX;
        top = rect.top + scrollTop + rect.height / 2;
      }
      if (placement.includes('-start')) {
        left = rect.left + offsetX;
      } else if (placement.includes('-end')) {
        left = rect.right - offsetX;
      } else if (placement === 'top' || placement === 'bottom') {
        left = rect.left + rect.width / 2;
      }
      setPosition({
        top,
        left
      });
    });
  };
  const handleTriggerClick = () => {
    if (trigger === 'click') {
      setIsOpen(!isOpen);
    }
  };
  const handleOverlayClick = () => {
    if (closeOnOverlayClick) {
      setIsOpen(false);
    }
  };
  const handleSelect = (action, index) => {
    if (action.disabled) return;
    onSelect?.(action, index);
    if (closeOnSelect) {
      setIsOpen(false);
    }
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (isOpen) {
      calculatePosition();
    }
  }, [children, isOpen, offset, placement]);
  require$$0.useEffect(() => {
    if (!isOpen) return undefined;
    const handleUpdate = () => calculatePosition();
    if (Taro.onWindowResize) {
      Taro.onWindowResize(handleUpdate);
    }
    if (typeof window !== 'undefined') {
      window.addEventListener('scroll', handleUpdate, true);
      window.addEventListener('resize', handleUpdate);
    }
    return () => {
      Taro.offWindowResize?.(handleUpdate);
      if (typeof window !== 'undefined') {
        window.removeEventListener('scroll', handleUpdate, true);
        window.removeEventListener('resize', handleUpdate);
      }
    };
  }, [isOpen, offset, placement]);
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-popover', className),
    children: [jsxRuntimeExports.jsx(components.View, {
      id: triggerRef.current,
      className: "ct-popover__trigger",
      onClick: handleTriggerClick,
      children: children
    }), isOpen && overlay && jsxRuntimeExports.jsx(components.View, {
      className: "ct-popover__overlay",
      onClick: handleOverlayClick
    }), isOpen && jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-popover__content', `ct-popover__content--${placement}`, `ct-popover__content--${theme}`),
      style: {
        top: `${position.top}px`,
        left: `${position.left}px`
      },
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-popover__arrow"
      }), content ? jsxRuntimeExports.jsx(components.View, {
        className: "ct-popover__body",
        children: content
      }) : jsxRuntimeExports.jsx(components.View, {
        className: "ct-popover__actions",
        children: actions.map((action, index) => jsxRuntimeExports.jsxs(components.View, {
          className: classNames('ct-popover__action', {
            'ct-popover__action--disabled': action.disabled
          }),
          style: action.color ? {
            color: action.color
          } : undefined,
          onClick: () => handleSelect(action, index),
          children: [action.icon && jsxRuntimeExports.jsx(components.View, {
            className: "ct-popover__action-icon",
            children: action.icon
          }), jsxRuntimeExports.jsx(components.View, {
            className: "ct-popover__action-text",
            children: action.text
          })]
        }, index))
      })]
    })]
  });
};

var css_248z$o = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-progress--line {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-progress--line .ct-progress__track {\n  -webkit-box-flex: 1;\n          flex: 1;\n  background: var(--ct-color-fill-tertiary);\n  border-radius: 100rpx;\n  overflow: hidden;\n}\n.ct-progress--line .ct-progress__fill {\n  height: 100%;\n  border-radius: 100rpx;\n  -webkit-transition: width 250ms ease;\n  transition: width 250ms ease;\n}\n.ct-progress--line .ct-progress__text {\n  margin-left: 16rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-secondary);\n  white-space: nowrap;\n}\n.ct-progress--circle {\n  position: relative;\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-progress--circle svg {\n  -webkit-transform: rotate(-90deg);\n          transform: rotate(-90deg);\n}\n.ct-progress--circle .ct-progress__track {\n  fill: none;\n  stroke: var(--ct-color-fill-tertiary);\n}\n.ct-progress--circle .ct-progress__fill {\n  fill: none;\n  stroke-linecap: round;\n  -webkit-transition: stroke-dashoffset 250ms ease;\n  transition: stroke-dashoffset 250ms ease;\n}\n.ct-progress--circle .ct-progress__text {\n  position: absolute;\n  font-size: 32rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n}";
styleInject(css_248z$o);

/**
 * Constants
 */
const STATUS_COLOR$1 = {
  normal: 'var(--ct-color-primary)',
  success: 'var(--ct-color-success)',
  error: 'var(--ct-color-error)'
};
const STATUS_COLOR_FALLBACK = {
  normal: '#3b82f6',
  success: '#22c55e',
  error: '#ef4444'
};
const Progress = ({
  percent,
  type = 'line',
  status = 'normal',
  showText = true,
  format,
  trackColor,
  fillColor,
  strokeWidth = 8,
  size = 200,
  className
}) => {
  /**
   * Derived Values
   */
  const validPercent = Math.max(0, Math.min(100, percent));
  const color = fillColor || STATUS_COLOR$1[status];
  const isFillCssVar = typeof fillColor === 'string' && fillColor.startsWith('var(');
  const svgColor = isFillCssVar ? STATUS_COLOR_FALLBACK[status] : fillColor || STATUS_COLOR_FALLBACK[status];
  const displayText = format ? format(validPercent) : `${validPercent}%`;
  const resolvedTrackColor = trackColor ?? 'var(--ct-color-border)';
  const isTrackCssVar = typeof trackColor === 'string' && trackColor.startsWith('var(');
  const svgTrackColor = isTrackCssVar ? '#e5e7eb' : trackColor ?? '#e5e7eb';
  /**
   * Render Helpers
   */
  if (type === 'circle') {
    const radius = (size - strokeWidth) / 2;
    const circumference = 2 * Math.PI * radius;
    const offset = circumference * (1 - validPercent / 100);
    const env = Taro.getEnv?.();
    const useInlineSvg = env === Taro.ENV_TYPE.WEB;
    const svgMarkup = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${size} ${size}"><circle cx="${size / 2}" cy="${size / 2}" r="${radius}" stroke="${svgTrackColor}" stroke-width="${strokeWidth}" fill="none"/><circle cx="${size / 2}" cy="${size / 2}" r="${radius}" stroke="${svgColor}" stroke-width="${strokeWidth}" stroke-dasharray="${circumference}" stroke-dashoffset="${offset}" stroke-linecap="round" fill="none"/></svg>`;
    const svgDataUri = `data:image/svg+xml;utf8,${encodeURIComponent(svgMarkup)}`;
    return jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-progress', 'ct-progress--circle', className),
      children: [useInlineSvg ? jsxRuntimeExports.jsxs("svg", {
        viewBox: `0 0 ${size} ${size}`,
        style: {
          width: `${size}rpx`,
          height: `${size}rpx`
        },
        children: [jsxRuntimeExports.jsx("circle", {
          className: "ct-progress__track",
          cx: size / 2,
          cy: size / 2,
          r: radius,
          strokeWidth: strokeWidth,
          style: {
            stroke: resolvedTrackColor
          }
        }), jsxRuntimeExports.jsx("circle", {
          className: "ct-progress__fill",
          cx: size / 2,
          cy: size / 2,
          r: radius,
          strokeWidth: strokeWidth,
          strokeDasharray: circumference,
          strokeDashoffset: offset,
          style: {
            stroke: color
          }
        })]
      }) : jsxRuntimeExports.jsx(components.Image, {
        src: svgDataUri,
        mode: "aspectFit",
        style: {
          width: `${size}rpx`,
          height: `${size}rpx`
        }
      }), showText && jsxRuntimeExports.jsx(components.View, {
        className: "ct-progress__text",
        children: jsxRuntimeExports.jsx(components.Text, {
          children: displayText
        })
      })]
    });
  }
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-progress', 'ct-progress--line', className),
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-progress__track",
      style: {
        height: `${strokeWidth}rpx`,
        backgroundColor: resolvedTrackColor
      },
      children: jsxRuntimeExports.jsx(components.View, {
        className: "ct-progress__fill",
        style: {
          width: `${validPercent}%`,
          backgroundColor: color
        }
      })
    }), showText && jsxRuntimeExports.jsx(components.View, {
      className: "ct-progress__text",
      children: jsxRuntimeExports.jsx(components.Text, {
        children: displayText
      })
    })]
  });
};

/**
 * Constants
 */
const initialState = {
  startX: 0,
  startY: 0,
  deltaX: 0,
  deltaY: 0,
  offsetX: 0,
  offsetY: 0,
  direction: '',
  velocity: 0,
  velocityX: 0,
  velocityY: 0,
  startTime: 0,
  isLocked: false
};
/**
 * Main Hook
 */
function useTouch(options = {}) {
  const {
    lockDirection = false,
    threshold = 10,
    velocityThreshold = 0.3
  } = options;
  /**
   * States
   */
  const [state, setState] = require$$0.useState(initialState);
  /**
   * Refs
   */
  const touchRef = require$$0.useRef(initialState);
  /**
   * Callbacks
   */
  const onTouchStart = event => {
    const touch = event.touches[0];
    const now = Date.now();
    touchRef.current = {
      ...touchRef.current,
      startX: touch.clientX,
      startY: touch.clientY,
      startTime: now,
      direction: '',
      isLocked: false
    };
    setState(touchRef.current);
  };
  const onTouchMove = event => {
    const touch = event.touches[0];
    const now = Date.now();
    const deltaX = touch.clientX - touchRef.current.startX;
    const deltaY = touch.clientY - touchRef.current.startY;
    const offsetX = Math.abs(deltaX);
    const offsetY = Math.abs(deltaY);
    const duration = now - touchRef.current.startTime;
    const velocityX = duration > 0 ? deltaX / duration : 0;
    const velocityY = duration > 0 ? deltaY / duration : 0;
    const velocity = Math.sqrt(velocityX ** 2 + velocityY ** 2);
    let direction = touchRef.current.direction;
    let isLocked = touchRef.current.isLocked;
    if (!direction && (offsetX > threshold || offsetY > threshold)) {
      direction = offsetX > offsetY ? 'horizontal' : 'vertical';
      if (lockDirection) {
        isLocked = true;
      }
    }
    touchRef.current = {
      ...touchRef.current,
      deltaX,
      deltaY,
      offsetX,
      offsetY,
      direction,
      velocity,
      velocityX,
      velocityY,
      isLocked
    };
    setState(touchRef.current);
  };
  const onTouchEnd = () => {
    // keep final state for swipe detection
  };
  const isSwipe = direction => {
    const {
      velocityX,
      velocityY,
      deltaX,
      deltaY
    } = touchRef.current;
    switch (direction) {
      case 'left':
        return velocityX < -velocityThreshold && deltaX < 0;
      case 'right':
        return velocityX > velocityThreshold && deltaX > 0;
      case 'up':
        return velocityY < -velocityThreshold && deltaY < 0;
      case 'down':
        return velocityY > velocityThreshold && deltaY > 0;
      default:
        return false;
    }
  };
  const reset = () => {
    touchRef.current = {
      ...initialState
    };
    setState(touchRef.current);
  };
  return {
    state,
    reset,
    onTouchStart,
    onTouchMove,
    onTouchEnd,
    isSwipe
  };
}

/**
 * Helpers
 */
function px2rpx(px) {
  return px * 2;
}

var css_248z$n = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-pull-refresh {\n  overflow: hidden;\n}\n.ct-pull-refresh__head {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  overflow: hidden;\n}\n.ct-pull-refresh__tip {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n  gap: 16rpx;\n}\n.ct-pull-refresh__content {\n  min-height: 100%;\n}";
styleInject(css_248z$n);

/**
 * Main Component
 */
const PullRefresh = ({
  refreshing,
  onRefresh,
  threshold = 100,
  pullingText = '下拉刷新',
  loosingText = '释放刷新',
  loadingText = '加载中...',
  successText = '刷新成功',
  successDuration = 500,
  disabled = false,
  head,
  children,
  className
}) => {
  /**
   * States
   */
  const [status, setStatus] = require$$0.useState('normal');
  const [pullDistance, setPullDistance] = require$$0.useState(0);
  /**
   * Refs
   */
  const successTimerRef = require$$0.useRef(null);
  const canPullRef = require$$0.useRef(true);
  /**
   * Hooks
   */
  const touch = useTouch({
    lockDirection: true
  });
  /**
   * Callbacks
   */
  const clearSuccessTimer = () => {
    if (successTimerRef.current) {
      clearTimeout(successTimerRef.current);
      successTimerRef.current = null;
    }
  };
  const handleTouchStart = event => {
    if (disabled || refreshing) return;
    touch.onTouchStart(event);
    Taro.createSelectorQuery().selectViewport().scrollOffset().exec(res => {
      const scrollTop = res?.[0]?.scrollTop ?? 0;
      canPullRef.current = scrollTop <= 0;
      if (!canPullRef.current) {
        setPullDistance(0);
        setStatus('normal');
      }
    });
  };
  const handleTouchMove = event => {
    if (disabled || refreshing) return;
    if (!canPullRef.current) return;
    touch.onTouchMove(event);
    const {
      direction,
      deltaY
    } = touch.state;
    if (direction && direction !== 'vertical') return;
    if (deltaY <= 0) {
      setPullDistance(0);
      setStatus('normal');
      return;
    }
    const distance = px2rpx(deltaY);
    const damped = Math.min(distance, threshold * 1.6);
    setPullDistance(damped);
    setStatus(damped >= threshold ? 'loosing' : 'pulling');
  };
  const handleTouchEnd = () => {
    if (disabled || refreshing) return;
    touch.onTouchEnd();
    if (status === 'loosing') {
      setStatus('loading');
      setPullDistance(threshold);
      onRefresh?.();
      return;
    }
    setStatus('normal');
    setPullDistance(0);
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (refreshing) {
      clearSuccessTimer();
      setStatus('loading');
      setPullDistance(threshold);
      return;
    }
    if (status === 'loading') {
      if (successDuration > 0) {
        setStatus('success');
        setPullDistance(threshold);
        successTimerRef.current = setTimeout(() => {
          setStatus('normal');
          setPullDistance(0);
        }, successDuration);
      } else {
        setStatus('normal');
        setPullDistance(0);
      }
    }
  }, [refreshing, status, successDuration, threshold]);
  require$$0.useEffect(() => {
    return () => {
      clearSuccessTimer();
    };
  }, []);
  /**
   * Render Helpers
   */
  const renderHead = () => {
    if (head) return head(status);
    switch (status) {
      case 'pulling':
        return jsxRuntimeExports.jsxs(components.View, {
          className: "ct-pull-refresh__tip",
          children: [jsxRuntimeExports.jsx(Icon, {
            name: "arrow-down",
            size: 32
          }), jsxRuntimeExports.jsx(components.Text, {
            children: pullingText
          })]
        });
      case 'loosing':
        return jsxRuntimeExports.jsxs(components.View, {
          className: "ct-pull-refresh__tip",
          children: [jsxRuntimeExports.jsx(Icon, {
            name: "arrow-up",
            size: 32
          }), jsxRuntimeExports.jsx(components.Text, {
            children: loosingText
          })]
        });
      case 'loading':
        return jsxRuntimeExports.jsxs(components.View, {
          className: "ct-pull-refresh__tip",
          children: [jsxRuntimeExports.jsx(Loading, {
            type: "spinner",
            size: "sm"
          }), jsxRuntimeExports.jsx(components.Text, {
            children: loadingText
          })]
        });
      case 'success':
        return jsxRuntimeExports.jsxs(components.View, {
          className: "ct-pull-refresh__tip",
          children: [jsxRuntimeExports.jsx(Icon, {
            name: "check",
            size: 32
          }), jsxRuntimeExports.jsx(components.Text, {
            children: successText
          })]
        });
      default:
        return null;
    }
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-pull-refresh', className),
    onTouchStart: handleTouchStart,
    onTouchMove: handleTouchMove,
    onTouchEnd: handleTouchEnd,
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-pull-refresh__head",
      style: {
        height: `${pullDistance}rpx`
      },
      children: renderHead()
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-pull-refresh__content",
      children: children
    })]
  });
};

var css_248z$m = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-radio {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-radio:active, .ct-radio.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-radio--right {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: reverse;\n          flex-direction: row-reverse;\n}\n.ct-radio--right .ct-radio__label {\n  margin-right: 16rpx;\n  margin-left: 0;\n}\n.ct-radio__icon {\n  width: 40rpx;\n  height: 40rpx;\n  border: 2rpx solid var(--ct-color-border);\n  border-radius: 50%;\n  background: var(--ct-color-bg-card);\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  -webkit-transition: all 150ms ease;\n  transition: all 150ms ease;\n  flex-shrink: 0;\n}\n.ct-radio__dot {\n  width: 20rpx;\n  height: 20rpx;\n  border-radius: 50%;\n  background: var(--ct-color-primary);\n  -webkit-transform: scale(0);\n          transform: scale(0);\n  -webkit-transition: -webkit-transform 150ms ease;\n  transition: -webkit-transform 150ms ease;\n  transition: transform 150ms ease;\n  transition: transform 150ms ease, -webkit-transform 150ms ease;\n}\n.ct-radio--checked .ct-radio__icon {\n  border-color: var(--ct-color-primary);\n}\n.ct-radio--checked .ct-radio__dot {\n  -webkit-transform: scale(1);\n          transform: scale(1);\n}\n.ct-radio__label {\n  margin-left: 16rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  line-height: 1.5;\n}\n.ct-radio--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-radio--disabled .ct-radio__icon {\n  background: var(--ct-color-bg-disabled);\n  border-color: var(--ct-color-border);\n}\n.ct-radio--disabled.ct-radio--checked .ct-radio__dot {\n  background: var(--ct-color-text-quaternary);\n}\n\n.ct-radio-group {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  flex-wrap: wrap;\n}\n.ct-radio-group--vertical {\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n}\n.ct-radio-group--vertical .ct-radio {\n  margin-bottom: 24rpx;\n}\n.ct-radio-group--vertical .ct-radio:last-child {\n  margin-bottom: 0;\n}\n.ct-radio-group--horizontal {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: normal;\n          flex-direction: row;\n}\n.ct-radio-group--horizontal .ct-radio {\n  margin-right: 32rpx;\n}\n.ct-radio-group--horizontal .ct-radio:last-child {\n  margin-right: 0;\n}";
styleInject(css_248z$m);

/**
 * Context
 */
const RadioContext = /*#__PURE__*/require$$0.createContext({});
const Radio = ({
  checked: propChecked,
  defaultChecked = false,
  disabled = false,
  value,
  onChange,
  iconPosition = 'left',
  checkedColor,
  className,
  children
}) => {
  /**
   * Hooks
   */
  const ctx = require$$0.useContext(RadioContext);
  /**
   * Derived Values
   */
  const isGroupMode = ctx.value !== undefined || ctx.onChange !== undefined;
  const isChecked = isGroupMode ? ctx.value === value : propChecked;
  const [checked, setChecked] = useControllable({
    value: isChecked,
    defaultValue: defaultChecked,
    onChange
  });
  const isDisabled = disabled || ctx.disabled;
  const iconStyle = checkedColor && checked ? {
    borderColor: checkedColor
  } : undefined;
  const dotStyle = checkedColor && checked ? {
    backgroundColor: checkedColor
  } : undefined;
  /**
   * Callbacks
   */
  const handleClick = () => {
    if (isDisabled || checked) return;
    if (isGroupMode) {
      if (value === undefined) {
        if (process.env.NODE_ENV !== 'production') {
          console.warn('[Radio] value is required when used inside Radio.Group.');
        }
        return;
      }
      ctx.onChange?.(value);
    } else {
      setChecked(true);
    }
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-radio', {
      'ct-radio--checked': checked,
      'ct-radio--disabled': isDisabled,
      'ct-radio--right': iconPosition === 'right'
    }, className),
    hoverClass: isDisabled ? 'none' : 'ct-clickable--hover',
    onClick: handleClick,
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-radio__icon",
      style: iconStyle,
      children: checked && jsxRuntimeExports.jsx(components.View, {
        className: "ct-radio__dot",
        style: dotStyle
      })
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-radio__label",
      children: typeof children === 'string' ? jsxRuntimeExports.jsx(components.Text, {
        children: children
      }) : children
    })]
  });
};
const RadioGroup = ({
  value: propValue,
  defaultValue,
  disabled = false,
  direction = 'vertical',
  onChange,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange: onChange
  });
  /**
   * Callbacks
   */
  const handleChange = val => {
    setValue(val);
  };
  return jsxRuntimeExports.jsx(RadioContext.Provider, {
    value: {
      value,
      disabled,
      onChange: handleChange
    },
    children: jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-radio-group', `ct-radio-group--${direction}`, className),
      children: children
    })
  });
};
Radio.Group = RadioGroup;

var css_248z$l = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-rate {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-rate__star {\n  position: relative;\n  flex-shrink: 0;\n}\n.ct-rate__star:active, .ct-rate__star.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-rate__star-first, .ct-rate__star-second {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-rate__star-first {\n  color: var(--ct-color-border);\n}\n.ct-rate__star-second {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%;\n  overflow: hidden;\n}\n.ct-rate__icon {\n  display: block;\n}\n.ct-rate__icon--active {\n  color: #f59e0b;\n}\n.ct-rate__star-full, .ct-rate__star-half-left, .ct-rate__star-half-right {\n  position: absolute;\n  top: 0;\n  height: 100%;\n}\n.ct-rate__star-full {\n  left: 0;\n  width: 100%;\n}\n.ct-rate__star-half-left {\n  left: 0;\n  width: 50%;\n}\n.ct-rate__star-half-right {\n  right: 0;\n  width: 50%;\n}\n.ct-rate--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-rate--disabled .ct-rate__star {\n  pointer-events: none;\n}\n.ct-rate--readonly .ct-rate__star {\n  pointer-events: none;\n}\n.ct-rate--readonly .ct-rate__star:active {\n  opacity: 1;\n}";
styleInject(css_248z$l);

const Rate = ({
  value: propValue,
  defaultValue = 0,
  count = 5,
  allowHalf = false,
  allowClear = false,
  disabled = false,
  readOnly = false,
  size = 48,
  gap = 8,
  character,
  onChange,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange
  });
  /**
   * Derived Values
   */
  const isInteractive = !disabled && !readOnly;
  const stars = require$$0.useMemo(() => {
    return Array.from({
      length: count
    }, (_, index) => {
      const starValue = index + 1;
      const isFull = value >= starValue;
      const isHalf = allowHalf && value >= starValue - 0.5 && value < starValue;
      return {
        index,
        isFull,
        isHalf,
        isEmpty: !isFull && !isHalf
      };
    });
  }, [count, value, allowHalf]);
  /**
   * Callbacks
   */
  const handleClick = (index, isHalf) => {
    if (!isInteractive) return;
    let newValue = isHalf ? index + 0.5 : index + 1;
    if (allowClear && newValue === value) {
      newValue = 0;
    }
    setValue(newValue);
  };
  /**
   * Render Helpers
   */
  const renderCharacter = (index, type) => {
    if (character) {
      return typeof character === 'function' ? character(index) : character;
    }
    return jsxRuntimeExports.jsx(Icon, {
      name: type === 'empty' ? 'star' : 'star-fill',
      size: size,
      className: classNames('ct-rate__icon', {
        'ct-rate__icon--active': type !== 'empty',
        'ct-rate__icon--half': type === 'half'
      })
    });
  };
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-rate', {
      'ct-rate--disabled': disabled,
      'ct-rate--readonly': readOnly
    }, className),
    style: {
      gap: `${gap}rpx`
    },
    children: stars.map(star => jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-rate__star', {
        'ct-rate__star--active': star.isFull || star.isHalf,
        'ct-rate__star--half': star.isHalf
      }),
      style: {
        width: `${size}rpx`,
        height: `${size}rpx`
      },
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-rate__star-first",
        children: renderCharacter(star.index, 'empty')
      }), (star.isFull || star.isHalf) && jsxRuntimeExports.jsx(components.View, {
        className: "ct-rate__star-second",
        style: star.isHalf ? {
          width: '50%'
        } : undefined,
        children: renderCharacter(star.index, star.isHalf ? 'half' : 'full')
      }), isInteractive && jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
        children: [allowHalf && jsxRuntimeExports.jsx(components.View, {
          className: "ct-rate__star-half-left",
          onClick: () => handleClick(star.index, true)
        }), jsxRuntimeExports.jsx(components.View, {
          className: allowHalf ? 'ct-rate__star-half-right' : 'ct-rate__star-full',
          onClick: () => handleClick(star.index, false)
        })]
      })]
    }, star.index))
  });
};

var css_248z$k = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-result {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 64rpx;\n  text-align: center;\n}\n.ct-result__icon {\n  margin-bottom: 40rpx;\n}\n.ct-result__title {\n  font-size: 36rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-primary);\n}\n.ct-result__description {\n  margin-top: 16rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-tertiary);\n  max-width: 560rpx;\n}\n.ct-result__actions {\n  margin-top: 48rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  gap: 24rpx;\n}";
styleInject(css_248z$k);

/**
 * Constants
 */
const STATUS_ICON = {
  success: 'check-circle-fill',
  error: 'close-circle-fill',
  info: 'info-circle',
  warning: 'warning-circle',
  waiting: 'clock',
  '404': 'search',
  '403': 'close-circle',
  '500': 'close-circle-fill',
  offline: 'close-circle'
};
const STATUS_COLOR = {
  success: 'var(--ct-color-success)',
  error: 'var(--ct-color-error)',
  info: 'var(--ct-color-primary)',
  warning: 'var(--ct-color-warning)',
  waiting: 'var(--ct-color-text-tertiary)',
  '404': 'var(--ct-color-text-tertiary)',
  '403': 'var(--ct-color-warning)',
  '500': 'var(--ct-color-error)',
  offline: 'var(--ct-color-text-tertiary)'
};
const Result = ({
  status,
  title,
  description,
  icon,
  children,
  className
}) => {
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-result', `ct-result--${status}`, className),
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-result__icon",
      children: icon || jsxRuntimeExports.jsx(Icon, {
        name: STATUS_ICON[status],
        size: 120,
        color: STATUS_COLOR[status]
      })
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-result__title",
      children: typeof title === 'string' ? jsxRuntimeExports.jsx(components.Text, {
        children: title
      }) : title
    }), description && jsxRuntimeExports.jsx(components.View, {
      className: "ct-result__description",
      children: typeof description === 'string' ? jsxRuntimeExports.jsx(components.Text, {
        children: description
      }) : description
    }), children && jsxRuntimeExports.jsx(components.View, {
      className: "ct-result__actions",
      children: children
    })]
  });
};

var css_248z$j = "";
styleInject(css_248z$j);

/**
 * Main Component
 */
const SafeArea = ({
  position,
  children,
  className
}) => {
  /**
   * States
   */
  const [height, setHeight] = require$$0.useState(0);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    const systemInfo = Taro.getSystemInfoSync();
    if (position === 'top') {
      setHeight(systemInfo.statusBarHeight || 0);
      return;
    }
    const screenHeight = systemInfo.screenHeight;
    const safeAreaBottom = systemInfo.safeArea?.bottom || screenHeight;
    setHeight(screenHeight - safeAreaBottom);
  }, [position]);
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-safe-area', `ct-safe-area--${position}`, className),
    style: {
      height: `${height}px`
    },
    children: children
  });
};

var css_248z$i = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-search-bar {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 16rpx 32rpx;\n  background: var(--ct-color-bg-card);\n}\n.ct-search-bar__content {\n  -webkit-box-flex: 1;\n          flex: 1;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  height: 72rpx;\n  padding: 0 24rpx;\n  background: var(--ct-color-fill-secondary);\n  border-radius: 9999rpx;\n  -webkit-transition: all 150ms;\n  transition: all 150ms;\n}\n.ct-search-bar__content--focused {\n  background: var(--ct-color-fill-tertiary);\n}\n.ct-search-bar__icon {\n  margin-right: 16rpx;\n}\n.ct-search-bar__input {\n  -webkit-box-flex: 1;\n          flex: 1;\n  height: 100%;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  background: transparent;\n}\n.ct-search-bar__input--center {\n  text-align: center;\n}\n.ct-search-bar__clear {\n  margin-left: 16rpx;\n}\n.ct-search-bar__clear:active, .ct-search-bar__clear.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-search-bar__cancel {\n  margin-left: 24rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-primary);\n}\n.ct-search-bar__cancel:active, .ct-search-bar__cancel.ct-clickable--hover {\n  opacity: 0.7;\n}";
styleInject(css_248z$i);

/**
 * Main Component
 */
const SearchBar = ({
  value: propValue,
  defaultValue = '',
  placeholder = '搜索',
  showCancel = false,
  cancelText = '取消',
  autoFocus = false,
  disabled = false,
  clearable = true,
  icon,
  align = 'left',
  backgroundColor,
  onChange,
  onFocus,
  onBlur,
  onSearch,
  onCancel,
  onClear,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange
  });
  /**
   * States
   */
  const [focused, setFocused] = require$$0.useState(false);
  /**
   * Derived Values
   */
  const showClear = clearable && value && !disabled;
  /**
   * Callbacks
   */
  const handleInput = event => {
    setValue(event.detail.value);
  };
  const handleFocus = event => {
    setFocused(true);
    onFocus?.(event);
  };
  const handleBlur = event => {
    setFocused(false);
    onBlur?.(event);
  };
  const handleClear = () => {
    setValue('');
    onClear?.();
  };
  const handleConfirm = () => {
    onSearch?.(value);
  };
  const handleCancel = () => {
    setValue('');
    setFocused(false);
    onCancel?.();
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-search-bar', className),
    children: [jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-search-bar__content', {
        'ct-search-bar__content--focused': focused
      }),
      style: backgroundColor ? {
        backgroundColor
      } : undefined,
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-search-bar__icon",
        children: icon || jsxRuntimeExports.jsx(Icon, {
          name: "search",
          size: 32,
          color: "var(--ct-color-text-tertiary)"
        })
      }), jsxRuntimeExports.jsx(components.Input, {
        className: classNames('ct-search-bar__input', `ct-search-bar__input--${align}`),
        value: value,
        placeholder: placeholder,
        disabled: disabled,
        focus: autoFocus,
        confirmType: "search",
        onInput: handleInput,
        onFocus: handleFocus,
        onBlur: handleBlur,
        onConfirm: handleConfirm
      }), showClear && jsxRuntimeExports.jsx(components.View, {
        className: "ct-search-bar__clear",
        onClick: handleClear,
        children: jsxRuntimeExports.jsx(Icon, {
          name: "close-circle-fill",
          size: 32,
          color: "var(--ct-color-text-quaternary)"
        })
      })]
    }), showCancel && (focused || value) && jsxRuntimeExports.jsx(components.View, {
      className: "ct-search-bar__cancel",
      onClick: handleCancel,
      children: jsxRuntimeExports.jsx(components.Text, {
        children: cancelText
      })
    })]
  });
};

var css_248z$h = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-select__trigger {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  height: 80rpx;\n  padding: 0 32rpx;\n  background: var(--ct-color-bg-card);\n  border: 2rpx solid var(--ct-color-border);\n  border-radius: 16rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  -webkit-transition: border-color 150ms ease;\n  transition: border-color 150ms ease;\n}\n.ct-select__trigger:active, .ct-select__trigger.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-select__value {\n  -webkit-box-flex: 1;\n          flex: 1;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.ct-select__value--placeholder {\n  color: var(--ct-color-text-tertiary);\n}\n.ct-select__clear {\n  flex-shrink: 0;\n  padding: 8rpx;\n  margin-right: 8rpx;\n}\n.ct-select__arrow {\n  flex-shrink: 0;\n  -webkit-transition: -webkit-transform 150ms ease;\n  transition: -webkit-transform 150ms ease;\n  transition: transform 150ms ease;\n  transition: transform 150ms ease, -webkit-transform 150ms ease;\n}\n.ct-select--disabled .ct-select__trigger {\n  opacity: 0.5;\n  pointer-events: none;\n  background: var(--ct-color-bg-disabled);\n}";
styleInject(css_248z$h);

const Select = ({
  value: propValue,
  defaultValue,
  options,
  placeholder = '请选择',
  disabled = false,
  title = '请选择',
  confirmText,
  cancelText,
  clearable = false,
  onChange,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange: val => {
      const option = options.find(opt => opt.value === val);
      onChange?.(val, option);
    }
  });
  /**
   * States
   */
  const [open, setOpen] = require$$0.useState(false);
  /**
   * Derived Values
   */
  const selectedOption = require$$0.useMemo(() => options.find(opt => opt.value === value), [options, value]);
  const pickerOptions = require$$0.useMemo(() => options.map(opt => ({
    label: opt.label,
    value: opt.value,
    disabled: opt.disabled
  })), [options]);
  /**
   * Callbacks
   */
  const handleTriggerClick = () => {
    if (disabled) return;
    setOpen(true);
  };
  const handleClear = e => {
    e.stopPropagation();
    setValue(undefined);
  };
  const handleConfirm = val => {
    setValue(val[0]);
    setOpen(false);
  };
  const handleClose = () => {
    setOpen(false);
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-select', {
      'ct-select--disabled': disabled
    }, className),
    children: [jsxRuntimeExports.jsxs(components.View, {
      className: "ct-select__trigger",
      onClick: handleTriggerClick,
      children: [jsxRuntimeExports.jsx(components.Text, {
        className: classNames('ct-select__value', {
          'ct-select__value--placeholder': !selectedOption
        }),
        children: selectedOption?.label || placeholder
      }), clearable && selectedOption && !disabled && jsxRuntimeExports.jsx(components.View, {
        className: "ct-select__clear",
        onClick: handleClear,
        children: jsxRuntimeExports.jsx(Icon, {
          name: "close-circle-fill",
          size: 32,
          color: "var(--ct-color-text-quaternary)"
        })
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-select__arrow",
        children: jsxRuntimeExports.jsx(Icon, {
          name: "arrow-down",
          size: 32,
          color: "var(--ct-color-text-tertiary)"
        })
      })]
    }), jsxRuntimeExports.jsx(Picker, {
      open: open,
      title: title,
      confirmText: confirmText,
      cancelText: cancelText,
      options: pickerOptions,
      value: value !== undefined ? [value] : undefined,
      onConfirm: handleConfirm,
      onClose: handleClose
    })]
  });
};

var css_248z$g = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-selector {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 24rpx;\n}\n.ct-selector--grid {\n  display: grid;\n}\n.ct-selector--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-selector__item {\n  position: relative;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  min-height: 72rpx;\n  padding: 16rpx 32rpx;\n  background: var(--ct-color-bg-card);\n  border: 2rpx solid var(--ct-color-border);\n  border-radius: 16rpx;\n}\n.ct-selector__item:active, .ct-selector__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-selector__item--selected {\n  background: var(--ct-color-primary-light);\n  border-color: var(--ct-color-primary);\n}\n.ct-selector__item--selected .ct-selector__item-label {\n  color: var(--ct-color-primary);\n}\n.ct-selector__item--selected .ct-selector__item-description {\n  color: var(--ct-color-primary);\n  opacity: 0.7;\n}\n.ct-selector__item--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n  pointer-events: none;\n}\n.ct-selector__item-content {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  gap: 8rpx;\n}\n.ct-selector__item-label {\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  text-align: center;\n}\n.ct-selector__item-description {\n  font-size: 22rpx;\n  color: var(--ct-color-text-tertiary);\n  text-align: center;\n}\n.ct-selector__check {\n  position: absolute;\n  right: 0;\n  bottom: 0;\n  width: 32rpx;\n  height: 32rpx;\n  background: var(--ct-color-primary);\n  border-radius: 8rpx 0 16rpx 0;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-selector__check-icon {\n  color: #fff;\n}";
styleInject(css_248z$g);

const Selector = ({
  options,
  value,
  defaultValue,
  multiple = false,
  columns,
  disabled = false,
  showCheckMark = true,
  onChange,
  className
}) => {
  /**
   * Hooks
   */
  const [internalValue, setInternalValue] = useControllable({
    value,
    defaultValue: defaultValue ?? (multiple ? [] : undefined)
  });
  /**
   * Derived Values
   */
  const selectedValues = require$$0.useMemo(() => {
    if (internalValue === undefined || internalValue === null) return [];
    return Array.isArray(internalValue) ? internalValue : [internalValue];
  }, [internalValue]);
  const gridStyle = columns ? {
    gridTemplateColumns: `repeat(${columns}, 1fr)`
  } : undefined;
  /**
   * Render Helpers
   */
  const isSelected = require$$0.useCallback(val => {
    return selectedValues.includes(val);
  }, [selectedValues]);
  /**
   * Callbacks
   */
  const handleOptionClick = require$$0.useCallback(option => {
    if (disabled || option.disabled) return;
    const val = option.value;
    let newValue;
    let newOptions;
    if (multiple) {
      if (isSelected(val)) {
        newValue = selectedValues.filter(v => v !== val);
        newOptions = options.filter(opt => newValue.includes(opt.value));
      } else {
        newValue = [...selectedValues, val];
        newOptions = options.filter(opt => newValue.includes(opt.value));
      }
    } else {
      if (isSelected(val)) {
        newValue = undefined;
        newOptions = undefined;
      } else {
        newValue = val;
        newOptions = option;
      }
    }
    setInternalValue(newValue);
    onChange?.(newValue, newOptions);
  }, [disabled, multiple, isSelected, selectedValues, options, setInternalValue, onChange]);
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-selector', {
      'ct-selector--disabled': disabled,
      'ct-selector--grid': columns
    }, className),
    style: gridStyle,
    children: options.map(option => {
      const selected = isSelected(option.value);
      return jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-selector__item', {
          'ct-selector__item--selected': selected,
          'ct-selector__item--disabled': option.disabled
        }),
        onClick: () => handleOptionClick(option),
        children: [jsxRuntimeExports.jsxs(components.View, {
          className: "ct-selector__item-content",
          children: [jsxRuntimeExports.jsx(components.Text, {
            className: "ct-selector__item-label",
            children: option.label
          }), option.description && jsxRuntimeExports.jsx(components.Text, {
            className: "ct-selector__item-description",
            children: option.description
          })]
        }), showCheckMark && selected && jsxRuntimeExports.jsx(components.View, {
          className: "ct-selector__check",
          children: jsxRuntimeExports.jsx(Icon, {
            name: "check",
            size: 24,
            className: "ct-selector__check-icon"
          })
        })]
      }, option.value);
    })
  });
};

var css_248z$f = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-sidebar {\n  width: 180rpx;\n  height: 100%;\n  background: var(--ct-color-bg-secondary);\n}\n.ct-sidebar__item {\n  position: relative;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 32rpx 24rpx;\n}\n.ct-sidebar__item:active, .ct-sidebar__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-sidebar__item--active {\n  background: var(--ct-color-bg-card);\n}\n.ct-sidebar__item--active .ct-sidebar__item-title {\n  color: var(--ct-color-primary);\n  font-weight: 500;\n}\n.ct-sidebar__item--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n  pointer-events: none;\n}\n.ct-sidebar__item-indicator {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  -webkit-transform: translateY(-50%);\n          transform: translateY(-50%);\n  width: 6rpx;\n  height: 40rpx;\n  background: var(--ct-color-primary);\n  border-radius: 0 3rpx 3rpx 0;\n}\n.ct-sidebar__item-content {\n  -webkit-box-flex: 1;\n          flex: 1;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-sidebar__item-title {\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  text-align: center;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}";
styleInject(css_248z$f);

const SideBar = ({
  items,
  activeKey,
  defaultActiveKey,
  onChange,
  className
}) => {
  /**
   * Derived Values
   */
  const defaultKey = defaultActiveKey || items[0]?.key;
  /**
   * Hooks
   */
  const [internalActiveKey, setInternalActiveKey] = useControllable({
    value: activeKey,
    defaultValue: defaultKey,
    onChange
  });
  /**
   * Callbacks
   */
  const handleItemClick = require$$0.useCallback(item => {
    if (item.disabled) return;
    setInternalActiveKey(item.key);
  }, [setInternalActiveKey]);
  /**
   * Render Helpers
   */
  const renderItem = item => {
    const isActive = internalActiveKey === item.key;
    return jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-sidebar__item', {
        'ct-sidebar__item--active': isActive,
        'ct-sidebar__item--disabled': item.disabled
      }),
      onClick: () => handleItemClick(item),
      children: [isActive && jsxRuntimeExports.jsx(components.View, {
        className: "ct-sidebar__item-indicator"
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-sidebar__item-content",
        children: item.badge !== undefined ? jsxRuntimeExports.jsx(Badge, {
          content: item.badge,
          children: jsxRuntimeExports.jsx(components.Text, {
            className: "ct-sidebar__item-title",
            children: item.title
          })
        }) : jsxRuntimeExports.jsx(components.Text, {
          className: "ct-sidebar__item-title",
          children: item.title
        })
      })]
    }, item.key);
  };
  return jsxRuntimeExports.jsx(components.ScrollView, {
    scrollY: true,
    className: classNames('ct-sidebar', className),
    children: items.map(renderItem)
  });
};

var css_248z$e = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-skeleton {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n}\n.ct-skeleton__avatar {\n  flex-shrink: 0;\n  margin-right: 32rpx;\n}\n.ct-skeleton__content {\n  -webkit-box-flex: 1;\n          flex: 1;\n}\n.ct-skeleton__title {\n  margin-bottom: 24rpx;\n}\n.ct-skeleton__row {\n  margin-top: 16rpx;\n}\n.ct-skeleton__row:first-child {\n  margin-top: 0;\n}\n.ct-skeleton__block {\n  background: var(--ct-color-fill-secondary);\n  border-radius: 8rpx;\n}\n.ct-skeleton__block--round {\n  border-radius: 50%;\n}\n.ct-skeleton__block--animated {\n  background: -webkit-gradient(linear, left top, right top, color-stop(25%, var(--ct-color-fill-secondary)), color-stop(37%, var(--ct-color-fill-tertiary)), color-stop(63%, var(--ct-color-fill-secondary)));\n  background: -webkit-linear-gradient(left, var(--ct-color-fill-secondary) 25%, var(--ct-color-fill-tertiary) 37%, var(--ct-color-fill-secondary) 63%);\n  background: linear-gradient(90deg, var(--ct-color-fill-secondary) 25%, var(--ct-color-fill-tertiary) 37%, var(--ct-color-fill-secondary) 63%);\n  background-size: 400% 100%;\n  -webkit-animation: ct-skeleton-loading 1.4s ease infinite;\n          animation: ct-skeleton-loading 1.4s ease infinite;\n}\n\n@-webkit-keyframes ct-skeleton-loading {\n  0% {\n    background-position: 100% 50%;\n  }\n  100% {\n    background-position: 0 50%;\n  }\n}\n\n@keyframes ct-skeleton-loading {\n  0% {\n    background-position: 100% 50%;\n  }\n  100% {\n    background-position: 0 50%;\n  }\n}";
styleInject(css_248z$e);

const SkeletonBlock = ({
  width = '100%',
  height = 32,
  round = false,
  animated = true,
  className
}) => {
  /**
   * Derived Values
   */
  const style = {
    width: typeof width === 'number' ? `${width}rpx` : width,
    height: typeof height === 'number' ? `${height}rpx` : height
  };
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-skeleton__block', {
      'ct-skeleton__block--animated': animated,
      'ct-skeleton__block--round': round
    }, className),
    style: style
  });
};
const Skeleton = ({
  loading = true,
  animated = true,
  rows = 3,
  avatar = false,
  avatarShape = 'circle',
  avatarSize = 80,
  title = true,
  titleWidth = '40%',
  rowsWidth,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const showTitle = title && rows > 0;
  /**
   * Render Helpers
   */
  const getRowWidth = index => {
    if (rowsWidth && rowsWidth[index]) {
      return rowsWidth[index];
    }
    if (index === rows - 1) {
      return '60%';
    }
    return '100%';
  };
  /**
   * Early Returns
   */
  if (!loading) {
    return jsxRuntimeExports.jsx(require$$0.Fragment, {
      children: children
    });
  }
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-skeleton', className),
    children: [avatar && jsxRuntimeExports.jsx(components.View, {
      className: "ct-skeleton__avatar",
      children: jsxRuntimeExports.jsx(SkeletonBlock, {
        width: avatarSize,
        height: avatarSize,
        round: avatarShape === 'circle',
        animated: animated
      })
    }), jsxRuntimeExports.jsxs(components.View, {
      className: "ct-skeleton__content",
      children: [showTitle && jsxRuntimeExports.jsx(SkeletonBlock, {
        width: titleWidth,
        height: 40,
        animated: animated,
        className: "ct-skeleton__title"
      }), rows > 0 && Array.from({
        length: rows
      }).map((_, index) => jsxRuntimeExports.jsx(SkeletonBlock, {
        width: getRowWidth(index),
        height: 32,
        animated: animated,
        className: "ct-skeleton__row"
      }, index))]
    })]
  });
};
Skeleton.Block = SkeletonBlock;

var css_248z$d = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-slider {\n  padding: 32rpx 0;\n}\n.ct-slider__track {\n  position: relative;\n  height: 8rpx;\n  background: var(--ct-color-fill-tertiary);\n  border-radius: 4rpx;\n}\n.ct-slider__fill {\n  position: absolute;\n  top: 0;\n  left: 0;\n  height: 100%;\n  background: var(--ct-color-primary);\n  border-radius: 4rpx;\n  -webkit-transition: width 0.1s ease;\n  transition: width 0.1s ease;\n}\n.ct-slider__thumb {\n  position: absolute;\n  top: 50%;\n  width: 48rpx;\n  height: 48rpx;\n  background: #fff;\n  border-radius: 50%;\n  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.15);\n  -webkit-transform: translate(-50%, -50%);\n          transform: translate(-50%, -50%);\n  z-index: 1;\n}\n.ct-slider__thumb--active {\n  -webkit-transform: translate(-50%, -50%) scale(1.1);\n          transform: translate(-50%, -50%) scale(1.1);\n}\n.ct-slider__tooltip {\n  position: absolute;\n  bottom: 100%;\n  left: 50%;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n  margin-bottom: 16rpx;\n  padding: 8rpx 16rpx;\n  background: var(--ct-color-text-primary);\n  border-radius: 8rpx;\n  font-size: 22rpx;\n  color: #fff;\n  white-space: nowrap;\n}\n.ct-slider__marks {\n  position: relative;\n  margin-top: 16rpx;\n}\n.ct-slider__mark {\n  position: absolute;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n  font-size: 22rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-slider--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}";
styleInject(css_248z$d);

const Slider = ({
  value: propValue,
  defaultValue = 0,
  min = 0,
  max = 100,
  step = 1,
  range = false,
  disabled = false,
  showMarks = false,
  marks,
  showTooltip = false,
  trackColor,
  activeColor,
  onChange,
  onAfterChange,
  className
}) => {
  /**
   * Derived Values
   */
  const normalizedDefaultValue = require$$0.useMemo(() => {
    if (!range) return defaultValue ?? min;
    if (Array.isArray(defaultValue)) return defaultValue;
    const fallback = defaultValue !== undefined ? Number(defaultValue) : min;
    return [min, fallback];
  }, [defaultValue, min, range]);
  const normalizedPropValue = require$$0.useMemo(() => {
    if (!range) return propValue;
    if (Array.isArray(propValue)) return propValue;
    if (propValue === undefined) return undefined;
    return [min, Number(propValue)];
  }, [min, propValue, range]);
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: normalizedPropValue,
    defaultValue: normalizedDefaultValue,
    onChange
  });
  /**
   * States
   */
  const trackId = require$$0.useRef(`ct-slider-track-${Date.now()}`);
  const trackRectRef = require$$0.useRef(null);
  const [dragging, setDragging] = require$$0.useState(false);
  const [tooltipVisible, setTooltipVisible] = require$$0.useState(false);
  /**
   * Render Helpers
   */
  const getPercent = val => (val - min) / (max - min) * 100;
  const formatValue = val => {
    const aligned = Math.round((val - min) / step) * step + min;
    return Math.max(min, Math.min(max, aligned));
  };
  const trackStyle = require$$0.useMemo(() => {
    if (range) {
      const [v1, v2] = Array.isArray(value) ? value : [min, min];
      return {
        left: `${getPercent(v1)}%`,
        width: `${getPercent(v2) - getPercent(v1)}%`,
        backgroundColor: activeColor
      };
    }
    return {
      width: `${getPercent(value)}%`,
      backgroundColor: activeColor
    };
  }, [value, activeColor, range]);
  /**
   * Callbacks
   */
  const handleTouchStart = thumbIndex => () => {
    if (disabled) return;
    Taro.createSelectorQuery().select(`#${trackId.current}`).boundingClientRect().exec(res => {
      if (res[0]) {
        trackRectRef.current = {
          left: res[0].left,
          width: res[0].width
        };
      }
    });
    setDragging(true);
    setTooltipVisible(showTooltip);
  };
  const handleTouchMove = thumbIndex => e => {
    if (disabled || !dragging) return;
    const touch = e.touches[0];
    const rect = trackRectRef.current;
    if (!rect) return;
    const {
      left,
      width
    } = rect;
    const percent = Math.max(0, Math.min(1, (touch.clientX - left) / width));
    const newVal = formatValue(min + percent * (max - min));
    if (range) {
      const [v1, v2] = Array.isArray(value) ? value : [min, min];
      const newValue = thumbIndex === 0 ? [Math.min(newVal, v2), v2] : [v1, Math.max(newVal, v1)];
      setValue(newValue);
    } else {
      setValue(newVal);
    }
  };
  const handleTouchEnd = () => {
    setDragging(false);
    setTooltipVisible(false);
    onAfterChange?.(value);
  };
  const renderThumb = (val, index) => jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-slider__thumb', {
      'ct-slider__thumb--active': dragging
    }),
    style: {
      left: `${getPercent(val)}%`
    },
    onTouchStart: handleTouchStart(),
    onTouchMove: handleTouchMove(index),
    onTouchEnd: handleTouchEnd,
    children: tooltipVisible && jsxRuntimeExports.jsx(components.View, {
      className: "ct-slider__tooltip",
      children: jsxRuntimeExports.jsx(components.Text, {
        children: val
      })
    })
  }, index);
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-slider', {
      'ct-slider--disabled': disabled
    }, className),
    children: [jsxRuntimeExports.jsxs(components.View, {
      className: "ct-slider__track",
      id: trackId.current,
      style: trackColor ? {
        backgroundColor: trackColor
      } : undefined,
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-slider__fill",
        style: trackStyle
      }), range ? jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
        children: [renderThumb((Array.isArray(value) ? value : [min, min])[0], 0), renderThumb((Array.isArray(value) ? value : [min, min])[1], 1)]
      }) : renderThumb(value, 0)]
    }), showMarks && marks && jsxRuntimeExports.jsx(components.View, {
      className: "ct-slider__marks",
      children: Object.entries(marks).map(([key, label]) => jsxRuntimeExports.jsx(components.View, {
        className: "ct-slider__mark",
        style: {
          left: `${getPercent(Number(key))}%`
        },
        children: jsxRuntimeExports.jsx(components.Text, {
          children: label
        })
      }, key))
    })]
  });
};

var css_248z$c = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-stepper {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  gap: 8rpx;\n}\n.ct-stepper__button {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  flex-shrink: 0;\n  background: var(--ct-color-fill-secondary);\n  border-radius: 8rpx;\n  color: var(--ct-color-text-primary);\n  -webkit-transition: all 150ms ease;\n  transition: all 150ms ease;\n}\n.ct-stepper__button:active, .ct-stepper__button.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-stepper__button--disabled {\n  color: var(--ct-color-text-disabled);\n  pointer-events: none;\n}\n.ct-stepper__input {\n  text-align: center;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  background: var(--ct-color-fill-secondary);\n  border-radius: 8rpx;\n  border: none;\n  box-sizing: border-box;\n}\n.ct-stepper__input:disabled {\n  color: var(--ct-color-text-disabled);\n}\n.ct-stepper--small .ct-stepper__input {\n  font-size: 26rpx;\n}\n.ct-stepper--large .ct-stepper__input {\n  font-size: 32rpx;\n}\n.ct-stepper--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-stepper--disabled .ct-stepper__button {\n  pointer-events: none;\n}";
styleInject(css_248z$c);

/**
 * Constants
 */
const sizeMap = {
  small: 56,
  medium: 64,
  large: 80
};
const Stepper = ({
  value: propValue,
  defaultValue = 1,
  min = 0,
  max = Infinity,
  step = 1,
  disabled = false,
  inputWidth,
  size = 'medium',
  disableInput = false,
  digits = 0,
  onChange,
  onOverflow,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange
  });
  /**
   * Render Helpers
   */
  const formatValue = require$$0.useCallback(val => {
    let result = Math.max(min, Math.min(max, val));
    if (digits > 0) {
      result = Number(result.toFixed(digits));
    } else {
      result = Math.round(result);
    }
    return result;
  }, [min, max, digits]);
  /**
   * Derived Values
   */
  const isMinDisabled = require$$0.useMemo(() => value <= min, [value, min]);
  const isMaxDisabled = require$$0.useMemo(() => value >= max, [value, max]);
  const buttonSize = sizeMap[size];
  const inputWidthValue = inputWidth ?? (size === 'small' ? 80 : size === 'large' ? 120 : 100);
  /**
   * Callbacks
   */
  const handleMinus = () => {
    if (disabled || isMinDisabled) {
      if (isMinDisabled) onOverflow?.('min');
      return;
    }
    const newValue = formatValue(value - step);
    setValue(newValue);
  };
  const handlePlus = () => {
    if (disabled || isMaxDisabled) {
      if (isMaxDisabled) onOverflow?.('max');
      return;
    }
    const newValue = formatValue(value + step);
    setValue(newValue);
  };
  const handleInputChange = e => {
    const inputValue = e.detail.value;
    if (inputValue === '' || inputValue === '-') return;
    const numValue = Number(inputValue);
    if (isNaN(numValue)) return;
    const newValue = formatValue(numValue);
    setValue(newValue);
  };
  const handleInputBlur = () => {
    const formattedValue = formatValue(value);
    if (formattedValue !== value) {
      setValue(formattedValue);
    }
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-stepper', `ct-stepper--${size}`, {
      'ct-stepper--disabled': disabled
    }, className),
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-stepper__button ct-stepper__minus', {
        'ct-stepper__button--disabled': isMinDisabled || disabled
      }),
      style: {
        width: `${buttonSize}rpx`,
        height: `${buttonSize}rpx`
      },
      onClick: handleMinus,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "minus",
        size: size === 'small' ? 28 : 32
      })
    }), jsxRuntimeExports.jsx(components.Input, {
      className: "ct-stepper__input",
      type: "digit",
      value: String(digits > 0 ? value.toFixed(digits) : value),
      disabled: disabled || disableInput,
      style: {
        width: typeof inputWidthValue === 'number' ? `${inputWidthValue}rpx` : inputWidthValue,
        height: `${buttonSize}rpx`
      },
      onInput: handleInputChange,
      onBlur: handleInputBlur
    }), jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-stepper__button ct-stepper__plus', {
        'ct-stepper__button--disabled': isMaxDisabled || disabled
      }),
      style: {
        width: `${buttonSize}rpx`,
        height: `${buttonSize}rpx`
      },
      onClick: handlePlus,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "plus",
        size: size === 'small' ? 28 : 32
      })
    })]
  });
};

var css_248z$b = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-steps {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n}\n.ct-steps--horizontal {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: normal;\n          flex-direction: row;\n}\n.ct-steps--horizontal .ct-steps__item {\n  -webkit-box-flex: 1;\n          flex: 1;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-steps--horizontal .ct-steps__line {\n  position: absolute;\n  top: 20rpx;\n  left: 50%;\n  right: -50%;\n  height: 2rpx;\n  background: var(--ct-color-border);\n}\n.ct-steps--horizontal .ct-steps__content {\n  margin-top: 16rpx;\n  text-align: center;\n}\n.ct-steps--vertical {\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n}\n.ct-steps--vertical .ct-steps__item {\n  -webkit-box-orient: horizontal;\n  -webkit-box-direction: normal;\n          flex-direction: row;\n  min-height: 120rpx;\n}\n.ct-steps--vertical .ct-steps__line {\n  position: absolute;\n  top: 48rpx;\n  left: 19rpx;\n  bottom: 0;\n  width: 2rpx;\n  background: var(--ct-color-border);\n}\n.ct-steps--vertical .ct-steps__content {\n  margin-left: 24rpx;\n  padding-bottom: 32rpx;\n}\n.ct-steps__item {\n  position: relative;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n}\n.ct-steps__item--finish .ct-steps__icon,\n.ct-steps__item--finish .ct-steps__dot {\n  background: var(--ct-color-primary);\n  border-color: var(--ct-color-primary);\n}\n.ct-steps__item--finish .ct-steps__line {\n  background: var(--ct-color-primary);\n}\n.ct-steps__item--finish .ct-steps__title {\n  color: var(--ct-color-text-primary);\n}\n.ct-steps__item--process .ct-steps__icon,\n.ct-steps__item--process .ct-steps__number {\n  background: var(--ct-color-primary);\n  border-color: var(--ct-color-primary);\n  color: #fff;\n}\n.ct-steps__item--process .ct-steps__dot {\n  background: var(--ct-color-primary);\n  border-color: var(--ct-color-primary);\n}\n.ct-steps__item--process .ct-steps__title {\n  color: var(--ct-color-text-primary);\n  font-weight: 500;\n}\n.ct-steps__item--wait .ct-steps__title {\n  color: var(--ct-color-text-tertiary);\n}\n.ct-steps__item--error .ct-steps__icon {\n  background: var(--ct-color-error);\n  border-color: var(--ct-color-error);\n}\n.ct-steps__item--error .ct-steps__title {\n  color: var(--ct-color-error);\n}\n.ct-steps__icon-wrapper {\n  position: relative;\n  z-index: 1;\n}\n.ct-steps__icon, .ct-steps__number {\n  width: 40rpx;\n  height: 40rpx;\n  border: 2rpx solid var(--ct-color-border);\n  border-radius: 50%;\n  background: var(--ct-color-bg-card);\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-steps__dot {\n  width: 16rpx;\n  height: 16rpx;\n  border-radius: 50%;\n  background: var(--ct-color-border);\n}\n.ct-steps--dot .ct-steps__icon-wrapper {\n  padding: 12rpx 0;\n}\n.ct-steps__title {\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n}\n.ct-steps__description {\n  margin-top: 8rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n}";
styleInject(css_248z$b);

/**
 * Helpers
 */
const getStepStatus = (index, current, itemStatus) => {
  if (itemStatus) return itemStatus;
  if (index < current) return 'finish';
  if (index === current) return 'process';
  return 'wait';
};
const Steps = ({
  current,
  items,
  direction = 'horizontal',
  dot = false,
  onChange,
  className
}) => {
  /**
   * Callbacks
   */
  const handleStepClick = index => {
    onChange?.(index);
  };
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-steps', `ct-steps--${direction}`, {
      'ct-steps--dot': dot
    }, className),
    children: items.map((item, index) => {
      const status = getStepStatus(index, current, item.status);
      const isLast = index === items.length - 1;
      return jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-steps__item', `ct-steps__item--${status}`),
        onClick: () => handleStepClick(index),
        children: [jsxRuntimeExports.jsx(components.View, {
          className: "ct-steps__icon-wrapper",
          children: dot ? jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__dot"
          }) : item.icon ? jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__icon",
            children: item.icon
          }) : status === 'finish' ? jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__icon",
            children: jsxRuntimeExports.jsx(Icon, {
              name: "check",
              size: 28,
              color: "#fff"
            })
          }) : status === 'error' ? jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__icon",
            children: jsxRuntimeExports.jsx(Icon, {
              name: "close",
              size: 28,
              color: "#fff"
            })
          }) : jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__number",
            children: jsxRuntimeExports.jsx(components.Text, {
              children: index + 1
            })
          })
        }), !isLast && jsxRuntimeExports.jsx(components.View, {
          className: "ct-steps__line"
        }), jsxRuntimeExports.jsxs(components.View, {
          className: "ct-steps__content",
          children: [jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__title",
            children: jsxRuntimeExports.jsx(components.Text, {
              children: item.title
            })
          }), item.description && jsxRuntimeExports.jsx(components.View, {
            className: "ct-steps__description",
            children: jsxRuntimeExports.jsx(components.Text, {
              children: item.description
            })
          })]
        })]
      }, index);
    })
  });
};

var css_248z$a = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-swipe-action {\n  position: relative;\n  overflow: hidden;\n  background: var(--ct-color-bg-card);\n}\n.ct-swipe-action__content {\n  position: relative;\n  z-index: 1;\n  background: var(--ct-color-bg-card);\n}\n.ct-swipe-action__actions {\n  position: absolute;\n  top: 0;\n  bottom: 0;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n}\n.ct-swipe-action__actions--left {\n  left: 0;\n  -webkit-transform: translateX(-100%);\n          transform: translateX(-100%);\n}\n.ct-swipe-action__actions--right {\n  right: 0;\n  -webkit-transform: translateX(100%);\n          transform: translateX(100%);\n}\n.ct-swipe-action__action {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  min-width: 160rpx;\n  padding: 0 32rpx;\n  color: var(--ct-color-text-inverse);\n}\n.ct-swipe-action__action:active, .ct-swipe-action__action.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-swipe-action__action-text {\n  font-size: 28rpx;\n  white-space: nowrap;\n}\n.ct-swipe-action--disabled .ct-swipe-action__content {\n  pointer-events: none;\n}";
styleInject(css_248z$a);

/**
 * Constants
 */
const SWIPE_THRESHOLD = 30; // 最小滑动距离
const ACTION_WIDTH = 80; // 单个操作按钮宽度 (px)
/**
 * Main Component
 */
const SwipeAction = ({
  leftActions = [],
  rightActions = [],
  closeOnAction = true,
  closeOnTouchOutside = true,
  disabled = false,
  onActionsReveal,
  onClose,
  children,
  className
}) => {
  /**
   * States
   */
  const [offset, setOffset] = require$$0.useState(0);
  const [side, setSide] = require$$0.useState(null);
  const [dragging, setDragging] = require$$0.useState(false);
  /**
   * Refs
   */
  const startOffsetRef = require$$0.useRef(0);
  /**
   * Derived Values
   */
  const leftActionsWidth = leftActions.length * ACTION_WIDTH;
  const rightActionsWidth = rightActions.length * ACTION_WIDTH;
  /**
   * Hooks
   */
  const touch = useTouch({
    lockDirection: true
  });
  /**
   * Callbacks
   */
  const close = require$$0.useCallback(() => {
    setOffset(0);
    setSide(null);
    onClose?.();
  }, [onClose]);
  const handleTouchStart = e => {
    if (disabled) return;
    touch.onTouchStart(e);
    startOffsetRef.current = offset;
    setDragging(true);
  };
  const handleTouchMove = e => {
    if (disabled) return;
    touch.onTouchMove(e);
    // 只处理水平滑动
    if (touch.state.direction === 'vertical') return;
    const deltaX = touch.state.deltaX;
    let newOffset = startOffsetRef.current + deltaX;
    // 限制滑动范围
    if (newOffset > 0) {
      // 向右滑，显示左侧操作
      newOffset = leftActions.length > 0 ? Math.min(newOffset, leftActionsWidth) : 0;
    } else {
      // 向左滑，显示右侧操作
      newOffset = rightActions.length > 0 ? Math.max(newOffset, -rightActionsWidth) : 0;
    }
    setOffset(newOffset);
  };
  const handleTouchEnd = () => {
    if (disabled) return;
    touch.onTouchEnd();
    setDragging(false);
    // 根据滑动距离决定是否展开
    if (offset > SWIPE_THRESHOLD && leftActions.length > 0) {
      setOffset(leftActionsWidth);
      setSide('left');
      onActionsReveal?.('left');
    } else if (offset < -SWIPE_THRESHOLD && rightActions.length > 0) {
      setOffset(-rightActionsWidth);
      setSide('right');
      onActionsReveal?.('right');
    } else {
      close();
    }
  };
  const handleActionClick = action => {
    action.onClick?.();
    if (closeOnAction) {
      close();
    }
  };
  const handleContentClick = () => {
    if (side && closeOnTouchOutside) {
      close();
    }
  };
  /**
   * Helpers
   */
  const getActionColor = color => {
    if (!color) return 'var(--ct-color-primary)';
    const colorMap = {
      primary: 'var(--ct-color-primary)',
      success: 'var(--ct-color-success)',
      warning: 'var(--ct-color-warning)',
      danger: 'var(--ct-color-error)'
    };
    return colorMap[color] || color;
  };
  /**
   * Render Helpers
   */
  const renderActions = (actions, position) => {
    if (actions.length === 0) return null;
    return jsxRuntimeExports.jsx(components.View, {
      className: `ct-swipe-action__actions ct-swipe-action__actions--${position}`,
      children: actions.map(action => jsxRuntimeExports.jsx(components.View, {
        className: "ct-swipe-action__action",
        style: {
          backgroundColor: getActionColor(action.color)
        },
        onClick: () => handleActionClick(action),
        children: jsxRuntimeExports.jsx(components.Text, {
          className: "ct-swipe-action__action-text",
          children: action.text
        })
      }, action.key))
    });
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-swipe-action', {
      'ct-swipe-action--disabled': disabled,
      'ct-swipe-action--open': side !== null
    }, className),
    children: [renderActions(leftActions, 'left'), renderActions(rightActions, 'right'), jsxRuntimeExports.jsx(components.View, {
      className: "ct-swipe-action__content",
      style: {
        transform: `translateX(${offset}px)`,
        transition: !dragging ? 'transform 0.25s ease' : 'none'
      },
      onClick: handleContentClick,
      onTouchStart: handleTouchStart,
      onTouchMove: handleTouchMove,
      onTouchEnd: handleTouchEnd,
      onTouchCancel: handleTouchEnd,
      children: children
    })]
  });
};

var css_248z$9 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-swiper {\n  position: relative;\n}\n.ct-swiper__container {\n  width: 100%;\n  height: 100%;\n}\n.ct-swiper__indicator {\n  position: absolute;\n  bottom: 24rpx;\n  left: 50%;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n}\n.ct-swiper__indicator--vertical {\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  top: 50%;\n  left: auto;\n  right: 24rpx;\n  bottom: auto;\n  -webkit-transform: translateY(-50%);\n          transform: translateY(-50%);\n}\n.ct-swiper__dot {\n  width: 12rpx;\n  height: 12rpx;\n  border-radius: 50%;\n  margin: 0 6rpx;\n  -webkit-transition: all 150ms;\n  transition: all 150ms;\n}\n.ct-swiper__dot--active {\n  width: 24rpx;\n  border-radius: 6rpx;\n}\n.ct-swiper__indicator--vertical .ct-swiper__dot {\n  margin: 6rpx 0;\n}\n.ct-swiper__indicator--vertical .ct-swiper__dot--active {\n  width: 12rpx;\n  height: 24rpx;\n}\n\n.ct-swiper-item {\n  width: 100%;\n  height: 100%;\n}";
styleInject(css_248z$9);

/**
 * Sub Components
 */
const SwiperItem = ({
  children,
  className
}) => {
  return jsxRuntimeExports.jsx(components.SwiperItem, {
    className: classNames('ct-swiper-item', className),
    children: children
  });
};
/**
 * Main Component
 */
const Swiper = ({
  current: propCurrent,
  defaultCurrent = 0,
  autoplay = false,
  interval = 5000,
  duration = 500,
  loop = true,
  vertical = false,
  showIndicator = true,
  indicatorColor = 'rgba(0, 0, 0, 0.3)',
  indicatorActiveColor = 'var(--ct-color-primary)',
  onChange,
  children,
  className
}) => {
  /**
   * Hooks
   */
  const [current, setCurrent] = useControllable({
    value: propCurrent,
    defaultValue: defaultCurrent,
    onChange
  });
  /**
   * Derived Values
   */
  const count = require$$0.Children.toArray(children).filter(require$$0.isValidElement).length;
  /**
   * Callbacks
   */
  const handleChange = event => {
    setCurrent(event.detail.current);
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-swiper', className),
    children: [jsxRuntimeExports.jsx(components.Swiper, {
      className: "ct-swiper__container",
      current: current,
      autoplay: autoplay,
      interval: interval,
      duration: duration,
      circular: loop,
      vertical: vertical,
      indicatorDots: false,
      onChange: handleChange,
      children: children
    }), showIndicator && count > 1 && jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-swiper__indicator', {
        'ct-swiper__indicator--vertical': vertical
      }),
      children: Array.from({
        length: count
      }).map((_, index) => jsxRuntimeExports.jsx(components.View, {
        className: classNames('ct-swiper__dot', {
          'ct-swiper__dot--active': index === current
        }),
        style: {
          backgroundColor: index === current ? indicatorActiveColor : indicatorColor
        }
      }, index))
    })]
  });
};
Swiper.Item = SwiperItem;

var css_248z$8 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-switch {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  position: relative;\n}\n.ct-switch:active, .ct-switch.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-switch--sm .ct-switch__track {\n  width: 72rpx;\n  height: 36rpx;\n  border-radius: 18rpx;\n}\n.ct-switch--sm .ct-switch__thumb {\n  width: 28rpx;\n  height: 28rpx;\n  top: 4rpx;\n  left: 4rpx;\n}\n.ct-switch--sm.ct-switch--checked .ct-switch__thumb {\n  left: 40rpx;\n}\n.ct-switch--md .ct-switch__track {\n  width: 96rpx;\n  height: 48rpx;\n  border-radius: 24rpx;\n}\n.ct-switch--md .ct-switch__thumb {\n  width: 40rpx;\n  height: 40rpx;\n  top: 4rpx;\n  left: 4rpx;\n}\n.ct-switch--md.ct-switch--checked .ct-switch__thumb {\n  left: 52rpx;\n}\n.ct-switch--lg .ct-switch__track {\n  width: 120rpx;\n  height: 60rpx;\n  border-radius: 30rpx;\n}\n.ct-switch--lg .ct-switch__thumb {\n  width: 52rpx;\n  height: 52rpx;\n  top: 4rpx;\n  left: 4rpx;\n}\n.ct-switch--lg.ct-switch--checked .ct-switch__thumb {\n  left: 64rpx;\n}\n.ct-switch__track {\n  background: var(--ct-color-fill-tertiary);\n  -webkit-transition: background-color 250ms ease;\n  transition: background-color 250ms ease;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 0 16rpx;\n}\n.ct-switch--textless .ct-switch__track {\n  padding: 0;\n}\n.ct-switch__text {\n  font-size: 22rpx;\n  color: #fff;\n  white-space: nowrap;\n}\n.ct-switch__thumb {\n  position: absolute;\n  background: #fff;\n  border-radius: 50%;\n  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.15);\n  -webkit-transition: left 250ms ease;\n  transition: left 250ms ease;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-switch--checked .ct-switch__track {\n  background: var(--ct-color-primary);\n}\n.ct-switch--checked .ct-switch__text {\n  margin-right: auto;\n}\n.ct-switch:not(.ct-switch--checked) .ct-switch__text {\n  margin-left: auto;\n}\n.ct-switch--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-switch--loading {\n  pointer-events: none;\n}";
styleInject(css_248z$8);

const Switch = ({
  checked: propChecked,
  defaultChecked = false,
  disabled = false,
  loading = false,
  size = 'md',
  checkedColor,
  uncheckedColor,
  checkedText,
  uncheckedText,
  onChange,
  className
}) => {
  /**
   * Hooks
   */
  const [checked, setChecked] = useControllable({
    value: propChecked,
    defaultValue: defaultChecked,
    onChange
  });
  /**
   * Derived Values
   */
  const isDisabled = disabled || loading;
  const trackStyle = checked ? checkedColor ? {
    backgroundColor: checkedColor
  } : undefined : uncheckedColor ? {
    backgroundColor: uncheckedColor
  } : undefined;
  const hasText = !!(checkedText || uncheckedText);
  /**
   * Callbacks
   */
  const handleClick = () => {
    if (isDisabled) return;
    setChecked(!checked);
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-switch', `ct-switch--${size}`, {
      'ct-switch--checked': checked,
      'ct-switch--disabled': isDisabled,
      'ct-switch--loading': loading,
      'ct-switch--textless': !hasText
    }, className),
    hoverClass: isDisabled ? 'none' : 'ct-clickable--hover',
    onClick: handleClick,
    children: [jsxRuntimeExports.jsx(components.View, {
      className: "ct-switch__track",
      style: trackStyle,
      children: hasText && jsxRuntimeExports.jsx(components.Text, {
        className: "ct-switch__text",
        children: checked ? checkedText : uncheckedText
      })
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-switch__thumb",
      children: loading && jsxRuntimeExports.jsx(Loading, {
        type: "spinner",
        size: "sm"
      })
    })]
  });
};

var css_248z$7 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-tabbar {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  background: var(--ct-color-bg-card);\n}\n.ct-tabbar--fixed {\n  position: fixed;\n  bottom: 0;\n  left: 0;\n  right: 0;\n  z-index: 300;\n}\n.ct-tabbar--bordered {\n  position: relative;\n}\n.ct-tabbar--bordered::after {\n  content: \"\";\n  position: absolute;\n  pointer-events: none;\n  box-sizing: border-box;\n  left: 0;\n  right: 0;\n  top: 0;\n  height: 1px;\n  background: var(--ct-color-border);\n  -webkit-transform: scaleY(0.5);\n          transform: scaleY(0.5);\n  -webkit-transform-origin: top;\n          transform-origin: top;\n}\n.ct-tabbar--safe-area {\n  padding-bottom: calc(0 + constant(safe-area-inset-bottom));\n  padding-bottom: calc(0 + env(safe-area-inset-bottom));\n}\n.ct-tabbar__item {\n  -webkit-box-flex: 1;\n          flex: 1;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  padding: 16rpx 0;\n}\n.ct-tabbar__item:active, .ct-tabbar__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-tabbar__icon {\n  position: relative;\n  font-size: 0;\n}\n.ct-tabbar__badge {\n  position: absolute;\n  top: -8rpx;\n  right: -16rpx;\n}\n.ct-tabbar__title {\n  margin-top: 8rpx;\n  font-size: 22rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-tabbar__item--active .ct-tabbar__title {\n  color: var(--ct-color-primary);\n}\n.ct-tabbar__placeholder {\n  height: 100rpx;\n}\n.ct-tabbar__placeholder--safe-area {\n  padding-bottom: calc(0 + constant(safe-area-inset-bottom));\n  padding-bottom: calc(0 + env(safe-area-inset-bottom));\n}";
styleInject(css_248z$7);

const TabBar = ({
  activeKey: propActiveKey,
  defaultActiveKey,
  items,
  onChange,
  fixed = true,
  bordered = true,
  safeArea = true,
  className
}) => {
  /**
   * Hooks
   */
  const [activeKey, setActiveKey] = useControllable({
    value: propActiveKey,
    defaultValue: defaultActiveKey || items[0]?.key,
    onChange
  });
  /**
   * Callbacks
   */
  const handleItemClick = item => {
    setActiveKey(item.key);
  };
  return jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
    children: [jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-tabbar', {
        'ct-tabbar--fixed': fixed,
        'ct-tabbar--bordered': bordered,
        'ct-tabbar--safe-area': safeArea
      }, className),
      children: items.map(item => {
        const isActive = activeKey === item.key;
        return jsxRuntimeExports.jsxs(components.View, {
          className: classNames('ct-tabbar__item', {
            'ct-tabbar__item--active': isActive
          }),
          onClick: () => handleItemClick(item),
          children: [jsxRuntimeExports.jsxs(components.View, {
            className: "ct-tabbar__icon",
            children: [isActive && item.activeIcon ? item.activeIcon : item.icon, (item.badge !== undefined || item.dot) && jsxRuntimeExports.jsx(Badge, {
              content: item.badge,
              dot: item.dot,
              className: "ct-tabbar__badge"
            })]
          }), jsxRuntimeExports.jsx(components.Text, {
            className: "ct-tabbar__title",
            children: item.title
          })]
        }, item.key);
      })
    }), fixed && jsxRuntimeExports.jsx(components.View, {
      className: classNames('ct-tabbar__placeholder', {
        'ct-tabbar__placeholder--safe-area': safeArea
      })
    })]
  });
};

var css_248z$6 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-table {\n  position: relative;\n  width: 100%;\n  background: var(--ct-color-bg-container);\n  border-radius: 16rpx;\n  overflow: hidden;\n}\n.ct-table__scroll {\n  width: 100%;\n  -webkit-overflow-scrolling: touch;\n}\n.ct-table__header {\n  background: var(--ct-color-bg-secondary);\n  border-bottom: 2rpx solid var(--ct-color-border);\n}\n.ct-table__header-row {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: stretch;\n          align-items: stretch;\n}\n.ct-table__header-cell {\n  flex-shrink: 0;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n  font-size: 26rpx;\n  font-weight: 500;\n  color: var(--ct-color-text-secondary);\n  background: inherit;\n  box-sizing: border-box;\n}\n.ct-table__header-cell--left {\n  -webkit-box-pack: start;\n          justify-content: flex-start;\n}\n.ct-table__header-cell--center {\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-table__header-cell--right {\n  -webkit-box-pack: end;\n          justify-content: flex-end;\n}\n.ct-table__header-cell--selection, .ct-table__header-cell--expand {\n  width: 80rpx;\n  min-width: 80rpx;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-table__header-cell--fixed {\n  position: -webkit-sticky;\n  position: sticky;\n  z-index: 2;\n  background: var(--ct-color-bg-secondary);\n}\n.ct-table__header-cell--fixed-left {\n  left: 0;\n  box-shadow: 4rpx 0 8rpx rgba(0, 0, 0, 0.05);\n}\n.ct-table__header-cell--fixed-right {\n  right: 0;\n  box-shadow: -4rpx 0 8rpx rgba(0, 0, 0, 0.05);\n}\n.ct-table__body {\n  min-height: 100rpx;\n}\n.ct-table__row {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: stretch;\n          align-items: stretch;\n  border-bottom: 2rpx solid var(--ct-color-border);\n  -webkit-transition: background-color 150ms ease;\n  transition: background-color 150ms ease;\n}\n.ct-table__row:last-child {\n  border-bottom: none;\n}\n.ct-table__row--striped {\n  background: var(--ct-color-bg-secondary);\n}\n.ct-table__row--selected {\n  background: var(--ct-color-primary-light);\n}\n.ct-table__row--clickable {\n  cursor: pointer;\n}\n.ct-table__row--clickable:active {\n  background: var(--ct-color-bg-hover);\n}\n.ct-table__cell {\n  flex-shrink: 0;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 24rpx 32rpx;\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  background: inherit;\n  box-sizing: border-box;\n  word-break: break-all;\n}\n.ct-table__cell--left {\n  -webkit-box-pack: start;\n          justify-content: flex-start;\n}\n.ct-table__cell--center {\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-table__cell--right {\n  -webkit-box-pack: end;\n          justify-content: flex-end;\n}\n.ct-table__cell--selection, .ct-table__cell--expand {\n  width: 80rpx;\n  min-width: 80rpx;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-table__cell--fixed {\n  position: -webkit-sticky;\n  position: sticky;\n  z-index: 1;\n  background: var(--ct-color-bg-container);\n}\n.ct-table__cell--fixed-left {\n  left: 0;\n  box-shadow: 4rpx 0 8rpx rgba(0, 0, 0, 0.05);\n}\n.ct-table__cell--fixed-right {\n  right: 0;\n  box-shadow: -4rpx 0 8rpx rgba(0, 0, 0, 0.05);\n}\n.ct-table__expand-icon {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  width: 48rpx;\n  height: 48rpx;\n  color: var(--ct-color-text-tertiary);\n  -webkit-transition: -webkit-transform 150ms ease;\n  transition: -webkit-transform 150ms ease;\n  transition: transform 150ms ease;\n  transition: transform 150ms ease, -webkit-transform 150ms ease;\n}\n.ct-table__expand-icon--expanded {\n  -webkit-transform: rotate(90deg);\n          transform: rotate(90deg);\n}\n.ct-table__expanded-row {\n  padding: 32rpx;\n  background: var(--ct-color-bg-secondary);\n  border-bottom: 2rpx solid var(--ct-color-border);\n}\n.ct-table__empty {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  padding: 64rpx 32rpx;\n}\n.ct-table__empty-text {\n  font-size: 28rpx;\n  color: var(--ct-color-text-tertiary);\n}\n.ct-table__loading {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  background: var(--ct-color-bg-overlay, rgba(255, 255, 255, 0.7));\n  z-index: 10;\n}\n.ct-table--sm .ct-table__header-cell,\n.ct-table--sm .ct-table__cell {\n  padding: 16rpx 24rpx;\n  font-size: 26rpx;\n}\n.ct-table--sm .ct-table__header-cell--selection,\n.ct-table--sm .ct-table__header-cell--expand,\n.ct-table--sm .ct-table__cell--selection,\n.ct-table--sm .ct-table__cell--expand {\n  width: 64rpx;\n  min-width: 64rpx;\n}\n.ct-table--lg .ct-table__header-cell,\n.ct-table--lg .ct-table__cell {\n  padding: 32rpx 40rpx;\n  font-size: 32rpx;\n}\n.ct-table--lg .ct-table__header-cell--selection,\n.ct-table--lg .ct-table__header-cell--expand,\n.ct-table--lg .ct-table__cell--selection,\n.ct-table--lg .ct-table__cell--expand {\n  width: 96rpx;\n  min-width: 96rpx;\n}\n.ct-table--bordered {\n  border: 2rpx solid var(--ct-color-border);\n}\n.ct-table--bordered .ct-table__header-cell,\n.ct-table--bordered .ct-table__cell {\n  border-right: 2rpx solid var(--ct-color-border);\n}\n.ct-table--bordered .ct-table__header-cell:last-child,\n.ct-table--bordered .ct-table__cell:last-child {\n  border-right: none;\n}\n.ct-table--sticky .ct-table__header {\n  position: -webkit-sticky;\n  position: sticky;\n  top: 0;\n  z-index: 3;\n}\n.ct-table--loading .ct-table__scroll {\n  overflow: hidden;\n}";
styleInject(css_248z$6);

/**
 * Helpers
 */
const getRowKey = (record, rowKey, index) => {
  if (typeof rowKey === 'function') {
    return rowKey(record);
  }
  if (typeof rowKey === 'string' && record) {
    return String(record[rowKey]);
  }
  return String(index);
};
const getCellValue = (record, column, index) => {
  if (column.render) {
    const value = column.dataIndex ? record[column.dataIndex] : record;
    return column.render(value, record, index);
  }
  if (column.dataIndex) {
    return record[column.dataIndex];
  }
  return null;
};
/**
 * Main Component
 */
const Table = ({
  columns,
  dataSource,
  rowKey = 'id',
  size = 'md',
  bordered = false,
  stickyHeader = false,
  stickyTop = 0,
  expandable,
  rowSelection,
  emptyText = '暂无数据',
  loading = false,
  onRowClick,
  className,
  style,
  maxHeight,
  striped = false
}) => {
  /**
   * States
   */
  const [innerExpandedKeys, setInnerExpandedKeys] = require$$0.useState(expandable?.defaultExpandedRowKeys || []);
  const [innerSelectedKeys, setInnerSelectedKeys] = require$$0.useState([]);
  /**
   * Derived Values
   */
  const expandedKeys = expandable?.expandedRowKeys ?? innerExpandedKeys;
  const selectedKeys = rowSelection?.selectedRowKeys ?? innerSelectedKeys;
  const {
    fixedLeftColumns,
    fixedRightColumns,
    scrollColumns
  } = require$$0.useMemo(() => {
    const left = [];
    const right = [];
    const scroll = [];
    columns.forEach(col => {
      if (col.fixed === 'left') {
        left.push(col);
      } else if (col.fixed === 'right') {
        right.push(col);
      } else {
        scroll.push(col);
      }
    });
    return {
      fixedLeftColumns: left,
      fixedRightColumns: right,
      scrollColumns: scroll
    };
  }, [columns]);
  const hasFixedLeft = fixedLeftColumns.length > 0;
  const hasFixedRight = fixedRightColumns.length > 0;
  const {
    isAllSelected,
    isIndeterminate
  } = require$$0.useMemo(() => {
    if (!rowSelection || rowSelection.type === 'radio') {
      return {
        isAllSelected: false,
        isIndeterminate: false
      };
    }
    const selectableKeys = dataSource.map((record, index) => {
      const checkboxProps = rowSelection?.getCheckboxProps?.(record);
      if (checkboxProps?.disabled) return null;
      return getRowKey(record, rowKey, index);
    }).filter(Boolean);
    const selectedCount = selectableKeys.filter(k => selectedKeys.includes(k)).length;
    return {
      isAllSelected: selectableKeys.length > 0 && selectedCount === selectableKeys.length,
      isIndeterminate: selectedCount > 0 && selectedCount < selectableKeys.length
    };
  }, [dataSource, selectedKeys, rowSelection, rowKey]);
  const tableStyle = {
    ...style,
    ...(maxHeight ? {
      maxHeight: typeof maxHeight === 'number' ? `${maxHeight}rpx` : maxHeight
    } : {})
  };
  /**
   * Callbacks
   */
  const handleExpand = require$$0.useCallback((record, key) => {
    const newKeys = expandedKeys.includes(key) ? expandedKeys.filter(k => k !== key) : [...expandedKeys, key];
    if (!expandable?.expandedRowKeys) {
      setInnerExpandedKeys(newKeys);
    }
    expandable?.onExpandChange?.(newKeys, record);
  }, [expandedKeys, expandable]);
  const handleSelect = require$$0.useCallback((record, key, checked) => {
    let newKeys;
    if (rowSelection?.type === 'radio') {
      newKeys = checked ? [key] : [];
    } else {
      newKeys = checked ? [...selectedKeys, key] : selectedKeys.filter(k => k !== key);
    }
    if (!rowSelection?.selectedRowKeys) {
      setInnerSelectedKeys(newKeys);
    }
    const selectedRows = dataSource.filter((item, idx) => newKeys.includes(getRowKey(item, rowKey, idx)));
    rowSelection?.onChange?.(newKeys, selectedRows);
  }, [selectedKeys, rowSelection, dataSource, rowKey]);
  const handleSelectAll = require$$0.useCallback(checked => {
    const allKeys = dataSource.map((record, index) => {
      const checkboxProps = rowSelection?.getCheckboxProps?.(record);
      if (checkboxProps?.disabled) return null;
      return getRowKey(record, rowKey, index);
    }).filter(Boolean);
    const newKeys = checked ? allKeys : [];
    if (!rowSelection?.selectedRowKeys) {
      setInnerSelectedKeys(newKeys);
    }
    const selectedRows = checked ? dataSource.filter((_, idx) => {
      const checkboxProps = rowSelection?.getCheckboxProps?.(dataSource[idx]);
      return !checkboxProps?.disabled;
    }) : [];
    rowSelection?.onChange?.(newKeys, selectedRows);
  }, [dataSource, rowKey, rowSelection]);
  /**
   * Render Helpers
   */
  const renderHeaderCell = (column, isFixed) => jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-table__header-cell', `ct-table__header-cell--${column.align || 'left'}`, {
      'ct-table__header-cell--fixed': isFixed,
      [`ct-table__header-cell--fixed-${isFixed}`]: isFixed
    }, column.className),
    style: {
      width: column.width
    },
    children: column.title
  }, column.key);
  const renderCell = (record, column, index, isFixed) => jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-table__cell', `ct-table__cell--${column.align || 'left'}`, {
      'ct-table__cell--fixed': isFixed,
      [`ct-table__cell--fixed-${isFixed}`]: isFixed
    }, column.className),
    style: {
      width: column.width
    },
    children: getCellValue(record, column, index)
  }, column.key);
  const renderRow = (record, index) => {
    const key = getRowKey(record, rowKey, index);
    const isExpanded = expandedKeys.includes(key);
    const isSelected = selectedKeys.includes(key);
    const checkboxProps = rowSelection?.getCheckboxProps?.(record);
    return jsxRuntimeExports.jsxs(require$$0.Fragment, {
      children: [jsxRuntimeExports.jsxs(components.View, {
        className: classNames('ct-table__row', {
          'ct-table__row--striped': striped && index % 2 === 1,
          'ct-table__row--selected': isSelected,
          'ct-table__row--clickable': !!onRowClick
        }),
        onClick: () => onRowClick?.(record, index),
        children: [rowSelection && jsxRuntimeExports.jsx(components.View, {
          className: "ct-table__cell ct-table__cell--selection",
          children: rowSelection.type === 'radio' ? jsxRuntimeExports.jsx(Radio, {
            checked: isSelected,
            disabled: checkboxProps?.disabled,
            onChange: checked => handleSelect(record, key, checked)
          }) : jsxRuntimeExports.jsx(Checkbox, {
            checked: isSelected,
            disabled: checkboxProps?.disabled,
            onChange: checked => handleSelect(record, key, checked)
          })
        }), expandable?.expandedRowRender && jsxRuntimeExports.jsx(components.View, {
          className: "ct-table__cell ct-table__cell--expand",
          onClick: e => {
            e.stopPropagation();
            handleExpand(record, key);
          },
          children: expandable.expandIcon ? expandable.expandIcon({
            expanded: isExpanded,
            record
          }) : jsxRuntimeExports.jsx(components.View, {
            className: classNames('ct-table__expand-icon', {
              'ct-table__expand-icon--expanded': isExpanded
            }),
            children: jsxRuntimeExports.jsx(Icon, {
              name: "chevron-right",
              size: 32
            })
          })
        }), fixedLeftColumns.map(col => renderCell(record, col, index, 'left')), scrollColumns.map(col => renderCell(record, col, index)), fixedRightColumns.map(col => renderCell(record, col, index, 'right'))]
      }), expandable?.expandedRowRender && isExpanded && jsxRuntimeExports.jsx(components.View, {
        className: "ct-table__expanded-row",
        children: expandable.expandedRowRender(record, index)
      })]
    }, key);
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-table', `ct-table--${size}`, {
      'ct-table--bordered': bordered,
      'ct-table--sticky': stickyHeader,
      'ct-table--loading': loading,
      'ct-table--has-fixed-left': hasFixedLeft,
      'ct-table--has-fixed-right': hasFixedRight
    }, className),
    style: tableStyle,
    children: [loading && jsxRuntimeExports.jsx(components.View, {
      className: "ct-table__loading",
      children: jsxRuntimeExports.jsx(Loading, {
        size: "md"
      })
    }), jsxRuntimeExports.jsxs(components.ScrollView, {
      className: "ct-table__scroll",
      scrollX: true,
      scrollY: !!maxHeight,
      enhanced: true,
      showScrollbar: false,
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-table__header",
        style: stickyHeader ? {
          top: `${stickyTop}rpx`
        } : undefined,
        children: jsxRuntimeExports.jsxs(components.View, {
          className: "ct-table__header-row",
          children: [rowSelection && jsxRuntimeExports.jsx(components.View, {
            className: "ct-table__header-cell ct-table__header-cell--selection",
            children: rowSelection.type !== 'radio' && jsxRuntimeExports.jsx(Checkbox, {
              checked: isAllSelected,
              indeterminate: isIndeterminate,
              onChange: handleSelectAll
            })
          }), expandable?.expandedRowRender && jsxRuntimeExports.jsx(components.View, {
            className: "ct-table__header-cell ct-table__header-cell--expand"
          }), fixedLeftColumns.map(col => renderHeaderCell(col, 'left')), scrollColumns.map(col => renderHeaderCell(col)), fixedRightColumns.map(col => renderHeaderCell(col, 'right'))]
        })
      }), jsxRuntimeExports.jsx(components.View, {
        className: "ct-table__body",
        children: dataSource.length > 0 ? dataSource.map((record, index) => renderRow(record, index)) : jsxRuntimeExports.jsx(components.View, {
          className: "ct-table__empty",
          children: jsxRuntimeExports.jsx(components.Text, {
            className: "ct-table__empty-text",
            children: emptyText
          })
        })
      })]
    })]
  });
};

var css_248z$5 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-tabs__header, .ct-tabs__scroll {\n  background: var(--ct-color-bg-card);\n}\n.ct-tabs__scroll {\n  white-space: nowrap;\n}\n.ct-tabs__scroll ::-webkit-scrollbar {\n  display: none;\n}\n.ct-tabs__nav {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  position: relative;\n}\n.ct-tabs__item {\n  -webkit-box-flex: 1;\n          flex: 1;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  padding: 24rpx 32rpx;\n}\n.ct-tabs__item:active, .ct-tabs__item.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-tabs__item--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n}\n.ct-tabs__item-text {\n  font-size: 28rpx;\n  color: var(--ct-color-text-secondary);\n  -webkit-transition: color 150ms ease;\n  transition: color 150ms ease;\n}\n.ct-tabs__item--active .ct-tabs__item-text {\n  color: var(--ct-color-primary);\n  font-weight: 500;\n}\n.ct-tabs__badge {\n  margin-left: 8rpx;\n}\n.ct-tabs__indicator {\n  position: absolute;\n  bottom: 0;\n  left: 0;\n  height: 4rpx;\n  background: var(--ct-color-primary);\n  border-radius: 2rpx;\n  -webkit-transition: -webkit-transform 250ms ease;\n  transition: -webkit-transform 250ms ease;\n  transition: transform 250ms ease;\n  transition: transform 250ms ease, -webkit-transform 250ms ease;\n}\n.ct-tabs__content {\n  padding: 32rpx;\n}\n.ct-tabs--capsule .ct-tabs__nav {\n  padding: 8rpx;\n  background: var(--ct-color-fill-secondary);\n  border-radius: 24rpx;\n}\n.ct-tabs--capsule .ct-tabs__item {\n  border-radius: 16rpx;\n  -webkit-transition: background-color 150ms ease;\n  transition: background-color 150ms ease;\n}\n.ct-tabs--capsule .ct-tabs__item--active {\n  background: var(--ct-color-bg-card);\n  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);\n}\n.ct-tabs--capsule .ct-tabs__item--active .ct-tabs__item-text {\n  color: var(--ct-color-primary);\n}\n.ct-tabs--capsule .ct-tabs__item-text {\n  color: var(--ct-color-text-secondary);\n}\n.ct-tabs--card .ct-tabs__nav {\n  gap: 16rpx;\n}\n.ct-tabs--card .ct-tabs__item {\n  -webkit-box-flex: 0;\n          flex: none;\n  padding: 16rpx 32rpx;\n  background: var(--ct-color-fill-secondary);\n  border-radius: 16rpx 16rpx 0 0;\n  -webkit-transition: background-color 150ms ease, border-color 150ms ease;\n  transition: background-color 150ms ease, border-color 150ms ease;\n}\n.ct-tabs--card .ct-tabs__item--active {\n  background: var(--ct-color-bg-card);\n  border: 2rpx solid var(--ct-color-border);\n  border-bottom-color: var(--ct-color-bg-card);\n  margin-bottom: -2rpx;\n}\n.ct-tabs--card .ct-tabs__item--active .ct-tabs__item-text {\n  color: var(--ct-color-primary);\n}\n.ct-tabs--card .ct-tabs__item-text {\n  color: var(--ct-color-text-secondary);\n}\n.ct-tabs--card .ct-tabs__content {\n  border: 2rpx solid var(--ct-color-border);\n  border-radius: 0 16rpx 16rpx 16rpx;\n}";
styleInject(css_248z$5);

/**
 * Sub Components
 */
const TabPanel = ({
  children,
  className
}) => {
  return jsxRuntimeExports.jsx(components.View, {
    className: classNames('ct-tabs__panel', className),
    children: children
  });
};
/**
 * Main Component
 */
const Tabs = ({
  activeKey: propActiveKey,
  defaultActiveKey,
  variant = 'line',
  items,
  onChange,
  scrollable = false,
  showIndicator = true,
  indicatorWidth = 'auto',
  className,
  children
}) => {
  /**
   * Hooks
   */
  const [activeKey, setActiveKey] = useControllable({
    value: propActiveKey,
    defaultValue: defaultActiveKey ?? items[0]?.key,
    onChange
  });
  /**
   * States
   */
  const [indicatorStyle, setIndicatorStyle] = require$$0.useState({});
  /**
   * Derived Values
   */
  const shouldShowIndicator = showIndicator && variant === 'line';
  const activePanel = require$$0.Children.toArray(children).find(child => {
    if (/*#__PURE__*/require$$0.isValidElement(child) && child.props.tabKey === activeKey) {
      return true;
    }
    return false;
  });
  /**
   * Callbacks
   */
  const handleTabClick = item => {
    if (item.disabled) return;
    setActiveKey(item.key);
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (!shouldShowIndicator || !activeKey || !items.length) return;
    Taro.nextTick(() => {
      const query = Taro.createSelectorQuery();
      query.select(`.ct-tabs__item--active`).boundingClientRect();
      query.select(`.ct-tabs__nav`).boundingClientRect();
      query.exec(res => {
        if (res[0] && res[1]) {
          const tabRect = res[0];
          const navRect = res[1];
          const left = tabRect.left - navRect.left;
          const width = indicatorWidth === 'auto' ? tabRect.width : indicatorWidth;
          setIndicatorStyle({
            width: typeof width === 'number' ? `${width}px` : width,
            transform: `translateX(${left}px)`
          });
        }
      });
    });
  }, [activeKey, indicatorWidth, shouldShowIndicator]);
  /**
   * Render Helpers
   */
  const NavContent = jsxRuntimeExports.jsxs(components.View, {
    className: "ct-tabs__nav",
    children: [items.map(item => jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-tabs__item', {
        'ct-tabs__item--active': activeKey === item.key,
        'ct-tabs__item--disabled': item.disabled
      }),
      hoverClass: item.disabled ? 'none' : 'ct-clickable--hover',
      onClick: () => handleTabClick(item),
      children: [jsxRuntimeExports.jsx(components.Text, {
        className: "ct-tabs__item-text",
        children: item.title
      }), item.badge !== undefined && jsxRuntimeExports.jsx(Badge, {
        content: item.badge,
        className: "ct-tabs__badge"
      })]
    }, item.key)), shouldShowIndicator && jsxRuntimeExports.jsx(components.View, {
      className: "ct-tabs__indicator",
      style: indicatorStyle
    })]
  });
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-tabs', `ct-tabs--${variant}`, className),
    children: [scrollable ? jsxRuntimeExports.jsx(components.ScrollView, {
      scrollX: true,
      className: "ct-tabs__scroll",
      children: NavContent
    }) : jsxRuntimeExports.jsx(components.View, {
      className: "ct-tabs__header",
      children: NavContent
    }), jsxRuntimeExports.jsx(components.View, {
      className: "ct-tabs__content",
      children: activePanel
    })]
  });
};
Tabs.Panel = TabPanel;

var css_248z$4 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-tag {\n  display: -webkit-inline-box;\n  display: -ms-inline-flexbox;\n  display: inline-flex;\n  -webkit-box-align: center;\n          align-items: center;\n  border: 2rpx solid transparent;\n  box-sizing: border-box;\n}\n.ct-tag--clickable:active, .ct-tag--clickable.ct-clickable--hover {\n  opacity: 0.7;\n}\n.ct-tag--sm {\n  height: 40rpx;\n  padding: 0 16rpx;\n  border-radius: 8rpx;\n}\n.ct-tag--sm .ct-tag__text {\n  font-size: 22rpx;\n}\n.ct-tag--md {\n  height: 48rpx;\n  padding: 0 24rpx;\n  border-radius: 8rpx;\n}\n.ct-tag--md .ct-tag__text {\n  font-size: 26rpx;\n}\n.ct-tag--lg {\n  height: 56rpx;\n  padding: 0 32rpx;\n  border-radius: 16rpx;\n}\n.ct-tag--lg .ct-tag__text {\n  font-size: 28rpx;\n}\n.ct-tag--round {\n  border-radius: 100rpx;\n}\n.ct-tag--primary.ct-tag--filled {\n  background: var(--ct-color-primary);\n}\n.ct-tag--primary.ct-tag--filled .ct-tag__text {\n  color: #fff;\n}\n.ct-tag--primary.ct-tag--outline {\n  border-color: var(--ct-color-primary);\n  background: transparent;\n}\n.ct-tag--primary.ct-tag--outline .ct-tag__text {\n  color: var(--ct-color-primary);\n}\n.ct-tag--primary.ct-tag--light {\n  background: var(--ct-color-primary-light);\n}\n.ct-tag--primary.ct-tag--light .ct-tag__text {\n  color: var(--ct-color-primary);\n}\n.ct-tag--success.ct-tag--filled {\n  background: var(--ct-color-success);\n}\n.ct-tag--success.ct-tag--filled .ct-tag__text {\n  color: #fff;\n}\n.ct-tag--success.ct-tag--outline {\n  border-color: var(--ct-color-success);\n  background: transparent;\n}\n.ct-tag--success.ct-tag--outline .ct-tag__text {\n  color: var(--ct-color-success);\n}\n.ct-tag--success.ct-tag--light {\n  background: var(--ct-color-success-light);\n}\n.ct-tag--success.ct-tag--light .ct-tag__text {\n  color: var(--ct-color-success);\n}\n.ct-tag--warning.ct-tag--filled {\n  background: var(--ct-color-warning);\n}\n.ct-tag--warning.ct-tag--filled .ct-tag__text {\n  color: #fff;\n}\n.ct-tag--warning.ct-tag--outline {\n  border-color: var(--ct-color-warning);\n  background: transparent;\n}\n.ct-tag--warning.ct-tag--outline .ct-tag__text {\n  color: var(--ct-color-warning);\n}\n.ct-tag--warning.ct-tag--light {\n  background: var(--ct-color-warning-light);\n}\n.ct-tag--warning.ct-tag--light .ct-tag__text {\n  color: var(--ct-color-warning);\n}\n.ct-tag--error.ct-tag--filled {\n  background: var(--ct-color-error);\n}\n.ct-tag--error.ct-tag--filled .ct-tag__text {\n  color: #fff;\n}\n.ct-tag--error.ct-tag--outline {\n  border-color: var(--ct-color-error);\n  background: transparent;\n}\n.ct-tag--error.ct-tag--outline .ct-tag__text {\n  color: var(--ct-color-error);\n}\n.ct-tag--error.ct-tag--light {\n  background: var(--ct-color-error-light);\n}\n.ct-tag--error.ct-tag--light .ct-tag__text {\n  color: var(--ct-color-error);\n}\n.ct-tag--default.ct-tag--filled {\n  background: var(--ct-color-text-secondary);\n}\n.ct-tag--default.ct-tag--filled .ct-tag__text {\n  color: #fff;\n}\n.ct-tag--default.ct-tag--outline {\n  border-color: var(--ct-color-text-secondary);\n  background: transparent;\n}\n.ct-tag--default.ct-tag--outline .ct-tag__text {\n  color: var(--ct-color-text-secondary);\n}\n.ct-tag--default.ct-tag--light {\n  background: var(--ct-color-bg-hover);\n}\n.ct-tag--default.ct-tag--light .ct-tag__text {\n  color: var(--ct-color-text-secondary);\n}\n.ct-tag__text {\n  line-height: 1;\n}\n.ct-tag__close {\n  margin-left: 8rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n}";
styleInject(css_248z$4);

/**
 * Helpers
 */
const toRgba = (color, alpha) => {
  if (color.startsWith('#')) {
    const hex = color.replace('#', '');
    const normalized = hex.length === 3 ? hex.split('').map(c => c + c).join('') : hex;
    if (normalized.length !== 6) return color;
    const r = parseInt(normalized.slice(0, 2), 16);
    const g = parseInt(normalized.slice(2, 4), 16);
    const b = parseInt(normalized.slice(4, 6), 16);
    return `rgba(${r}, ${g}, ${b}, ${alpha})`;
  }
  const match = color.match(/rgba?\(([^)]+)\)/);
  if (match) {
    const parts = match[1].split(',').map(val => val.trim());
    if (parts.length >= 3) {
      const [r, g, b] = parts;
      return `rgba(${r}, ${g}, ${b}, ${alpha})`;
    }
  }
  return color;
};
const Tag = ({
  variant = 'filled',
  size = 'md',
  color = 'primary',
  customColor,
  round = false,
  closable = false,
  onClose,
  onClick,
  children,
  className
}) => {
  /**
   * Derived Values
   */
  const isClickable = !!onClick;
  const tagStyle = customColor ? {
    backgroundColor: variant === 'filled' ? customColor : variant === 'light' ? toRgba(customColor, 0.12) : 'transparent',
    borderColor: customColor,
    color: variant === 'filled' ? '#fff' : customColor
  } : undefined;
  /**
   * Callbacks
   */
  const handleClose = e => {
    e.stopPropagation();
    onClose?.();
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-tag', `ct-tag--${variant}`, `ct-tag--${size}`, `ct-tag--${color}`, {
      'ct-tag--round': round,
      'ct-tag--clickable': isClickable
    }, className),
    style: tagStyle,
    hoverClass: isClickable ? 'ct-clickable--hover' : 'none',
    onClick: onClick,
    children: [jsxRuntimeExports.jsx(components.Text, {
      className: "ct-tag__text",
      children: children
    }), closable && jsxRuntimeExports.jsx(components.View, {
      className: "ct-tag__close",
      onClick: handleClose,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "close",
        size: 20
      })
    })]
  });
};

var css_248z$3 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-textarea {\n  position: relative;\n  min-height: 80rpx;\n  padding: 24rpx 32rpx;\n  background: var(--ct-color-bg-card);\n  border-radius: 16rpx;\n  border: 2rpx solid var(--ct-color-border);\n  -webkit-transition: border-color 150ms ease;\n  transition: border-color 150ms ease;\n  box-sizing: border-box;\n}\n.ct-textarea--focused {\n  border-color: var(--ct-color-primary);\n}\n.ct-textarea--disabled {\n  opacity: 0.5;\n  pointer-events: none;\n  background: var(--ct-color-bg-disabled);\n}\n.ct-textarea__inner {\n  width: 100%;\n  min-height: calc(80rpx - 24rpx * 2);\n  font-size: 28rpx;\n  color: var(--ct-color-text-primary);\n  line-height: 1.5;\n}\n.ct-textarea--with-count {\n  min-height: calc(80rpx + 26rpx + 16rpx);\n}\n.ct-textarea__count {\n  text-align: right;\n  margin-top: 16rpx;\n  font-size: 26rpx;\n  color: var(--ct-color-text-tertiary);\n  line-height: 1;\n}";
styleInject(css_248z$3);

const Textarea = ({
  value: propValue,
  defaultValue = '',
  placeholder,
  disabled = false,
  maxLength,
  showCount = false,
  autoHeight = false,
  rows = 3,
  onChange,
  onFocus,
  onBlur,
  className
}) => {
  /**
   * Hooks
   */
  const [value, setValue] = useControllable({
    value: propValue,
    defaultValue,
    onChange
  });
  /**
   * States
   */
  const [focused, setFocused] = require$$0.useState(false);
  /**
   * Callbacks
   */
  const handleInput = e => setValue(e.detail.value);
  const handleFocus = e => {
    setFocused(true);
    onFocus?.(e);
  };
  const handleBlur = e => {
    setFocused(false);
    onBlur?.(e);
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-textarea', {
      'ct-textarea--disabled': disabled,
      'ct-textarea--focused': focused,
      'ct-textarea--with-count': showCount
    }, className),
    children: [jsxRuntimeExports.jsx(components.Textarea, {
      className: "ct-textarea__inner",
      style: !autoHeight ? {
        minHeight: `${rows * 42}rpx`
      } : undefined,
      value: value,
      placeholder: placeholder,
      disabled: disabled,
      maxlength: maxLength,
      autoHeight: autoHeight,
      onInput: handleInput,
      onFocus: handleFocus,
      onBlur: handleBlur
    }), showCount && jsxRuntimeExports.jsx(components.View, {
      className: "ct-textarea__count",
      children: jsxRuntimeExports.jsxs(components.Text, {
        children: [value.length, maxLength && `/${maxLength}`]
      })
    })]
  });
};

/**
 * Helpers
 */
const generateTimeColumns = (precision, minuteStep) => {
  const columns = [];
  const hours = [];
  for (let h = 0; h < 24; h += 1) {
    hours.push({
      label: `${h.toString().padStart(2, '0')}时`,
      value: h
    });
  }
  columns.push({
    options: hours
  });
  if (precision === 'hour') return columns;
  const minutes = [];
  for (let m = 0; m < 60; m += minuteStep) {
    minutes.push({
      label: `${m.toString().padStart(2, '0')}分`,
      value: m
    });
  }
  columns.push({
    options: minutes
  });
  if (precision === 'minute') return columns;
  const seconds = [];
  for (let s = 0; s < 60; s += 1) {
    seconds.push({
      label: `${s.toString().padStart(2, '0')}秒`,
      value: s
    });
  }
  columns.push({
    options: seconds
  });
  return columns;
};
const TimePicker = ({
  open,
  value,
  defaultValue,
  precision = 'minute',
  title = '选择时间',
  minuteStep = 1,
  onConfirm,
  onCancel,
  onClose
}) => {
  /**
   * Derived Values
   */
  const safeMinuteStep = Math.max(1, Math.min(59, minuteStep));
  const columns = require$$0.useMemo(() => generateTimeColumns(precision, safeMinuteStep), [precision, safeMinuteStep]);
  /**
   * Render Helpers
   */
  const timeToValue = time => {
    if (!time) return undefined;
    const parts = time.split(':').map(Number);
    if (precision === 'hour') {
      return [parts[0]];
    }
    if (precision === 'minute') {
      return parts.slice(0, 2);
    }
    return parts;
  };
  const valueToTime = val => {
    const [hour, minute = 0, second = 0] = val;
    if (precision === 'hour') {
      return `${hour.toString().padStart(2, '0')}`;
    }
    if (precision === 'minute') {
      return `${hour.toString().padStart(2, '0')}:${minute.toString().padStart(2, '0')}`;
    }
    return `${hour.toString().padStart(2, '0')}:${minute.toString().padStart(2, '0')}:${second.toString().padStart(2, '0')}`;
  };
  /**
   * Callbacks
   */
  const handleConfirm = val => {
    onConfirm?.(valueToTime(val));
  };
  return jsxRuntimeExports.jsx(Picker, {
    open: open,
    title: title,
    columns: columns,
    value: timeToValue(value),
    defaultValue: timeToValue(defaultValue ?? (precision === 'hour' ? '00' : '00:00')),
    onConfirm: handleConfirm,
    onCancel: onCancel,
    onClose: onClose
  });
};

var css_248z$2 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-toast {\n  position: fixed;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 800;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  pointer-events: none;\n}\n.ct-toast__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  background: transparent;\n  pointer-events: auto;\n}\n.ct-toast__content {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  padding: 32rpx 40rpx;\n  background: rgba(0, 0, 0, 0.75);\n  border-radius: 24rpx;\n  max-width: 70%;\n  pointer-events: auto;\n}\n.ct-toast__content--top {\n  margin-bottom: auto;\n  margin-top: 20vh;\n}\n.ct-toast__content--bottom {\n  margin-top: auto;\n  margin-bottom: 20vh;\n}\n.ct-toast__text {\n  margin-top: 16rpx;\n  font-size: 28rpx;\n  color: #fff;\n  text-align: center;\n  word-break: break-word;\n}";
styleInject(css_248z$2);

/**
 * Constants
 */
const ICON_MAP = {
  success: 'check-circle-fill',
  error: 'close-circle-fill',
  warning: 'warning-circle',
  info: 'info-circle'
};
const ToastView = ({
  open,
  type = 'info',
  content,
  position = 'center',
  duration = 2000,
  icon,
  mask = false,
  onClose
}) => {
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (!open || duration <= 0) return;
    const timer = setTimeout(() => onClose?.(), duration);
    return () => clearTimeout(timer);
  }, [duration, onClose, open]);
  /**
   * Early Returns
   */
  if (!open) return null;
  /**
   * Render Helpers
   */
  const renderIcon = () => {
    if (icon) return icon;
    if (type === 'loading') {
      return jsxRuntimeExports.jsx(Loading, {
        type: "spinner",
        size: "md",
        color: "#fff"
      });
    }
    const iconName = ICON_MAP[type];
    return iconName ? jsxRuntimeExports.jsx(Icon, {
      name: iconName,
      size: 48,
      color: "#fff"
    }) : null;
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: "ct-toast",
    catchMove: mask,
    children: [mask && jsxRuntimeExports.jsx(components.View, {
      className: "ct-toast__mask"
    }), jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-toast__content', `ct-toast__content--${position}`),
      children: [renderIcon(), jsxRuntimeExports.jsx(components.Text, {
        className: "ct-toast__text",
        children: content
      })]
    })]
  });
};

/**
 * Constants
 */
const isH5 = process.env.TARO_ENV === 'h5';
/**
 * States
 */
let toastRoot = null;
let toastContainer = null;
/**
 * Helpers
 */
const normalizeOptions = options => typeof options === 'string' ? {
  content: options
} : options;
const resolveTaroIcon = type => {
  if (type === 'success') return 'success';
  if (type === 'loading') return 'loading';
  return 'none';
};
const ensureContainer = () => {
  if (toastContainer || typeof document === 'undefined') return;
  toastContainer = document.createElement('div');
  document.body.appendChild(toastContainer);
  toastRoot = client.createRoot(toastContainer);
};
/**
 * Exports
 */
const hideToast = () => {
  if (!isH5) {
    Taro.hideToast();
    return;
  }
  if (toastRoot) {
    toastRoot.unmount();
    toastRoot = null;
  }
  if (toastContainer) {
    toastContainer.remove();
    toastContainer = null;
  }
};
const showToast = options => {
  const config = normalizeOptions(options);
  if (!isH5) {
    if (process.env.NODE_ENV !== 'production' && (config.position || config.icon)) {
      console.warn('[Toast] Non-H5 platforms only support basic icon and duration options.');
    }
    const duration = config.duration ?? 2000;
    Taro.showToast({
      title: config.content,
      icon: resolveTaroIcon(config.type),
      duration,
      mask: config.mask ?? false
    });
    if (config.onClose) {
      setTimeout(() => config.onClose?.(), duration);
    }
    return;
  }
  ensureContainer();
  if (!toastRoot) return;
  const handleClose = () => {
    config.onClose?.();
    hideToast();
  };
  const props = {
    open: true,
    content: config.content,
    type: config.type ?? 'info',
    position: config.position ?? 'center',
    duration: config.duration ?? 2000,
    icon: config.icon,
    mask: config.mask ?? false,
    onClose: handleClose
  };
  toastRoot.render(jsxRuntimeExports.jsx(ToastView, {
    ...props
  }));
};

const Toast = ToastView;
Toast.show = showToast;
Toast.success = (content, duration) => {
  showToast({
    content,
    type: 'success',
    duration
  });
};
Toast.error = (content, duration) => {
  showToast({
    content,
    type: 'error',
    duration
  });
};
Toast.loading = (content = '加载中...') => {
  showToast({
    content,
    type: 'loading',
    duration: 0,
    mask: true
  });
};
Toast.hide = hideToast;

var css_248z$1 = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-tooltip {\n  position: relative;\n  display: inline-block;\n}\n.ct-tooltip__trigger {\n  display: inline-block;\n}\n.ct-tooltip__content {\n  position: fixed;\n  z-index: 700;\n  padding: 16rpx 24rpx;\n  border-radius: 16rpx;\n  font-size: 26rpx;\n  max-width: 400rpx;\n  word-wrap: break-word;\n  -webkit-transform: translateX(-50%);\n          transform: translateX(-50%);\n}\n.ct-tooltip__content--dark {\n  background: rgba(0, 0, 0, 0.8);\n  color: #fff;\n}\n.ct-tooltip__content--dark .ct-tooltip__arrow {\n  border-color: rgba(0, 0, 0, 0.8);\n}\n.ct-tooltip__content--light {\n  background: var(--ct-color-bg);\n  color: var(--ct-color-text-primary);\n  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);\n}\n.ct-tooltip__content--light .ct-tooltip__arrow {\n  border-color: var(--ct-color-bg);\n}\n.ct-tooltip__content--top, .ct-tooltip__content--top-start, .ct-tooltip__content--top-end {\n  -webkit-transform: translateX(-50%) translateY(-100%);\n          transform: translateX(-50%) translateY(-100%);\n}\n.ct-tooltip__content--top .ct-tooltip__arrow, .ct-tooltip__content--top-start .ct-tooltip__arrow, .ct-tooltip__content--top-end .ct-tooltip__arrow {\n  bottom: -12rpx;\n  border-width: 12rpx 12rpx 0;\n  border-bottom-color: transparent;\n}\n.ct-tooltip__content--bottom .ct-tooltip__arrow, .ct-tooltip__content--bottom-start .ct-tooltip__arrow, .ct-tooltip__content--bottom-end .ct-tooltip__arrow {\n  top: -12rpx;\n  border-width: 0 12rpx 12rpx;\n  border-top-color: transparent;\n}\n.ct-tooltip__content--top-start, .ct-tooltip__content--bottom-start {\n  -webkit-transform: translateY(0);\n          transform: translateY(0);\n}\n.ct-tooltip__content--top-start .ct-tooltip__arrow, .ct-tooltip__content--bottom-start .ct-tooltip__arrow {\n  left: 24rpx;\n}\n.ct-tooltip__content--top-end, .ct-tooltip__content--bottom-end {\n  -webkit-transform: translateX(-100%);\n          transform: translateX(-100%);\n}\n.ct-tooltip__content--top-end .ct-tooltip__arrow, .ct-tooltip__content--bottom-end .ct-tooltip__arrow {\n  right: 24rpx;\n  left: auto;\n}\n.ct-tooltip__content--left {\n  -webkit-transform: translateX(-100%) translateY(-50%);\n          transform: translateX(-100%) translateY(-50%);\n}\n.ct-tooltip__content--left .ct-tooltip__arrow {\n  right: -12rpx;\n  border-width: 12rpx 0 12rpx 12rpx;\n  border-right-color: transparent;\n}\n.ct-tooltip__content--right {\n  -webkit-transform: translateY(-50%);\n          transform: translateY(-50%);\n}\n.ct-tooltip__content--right .ct-tooltip__arrow {\n  left: -12rpx;\n  border-width: 12rpx 12rpx 12rpx 0;\n  border-left-color: transparent;\n}\n.ct-tooltip__arrow {\n  position: absolute;\n  width: 0;\n  height: 0;\n  border-style: solid;\n  border-color: inherit;\n  left: 50%;\n  margin-left: -12rpx;\n}\n.ct-tooltip__text {\n  line-height: 1.5;\n}";
styleInject(css_248z$1);

/**
 * Main Component
 */
const Tooltip = ({
  content,
  open,
  defaultOpen = false,
  onOpenChange,
  placement = 'top',
  trigger = 'click',
  children,
  theme = 'dark',
  showDelay = 0,
  className
}) => {
  /**
   * Hooks
   */
  const [isOpen, setIsOpen] = useControllable({
    value: open,
    defaultValue: defaultOpen,
    onChange: onOpenChange
  });
  /**
   * States
   */
  const [position, setPosition] = require$$0.useState({
    top: 0,
    left: 0
  });
  /**
   * Refs
   */
  const triggerRef = require$$0.useRef(`tooltip-${Date.now()}`);
  const timerRef = require$$0.useRef();
  /**
   * Callbacks
   */
  const calculatePosition = () => {
    const query = Taro.createSelectorQuery();
    query.select(`#${triggerRef.current}`).boundingClientRect();
    query.selectViewport().scrollOffset();
    query.exec(res => {
      if (!res[0]) return;
      const rect = res[0];
      const scrollTop = res[1]?.scrollTop || 0;
      let top = 0;
      let left = rect.left + rect.width / 2;
      switch (placement) {
        case 'top':
        case 'top-start':
        case 'top-end':
          top = rect.top + scrollTop - 16;
          break;
        case 'bottom':
        case 'bottom-start':
        case 'bottom-end':
          top = rect.bottom + scrollTop + 16;
          break;
        case 'left':
          top = rect.top + scrollTop + rect.height / 2;
          left = rect.left - 16;
          break;
        case 'right':
          top = rect.top + scrollTop + rect.height / 2;
          left = rect.right + 16;
          break;
      }
      if (placement === 'top-start' || placement === 'bottom-start') {
        left = rect.left;
      } else if (placement === 'top-end' || placement === 'bottom-end') {
        left = rect.right;
      }
      setPosition({
        top,
        left
      });
    });
  };
  const handleClick = () => {
    if (trigger !== 'click') return;
    if (timerRef.current) clearTimeout(timerRef.current);
    if (!isOpen && showDelay > 0) {
      timerRef.current = setTimeout(() => setIsOpen(true), showDelay);
    } else {
      setIsOpen(!isOpen);
    }
  };
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (isOpen) {
      calculatePosition();
    }
    return () => {
      if (timerRef.current) clearTimeout(timerRef.current);
    };
  }, [isOpen]);
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-tooltip', className),
    children: [jsxRuntimeExports.jsx(components.View, {
      id: triggerRef.current,
      className: "ct-tooltip__trigger",
      onClick: handleClick,
      children: children
    }), isOpen && jsxRuntimeExports.jsxs(components.View, {
      className: classNames('ct-tooltip__content', `ct-tooltip__content--${placement}`, `ct-tooltip__content--${theme}`),
      style: {
        top: `${position.top}px`,
        left: `${position.left}px`
      },
      onClick: () => setIsOpen(false),
      children: [jsxRuntimeExports.jsx(components.View, {
        className: "ct-tooltip__arrow"
      }), jsxRuntimeExports.jsx(components.Text, {
        className: "ct-tooltip__text",
        children: content
      })]
    })]
  });
};

var css_248z = ":root {\n  --ct-color-primary: #20c6ff;\n  --ct-color-primary-hover: #1daedb;\n  --ct-color-primary-light: #e9f9ff;\n  --ct-color-primary-dark: #0891b2;\n  --ct-color-success: #10b981;\n  --ct-color-success-light: #d1fae5;\n  --ct-color-warning: #f59e0b;\n  --ct-color-warning-light: #fef3c7;\n  --ct-color-error: #ef4444;\n  --ct-color-error-light: #fee2e2;\n  --ct-color-info: #3b82f6;\n  --ct-color-info-light: #dbeafe;\n  --ct-color-text-primary: #1b1b1b;\n  --ct-color-text-secondary: #64748b;\n  --ct-color-text-tertiary: #94a3b8;\n  --ct-color-text-quaternary: #cbd5e1;\n  --ct-color-text-disabled: #cbd5e1;\n  --ct-color-text-inverse: #ffffff;\n  --ct-color-fill-secondary: #f1f5f9;\n  --ct-color-fill-tertiary: #e2e8f0;\n  --ct-color-bg-page: #f8fafc;\n  --ct-color-bg-container: #ffffff;\n  --ct-color-bg-base: #ffffff;\n  --ct-color-bg-card: #ffffff;\n  --ct-color-bg: #ffffff;\n  --ct-color-bg-secondary: #f8fafc;\n  --ct-color-bg-hover: #f1f5f9;\n  --ct-color-bg-active: #e2e8f0;\n  --ct-color-bg-disabled: #f1f5f9;\n  --ct-color-bg-mask: rgba(0, 0, 0, 0.5);\n  --ct-color-bg-mask-strong: rgba(0, 0, 0, 0.7);\n  --ct-color-border: #e2e8f0;\n  --ct-color-border-light: #f1f5f9;\n  --ct-color-border-focus: var(--ct-color-primary);\n}\n\n.ct-upload {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  flex-wrap: wrap;\n}\n.ct-upload__item, .ct-upload__trigger {\n  width: 160rpx;\n  height: 160rpx;\n  margin-right: 16rpx;\n  margin-bottom: 16rpx;\n  border-radius: 16rpx;\n  overflow: hidden;\n}\n.ct-upload__item {\n  position: relative;\n}\n.ct-upload__image {\n  width: 100%;\n  height: 100%;\n}\n.ct-upload__mask {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-orient: vertical;\n  -webkit-box-direction: normal;\n          flex-direction: column;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  background: var(--ct-color-bg-mask);\n}\n.ct-upload__mask--error {\n  background: var(--ct-color-bg-mask-strong);\n}\n.ct-upload__error {\n  margin-top: 8rpx;\n  font-size: 22rpx;\n  color: #fff;\n}\n.ct-upload__delete {\n  position: absolute;\n  top: 0;\n  right: 0;\n  width: 36rpx;\n  height: 36rpx;\n  background: var(--ct-color-bg-mask);\n  border-radius: 0 0 0 16rpx;\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n}\n.ct-upload__trigger {\n  display: -webkit-box;\n  display: -ms-flexbox;\n  display: flex;\n  -webkit-box-align: center;\n          align-items: center;\n  -webkit-box-pack: center;\n          justify-content: center;\n  background: var(--ct-color-fill-secondary);\n  border: 2rpx dashed var(--ct-color-border);\n}\n.ct-upload__trigger:active, .ct-upload__trigger.ct-clickable--hover {\n  opacity: 0.7;\n}";
styleInject(css_248z);

/**
 * Main Component
 */
const Upload = ({
  fileList: propFileList,
  defaultFileList = [],
  accept = 'image',
  maxCount = 9,
  maxSize = 10240,
  multiple = true,
  disabled = false,
  preview = true,
  deletable = true,
  url,
  headers,
  data,
  name = 'file',
  customUpload,
  onChange,
  onBeforeUpload,
  onRemove,
  onPreview,
  className
}) => {
  /**
   * Hooks
   */
  const [fileList, setFileList] = useControllable({
    value: propFileList,
    defaultValue: defaultFileList,
    onChange
  });
  /**
   * Derived Values
   */
  const canUpload = !disabled && fileList.length < maxCount;
  /**
   * Callbacks
   */
  const updateFile = (uid, patch) => {
    const next = fileList.map(item => item.uid === uid ? {
      ...item,
      ...patch
    } : item);
    setFileList(next);
  };
  const uploadFile = async file => {
    if (onBeforeUpload) {
      const allow = await onBeforeUpload(file.raw ?? file);
      if (allow === false) {
        updateFile(file.uid, {
          status: 'error',
          message: '上传已取消'
        });
        return;
      }
    }
    if (customUpload) {
      try {
        const uploadedUrl = await customUpload(file);
        updateFile(file.uid, {
          status: 'done',
          url: uploadedUrl
        });
      } catch (error) {
        updateFile(file.uid, {
          status: 'error',
          message: '上传失败'
        });
      }
      return;
    }
    if (!url) {
      updateFile(file.uid, {
        status: 'error',
        message: '缺少上传地址'
      });
      return;
    }
    const resolveErrorMessage = responseData => {
      if (!responseData) return undefined;
      if (typeof responseData === 'string') {
        try {
          const parsed = JSON.parse(responseData);
          return parsed?.message || parsed?.error || responseData;
        } catch (error) {
          return responseData;
        }
      }
      if (typeof responseData === 'object') {
        return responseData.message || responseData.error;
      }
      return undefined;
    };
    try {
      const res = await new Promise((resolve, reject) => {
        const uploadTask = Taro.uploadFile({
          url,
          filePath: file.url || '',
          name,
          header: headers,
          formData: data,
          success: resolve,
          fail: reject
        });
        uploadTask?.onProgressUpdate?.(progress => {
          updateFile(file.uid, {
            percent: progress?.progress ?? progress?.percent ?? 0
          });
        });
      });
      if (res?.statusCode && res.statusCode >= 400) {
        updateFile(file.uid, {
          status: 'error',
          message: resolveErrorMessage(res.data) || '上传失败'
        });
        return;
      }
      updateFile(file.uid, {
        status: 'done',
        percent: 100
      });
    } catch (error) {
      updateFile(file.uid, {
        status: 'error',
        message: '上传失败'
      });
    }
  };
  const handleChoose = async () => {
    if (disabled || fileList.length >= maxCount) return;
    const count = multiple ? maxCount - fileList.length : 1;
    try {
      let rawFiles = [];
      if (accept === 'video') {
        const res = await Taro.chooseVideo({
          sourceType: ['album', 'camera']
        });
        rawFiles = [{
          path: res.tempFilePath,
          size: res.size,
          name: res.tempFilePath?.split('/').pop()
        }];
      } else if (accept === 'all' && Taro.chooseMessageFile) {
        const res = await Taro.chooseMessageFile({
          count,
          type: 'file'
        });
        rawFiles = (res?.tempFiles || []).map(file => ({
          path: file.path,
          size: file.size,
          name: file.name
        }));
      } else {
        const res = await Taro.chooseImage({
          count,
          sizeType: ['compressed'],
          sourceType: ['album', 'camera']
        });
        rawFiles = (res.tempFiles || []).map(file => ({
          path: file.path,
          size: file.size,
          name: file.originalFileObj?.name
        }));
        if (!rawFiles.length && res.tempFilePaths?.length) {
          rawFiles = res.tempFilePaths.map(path => ({
            path
          }));
        }
      }
      const newFiles = rawFiles.map((file, index) => ({
        uid: `${Date.now()}-${index}`,
        url: file.path,
        name: file.name,
        size: file.size ? Math.round(file.size / 1024) : undefined,
        raw: file,
        status: 'uploading',
        percent: 0
      }));
      setFileList([...fileList, ...newFiles]);
      newFiles.forEach(file => {
        const isOversize = maxSize && file.size !== undefined && file.size > maxSize;
        if (isOversize) {
          updateFile(file.uid, {
            status: 'error',
            message: `文件大小超过 ${maxSize}KB`
          });
          return;
        }
        uploadFile(file);
      });
    } catch (error) {
      // user canceled
    }
  };
  const handleRemove = async (file, index) => {
    if (onRemove) {
      const result = await onRemove(file, index);
      if (result === false) return;
    }
    const next = [...fileList];
    next.splice(index, 1);
    setFileList(next);
  };
  const handlePreview = file => {
    if (!preview) return;
    onPreview?.(file);
  };
  return jsxRuntimeExports.jsxs(components.View, {
    className: classNames('ct-upload', className),
    children: [fileList.map((file, index) => jsxRuntimeExports.jsxs(components.View, {
      className: "ct-upload__item",
      children: [jsxRuntimeExports.jsx(components.Image, {
        className: "ct-upload__image",
        src: file.url || '',
        mode: "aspectFill",
        onClick: () => handlePreview(file)
      }), file.status === 'uploading' && jsxRuntimeExports.jsx(components.View, {
        className: "ct-upload__mask",
        children: jsxRuntimeExports.jsx(Loading, {
          type: "spinner",
          size: "sm",
          color: "#fff"
        })
      }), file.status === 'error' && jsxRuntimeExports.jsxs(components.View, {
        className: "ct-upload__mask ct-upload__mask--error",
        children: [jsxRuntimeExports.jsx(Icon, {
          name: "close-circle",
          size: 48,
          color: "#fff"
        }), jsxRuntimeExports.jsx(components.Text, {
          className: "ct-upload__error",
          children: file.message || '上传失败'
        })]
      }), deletable && !disabled && jsxRuntimeExports.jsx(components.View, {
        className: "ct-upload__delete",
        onClick: () => handleRemove(file, index),
        children: jsxRuntimeExports.jsx(Icon, {
          name: "close",
          size: 24,
          color: "#fff"
        })
      })]
    }, file.uid)), canUpload && jsxRuntimeExports.jsx(components.View, {
      className: "ct-upload__trigger",
      onClick: handleChoose,
      children: jsxRuntimeExports.jsx(Icon, {
        name: "plus",
        size: 48,
        color: "var(--ct-color-text-quaternary)"
      })
    })]
  });
};

/**
 * Main Hook
 */
function useDisclosure(defaultOpen = false) {
  const [isOpen, setIsOpen] = require$$0.useState(defaultOpen);
  const open = require$$0.useCallback(() => setIsOpen(true), []);
  const close = require$$0.useCallback(() => setIsOpen(false), []);
  const toggle = require$$0.useCallback(() => setIsOpen(prev => !prev), []);
  return {
    isOpen,
    open,
    close,
    toggle,
    setIsOpen
  };
}

/**
 * Main Hook
 */
function useOverlayAnimation({
  open,
  duration = OVERLAY_ANIMATION_DURATION
}) {
  /**
   * States
   */
  const [visible, setVisible] = require$$0.useState(false);
  const [animating, setAnimating] = require$$0.useState(false);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (open) {
      setVisible(true);
      Taro.nextTick(() => {
        setAnimating(true);
      });
    } else {
      setAnimating(false);
      const timer = setTimeout(() => {
        setVisible(false);
      }, duration);
      return () => clearTimeout(timer);
    }
    return undefined;
  }, [open, duration]);
  return {
    visible,
    animating
  };
}

/**
 * Main Hook
 */
function useTimeout(callback, delay) {
  /**
   * Refs
   */
  const savedCallback = require$$0.useRef(callback);
  const timeoutRef = require$$0.useRef();
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    savedCallback.current = callback;
  }, [callback]);
  /**
   * Callbacks
   */
  const clear = require$$0.useCallback(() => {
    if (timeoutRef.current) {
      clearTimeout(timeoutRef.current);
    }
  }, []);
  const reset = require$$0.useCallback(() => {
    clear();
    if (delay !== null) {
      timeoutRef.current = setTimeout(() => savedCallback.current(), delay);
    }
  }, [clear, delay]);
  /**
   * Effects
   */
  require$$0.useEffect(() => {
    if (delay !== null) {
      timeoutRef.current = setTimeout(() => savedCallback.current(), delay);
      return clear;
    }
    return undefined;
  }, [clear, delay]);
  return {
    clear,
    reset
  };
}

/**
 * Constants
 */
let globalZIndex = 500;
/**
 * Main Hook
 */
function useZIndex(baseZ = 500) {
  /**
   * States
   */
  const [zIndex, setZIndex] = require$$0.useState(baseZ);
  /**
   * Callbacks
   */
  const acquire = require$$0.useCallback(() => {
    globalZIndex += 2;
    setZIndex(globalZIndex);
    return globalZIndex;
  }, []);
  const release = require$$0.useCallback(() => {
    // optional: reduce globalZIndex when releasing
  }, []);
  return {
    zIndex,
    acquire,
    release
  };
}

exports.Accordion = Accordion;
exports.ActionSheet = ActionSheet;
exports.Avatar = Avatar;
exports.Badge = Badge;
exports.BottomSheet = BottomSheet;
exports.Breadcrumb = Breadcrumb;
exports.Button = Button;
exports.Calendar = Calendar;
exports.Card = Card;
exports.Cascader = Cascader;
exports.Checkbox = Checkbox;
exports.Collapse = Collapse;
exports.ConfigProvider = ConfigProvider;
exports.DatePicker = DatePicker;
exports.Divider = Divider;
exports.Dropdown = Dropdown;
exports.DropdownItem = DropdownItem;
exports.DropdownMenu = DropdownMenu;
exports.Empty = Empty;
exports.Form = Form;
exports.Grid = Grid;
exports.Icon = Icon;
exports.ImagePreview = ImagePreview;
exports.IndexList = IndexList;
exports.InfiniteScroll = InfiniteScroll;
exports.Input = Input;
exports.List = List;
exports.Loading = Loading;
exports.Modal = Modal;
exports.NavBar = NavBar;
exports.NoticeBar = NoticeBar;
exports.Pagination = Pagination;
exports.Picker = Picker;
exports.PickerView = PickerView;
exports.Popover = Popover;
exports.Progress = Progress;
exports.PullRefresh = PullRefresh;
exports.Radio = Radio;
exports.Rate = Rate;
exports.Result = Result;
exports.SafeArea = SafeArea;
exports.SearchBar = SearchBar;
exports.Select = Select;
exports.Selector = Selector;
exports.SideBar = SideBar;
exports.Skeleton = Skeleton;
exports.Slider = Slider;
exports.Stepper = Stepper;
exports.Steps = Steps;
exports.SwipeAction = SwipeAction;
exports.Swiper = Swiper;
exports.Switch = Switch;
exports.TabBar = TabBar;
exports.Table = Table;
exports.Tabs = Tabs;
exports.Tag = Tag;
exports.Textarea = Textarea;
exports.TimePicker = TimePicker;
exports.Toast = Toast;
exports.Tooltip = Tooltip;
exports.Upload = Upload;
exports.useConfig = useConfig;
exports.useControllable = useControllable;
exports.useDisclosure = useDisclosure;
exports.useOverlayAnimation = useOverlayAnimation;
exports.useTimeout = useTimeout;
exports.useTouch = useTouch;
exports.useZIndex = useZIndex;
exports.zhCN = zhCN;
//# sourceMappingURL=index.cjs.map
